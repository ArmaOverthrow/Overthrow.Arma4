---
id: BUG-088
title: "Multiplayer: the player is never given a faction or a group — no commanding, no group menu, recruits stay in their civilian group"
status: closed
priority: high
linkedFeature: resistance/recruits
createdAt: 2026-08-06T00:00:00.000Z
updatedAt: 2026-08-06T08:00:00.000Z
---

**Reported by:** user play-test on a dedicated server (2026-08-06), plus GitHub issue **#147** ("Groups/recruits not working on Dedicated Server", open since Overthrow 1.3.33 / Reforger 1.6.0.119).

## Symptom

On a dedicated server (and, by the same mechanism, any hosted session — anything with replication running):

- Pressing the AI-commanding key (`~`) reports **"Commanding is only available to group leaders."**
- The player has **no faction assigned at all** (confirmed in admin mode).
- The player has **no group indicator**; a recruited civilian keeps his own indicator (green FIA, "Non-Combatant(s)") and stays in his original civilian group (e.g. `Bnza 251`) instead of joining the player.
- The vanilla Group Menu lists a single foreign-faction group; **Create new group** and **Join group** do nothing.
- Identical whether the recruit was just recruited or respawned after a reconnect.

Works in solo, which is what hid it: with no replication session the dropped RPCs below execute locally.

## Root cause — the headline: the faction manager was never replicated

`OVT_OverthrowFactionManager : SCR_FactionManager` **is** the game's faction manager, and its prefab
`Prefabs/GameMode/OVT_OverthrowFactionManager.et` had **no `components {}` block at all** — so no `RplComponent`. Vanilla's equivalent (`Prefabs/MP/Managers/Factions/FactionManager_Base.et`) carries `RplComponent { Streamable Disabled }`.

`SCR_FactionManager.m_aPlayerFactionInfo` is `[RplProp(onRplName: "OnPlayerFactionInfoChanged")]` and `UpdatePlayerFaction_S` ends with `Replication.BumpMe()` — but with no RplComponent on the entity, **none of it ever leaves the server**. The client's `m_MappedPlayerFactionInfo` is therefore empty forever, and it is the *only* source for `GetPlayerFaction()`:

- `GetPlayerFaction(playerId)` on the client → null → **admin shows the player unassigned** even though the server log says `now=FIA`.
- `GetLocalPlayerFaction()` → null → `SCR_GroupSubMenuBase.UpdateGroups()` returns before listing anything → **Group Menu inert, Create/Join do nothing**.
- `SCR_GroupsManagerComponent.GetPlayerGroup()` resolves the group *through* the faction → null → `IsPlayerLeaderOwnGroup()` false → **"Commanding is only available to group leaders."**

Single-player is immune: client and server share one in-process map, so nothing needs to replicate. That is why this survived years of solo play and issue #147 has been open since 1.3.33.

**Fix:** added `RplComponent { Streamable Disabled }` (id `{6B1C4E7A0D3F5182}`, verified unused across this repo and the vanilla tree) to `Prefabs/GameMode/OVT_OverthrowFactionManager.et`. The world layers instantiate that prefab and override only `coords`/`Factions`, so every world inherits it.

## Root cause — the server-side half (found first, fixed first, necessary but not sufficient)

Two server-side calls to vanilla **client-request wrappers**. Each is a thin `Rpc(…)` of an `RplRcver.Server` handler, written to be called *by the owning client*; Overthrow calls both from server code, where the request never reaches the authority's assignment path — silently, since neither return value is checked.

**1. The faction is never assigned.** `OVT_SpawnLogic.SetCivilianFaction()` called
`SCR_PlayerFactionAffiliationComponent.RequestFaction()`, so `SetFaction_S()` never ran. This is the head of the chain: `CreateAndJoinGroup()` reads `factionManager.GetPlayerFaction(playerId)` and **returns early when it is null**, so the player's group is never even created.

**2. The player never joins the group.** `OVT_SpawnLogic.CreateAndJoinGroup()` called
`SCR_PlayerControllerGroupComponent.RequestJoinGroup()`. Even with a faction present, the group would be created empty and leaderless.

**Which failure mode inside the wrapper — not fully pinned down, and it does not change the fix.** Two candidates, both bypassed by calling the server-side entry point directly:
- the `Rpc()` never loops back when the caller *is* the authority (the reason vanilla calls `RPC_AskJoinGroup(…)` directly inside `RpcAsk_CreateGroupWithData`); **or**
- `RequestFaction()` bailed at its very first line — `SCR_SpawnLockComponent.TryLock()` fails and it returns false without attempting anything, printing *"Caught request on locked player!"* at `LogLevel.DEBUG`. Overthrow's spawn path is custom and does not drive vanilla's request/unlock cycle, so a stale request lock is plausible.

Note vanilla's own `SCR_GroupsManagerComponent.RejoinPlayer()` calls `RequestJoinGroup()` from the server, which argues *for* the loopback existing — hence the honest uncertainty. **Cheap discriminator:** grep the dedicated-server log (run with `-logLevel debug`) for `Caught request on locked player`.

Everything else follows from an empty/absent group, because group membership resolves *through* the faction (`SCR_GroupsManagerComponent.GetPlayerGroup()` → `GetPlayableGroupsByFaction(playerFaction)`):

- `IsPlayerLeaderOwnGroup()` is false → `SCR_CommandingManagerComponent` refuses commanding.
- `OVT_RecruitManagerComponent.AddRecruitToPlayerGroup()` bails at its `groupId == -1` / `GetLeaderID() != playerId` guards (silently, before this fix) → the recruit is never moved into the player's slave group.
- Group Menu: `CanCreateNewGroup()` returns false while an empty group of the faction exists (`TryFindEmptyGroup`), which is why **Create new group** is inert.

## Fix (applied)

Use vanilla's own server-side entry points — the same ones vanilla calls from its own server code:

- `Scripts/Game/Respawn/Logic/OVT_SpawnLogic.c` — `SetCivilianFaction()` now calls `factionComponent.SetFaction_S(playerFaction)` when `Replication.IsServer()` (as `SCR_ReconnectComponent` and the faction-affiliation persistence serializer do), and skips the call entirely when the affiliation already matches.
- `Scripts/Game/Respawn/Logic/OVT_SpawnLogic.c` — `CreateAndJoinGroup()` now calls `groupController.RPC_AskJoinGroup(groupID)` directly when on the server (exactly what `SCR_PlayerControllerGroupComponent.RpcAsk_CreateGroupWithData` does), which joins the player, triggers `CheckForLeader` → leader, and tells the owning client via `RPC_DoChangeGroupID`.
- `Scripts/Game/Respawn/Logic/OVT_SpawnLogic.c` — the faction/group setup is now scheduled from `OnPlayerEntityChanged_S`, the chokepoint **every** spawn route passes through (fresh creation, restored body, death respawn, and vanilla's `SCR_ReconnectComponent.ApplyData` via `EmitPlayerEntityChange_S`). It used to be scheduled only from `HandoverToPlayer`, which a returning player with a saved body never travels — so on a live save nobody got a faction at all. `CreateAndJoinGroup` is idempotent, so the original call is harmless. `SetCivilianFaction()` also now logs its outcome instead of returning silently down five paths.
- `Scripts/Game/GameMode/Managers/OVT_RecruitManagerComponent.c` — `AddRecruitToPlayerGroup()` now calls `groupController.AddAIToSlaveGroup(recruitEntity, group)` on the server instead of broadcasting `RpcDo_AddRecruitToGroup` to every client with a 6 s pre-delay and a 10 × 2 s entity-resolution ladder so the *owning client* could ask the server to do the same thing. The dead client-side path (`DelayedRpcAddRecruitToGroup`, `RpcDo_AddRecruitToGroup`, `RpcDo_AddRecruitToGroupWithRetry`) is removed. Its silent early-returns now log a warning naming which guard failed.

## How it was actually found

Worth recording, because two plausible fixes changed nothing before the real one surfaced:

1. Fixed the server-side `Request*`-from-server calls → **no visible change**.
2. Moved the faction/group setup to `OnPlayerEntityChanged_S`, the chokepoint every spawn route passes through, and made `SetCivilianFaction` log its outcome → the DS log then proved the **server side was completely correct**: `SetCivilianFaction: player 1 -> FIA (isServer=true, accepted=true, now=FIA)` followed by `Created group 1001 ... Group faction: FIA`.
3. Server right + client wrong ⇒ a replication gap, not a logic bug. Tracing the client's only source for player factions led to the missing `RplComponent`.

**Lesson:** when server state is provably correct and the client disagrees, check that the entity owning the `RplProp` is actually replicated before touching any more logic. A missing `RplComponent` makes `Replication.BumpMe()` a silent no-op.

## Verification — PASSED (dedicated server, 2026-08-06)

Not automatable (MP is uncovered by the test suite), so this was a play-test on the reporter's live dedicated server. Confirmed after deploying the `RplComponent` fix:

- ✅ Player is assigned **FIA** and it is visible client-side (admin panel).
- ✅ AI commanding (`~`) opens with real commands — `IsPlayerLeaderOwnGroup()` now resolves true on the client, which also proves the group membership *and* leader id replicate correctly.
- ✅ Recruits join the player's group and **follow orders**.

**Deliberately still broken, and out of scope here:** the vanilla **Group Menu** itself. With the faction map now replicating, `UpdateGroups()` no longer early-returns, so whatever remains is further in (tile creation, the join-button path, or `CanCreateNewGroup()` → `TryFindEmptyGroup` disabling *Create* while your own one-man group exists — which is vanilla behaviour, not a defect). Ruled out while investigating: `m_bIsPlayable 0` on FIA/US/USSR in the world layer is **not** implicated — that flag gates deploy/faction-selection and the kill-if-not-playable path, and is never read by the group UI. Tracked in `core/player-groups`.

## Related

- Same class as **BUG-045** (officer promotion is a client broadcast that never reaches the server) and **BUG-052** (recruit rename never reached the authority): *"works hosted/solo, silently broken with replication running"*.
- `Scripts/Game/Respawn/Logic/OVT_PersistentRespawnLogic.c:120` carries the identical `RequestFaction()` trap. The class is **dead** (not referenced anywhere, not on the game-mode prefab, which uses `OVT_SpawnLogic`), so it was left alone — delete it or fix it before ever reviving it.
- Still open after this fix: the Group Menu, and players being unable to group up with each other (issue #147's second half). Both scoped into `docs/features/core/player-groups/requirements.md`, now **unblocked** — this fix is play-test-verified.
- **Check other Overthrow-authored replacements of vanilla manager prefabs for missing components.** `OVT_OverthrowFactionManager.et` was written as a bare entity rather than derived from `FactionManager_Base.et`, which is how it lost the `RplComponent`. Any other prefab authored the same way may be missing something vanilla relies on.
