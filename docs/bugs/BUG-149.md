---
id: BUG-149
title: "OVT_MapPlayerLocation carries a vestigial, never-assigned tool-menu entry — m_ToolMenuEntry, an empty Init() and caller-less ZoomInOnPlayer() read as a working registration precedent but have never executed"
status: open
priority: low
linkedFeature: map/core
createdAt: 2026-08-13T00:00:00.000Z
updatedAt: 2026-08-13T00:00:00.000Z
---

## Symptom

`OVT_MapPlayerLocation` declares `protected SCR_MapToolEntry m_ToolMenuEntry` that **nothing ever assigns**; its `override void Init()` is **empty**; and `protected void ZoomInOnPlayer()` — whose only other reference is a **commented-out** `m_ToolMenuEntry.SetColor(...)` line inside its own body — has **zero callers** (`grep -rn "ZoomInOnPlayer" Scripts/` returns exactly one hit, the declaration itself). The shape is unmistakable: someone started to register a "centre the map on me" tool-menu entry and stopped.

**Where:** `Scripts/Game/UI/Map/Visualization/OVT_MapPlayerLocation.c` — symbols `m_ToolMenuEntry`, `Init()`, `ZoomInOnPlayer()`.

## Why it matters beyond tidiness

It reads as a *working precedent for tool-menu registration* and it is not one. `map/map-layers` planning had to establish this by grep before designing its entry point, because a plan that trusted it would have copied a pattern that has never executed. The only working precedents in the tree are vanilla's nine registrants plus `OVT_MapLayersUI`.

## The decision to make (a choice, not a defect with one right answer)

**Delete it**, or **wire it up** as a second tool-menu entry beside the layers entry. The second is now cheap — `OVT_MapLayersUI` contains a working, commented `RegisterToolMenuEntry` call to copy, including the K7 trap (register in `Init()`, never in `OnMapOpen`, because `SCR_MapToolMenuUI.m_aMenuEntries` is only ever inserted into and has no unregister API).

## Provenance

Found by `map/map-layers` Phase 3 (2026-08-11), which edited this class under a standing "leave the class otherwise exactly as-is" rule — full write-up in `docs/features/map/map-layers/findings.md` (F1). Related: BUG-151 (F3) and BUG-152 (F4) live in the same class.
