---
id: BUG-150
title: "5 of 14 m_sDisplayName values in OverthrowMap.conf are raw English literals (Town, Bus Stop, Vehicle, Point of Interest, Waypoint) and render untranslated on the map info panel's type line"
status: closed
priority: medium
linkedFeature: map/core
createdAt: 2026-08-13T00:00:00.000Z
updatedAt: 2026-08-13T12:30:00.000Z
---

## Symptom

`Configs/Map/OverthrowMap.conf` sets `m_sDisplayName` on all 14 location types, and five are **raw English strings rather than localization keys**: `"Town"`, `"Bus Stop"`, `"Vehicle"`, `"Point of Interest"`, `"Waypoint"`. `m_sDisplayName` drives the **type line on the map info panel**, so those five render in English on every non-English client today.

**Where:** `Configs/Map/OverthrowMap.conf` — the `m_sDisplayName` entries in the Town, Bus Stop, Vehicle, Point of Interest and Waypoint `OVT_MapLocationType` blocks. Consumed by `OVT_MapLocationType.GetDisplayName()`. ⚠️ The Shop block also contains five **nested** `OVT_ShopTypeInfo.m_sDisplayName` values — a different field on a different class. Anchor on each block's `m_fVisibilityZoom` to find the right one.

## Fix shape

Each of the five needs a **singular** `.st` sibling id — the English text is already authored: `map/map-layers` added `#OVT-Map_Category_*` ids covering all 14 types in their **plural** forms, so no translation content has to be invented, only the singular forms. This is a localization change with its own gate: five new `.st` ids in `Language/localization_Overthrow.st` (the editable master — ❌ never the generated `.conf` exports), five conf edits, and a Workbench export regeneration by the user.

## Provenance

Found by `map/map-layers` Phase 6 (2026-08-11) — full write-up in `docs/features/map/map-layers/findings.md` (F2). Note that findings file's own warning: the line numbers F2 originally cited went stale within a day; the block symbols above are the durable anchors.
