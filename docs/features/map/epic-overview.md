# Map - Epic Overview

**Epic:** map
**Status:** 🟢 Committed scope COMPLETE (4/4) and fully play-tested; `respawn` COMPLETE; stretch goal 6 (`territory-overlay`) COMPLETE and play-tested — **stretch goal 9 (`influence-overlay`) COMPLETE and play-tested 2026-08-12; stretch goal 7 (`map-layers`) remains code-complete and entirely unobserved**; 1 stretch goal (`shared-markers`) unplanned and now unblocked
**Last Updated:** 2026-08-12

> **This file is the epic marker.** Its presence in `docs/features/map/` is what tells every Beast Mode command (and future Web App / Discord clients) that this folder is an **epic**, not a plain feature. Keep it present and keep the required sections below filled in. It is the epic's equivalent of the project's `docs/overview.md`, scoped to this epic. The master `docs/overview.md` tracks this epic as a **single row**; the per-feature detail lives **here**.

---

## Purpose

This epic owns **everything the player sees and does on the fullscreen map**: the interactive location markers, their info panels, and the map-driven travel verbs (fast travel and bus travel). It exists to land and finish the map rewrite that has been sitting unmerged on the long-dormant `new-map` branch since 2025-08-02 — a config-driven system built on vanilla's own `SCR_MapUIElementContainer`/`SCR_MapUIElement` widgets, replacing the hand-rolled `OVT_MapIcons` parallel-array icon layer and the three flag-based modes inside `OVT_MapContext`.

The branch is **substantially built, not started-from-scratch**: `OVT_OverthrowMapUI` is already wired into `Configs/Map/MapOverthrow.conf` with ten configured location types, and the legacy `OVT_MapIcons` component is already switched off there (`Enabled 0`, `m_bDisableComponent 1`). What remains is to document what was built, close the parity gaps that still keep the legacy code alive, verify the whole thing in multiplayer and on gamepad, and then delete the old system. These features belong together because they share one config (`Configs/Map/OverthrowMap.conf`), one element/info-panel layout pair, and one retirement: none of the legacy map code can be deleted until _all_ of its capabilities exist in the new system.

---

## Features

The constituent features of this epic, in build order. This is the epic's equivalent of `docs/overview.md`'s feature-status table, scoped to the epic. Each feature is a subfolder under `docs/features/map/`.

| #   | Feature           | Status                                                                                                                                                                                       | Tasks                                                                                                                                                                                                                                                                                                                              | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | core              | 🟢 **Documented + bugfixed** — SP-verified; its own verification boxes still unticked (see note)                                                                                             | Built 11/11 · bugs **5/5 closed** · verify **1/8** · cleanup **2/6** (was mis-stated as 1/5)                                                                                                                                                                                                                                       | Discovery + hardening of the shipped map infrastructure: `OVT_OverthrowMapUI`, `OVT_MapLocationType`/`Element`/`Data`, `OVT_OverthrowMapConfig`, the shared layouts, imageset and canvas-layer modules. **BUG-069's four lifecycle defects are avoided structurally.** **All five filed bugs (BUG-133 … BUG-137) are CLOSED — fixed and play-tested 2026-08-10.** BUG-133 was two stacked faults, not the predicted one-liner (wrong widget name _and_ a `FrameSlot.SetSize` that could never size a `LayoutSlot` child); BUG-134 gave the info panel a real `CloseButton` on a new `OverthrowCloseInfoPanel` action; BUG-135 turned out already fixed by `fast-travel`; **BUG-136 was fixed rather than deferred** — per-type `m_fRefreshInterval` polling now re-populates and reconciles markers mid-session; BUG-137 made `OnLocationClicked` reachable. **Still owed:** the `FindAnyWidget` name sweep — D1/D2 were found by spot-checking, not by the audit. ⚠️ **Bookkeeping gap, not necessarily a work gap:** core's own MP/JIP and gamepad rows are still unticked (1/8), but the ground they cover was almost certainly walked by `legacy-retirement`'s combined Phase 7 gate and the 2026-08-11 sweeps. `/update-epic` does not tick another feature's boxes — **run `/update-feature map/core` to reconcile**, ticking only what was actually observed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2   | location-types    | ✅ **COMPLETE** — Phase 7 discharged in full and 7b filed, both 2026-08-11                                                                                                                   | **42/42 (100%)** · verification 6/6 ✅ · **nothing open**                                                                                                                                                                                                                                                                          | **All 4 parity gaps closed 2026-08-10** (Phases 1–6 in one autorun): Vehicle (G1), waypoints (G2), POI registry (G3), bus stops as a marker component (G4). New `OVT_MapMarkerComponent` + `OVT_MapMarkerManagerComponent` registry (client-safe world scan, **no replication**) serves both POIs and bus stops. **`OVT_MapIcons.RegisterPOI` compile-level dependency eliminated** — `legacy-retirement`'s hard gate. 🔴 **N1 house privacy leak fixed** (every player saw every other player's property). Nine types gained real info panels via one additive `map/core` contract extension (K5). Shop scarcity carets + remoteness badge; gun-dealer panel pivoted mid-run by user directive to **signature weapons** (§4.6b). Gates: compile 0 (5956 files), Fast **43**, All **78**. ✅ **Phase 7 (MP/JIP, gamepad, zoom, save) DISCHARGED 2026-08-10** — folded into `legacy-retirement`'s combined gate per settled decision D-4 and reported all green, including the two-client pass that is the only confirmation the N1 house-privacy fix actually isolates per player across clients. ✅ **Phase 7 was then also run directly and reported green by the user 2026-08-11** — V-3 … V-7 plus the F-1 parity sign-off, the latter superseded by `legacy-retirement` having deleted the legacy map outright. Localization verified by measurement (521/521 `.st` ids resolve in `en-us`). ✅ **7b closed 2026-08-11 — the seven findings are filed as BUG-138 … BUG-144**: N4 → **BUG-138** (`map/core`), N5 → **BUG-139** (`resistance/fob`), N6+addendum → **BUG-140**, N17 → **BUG-141**, N18 → **BUG-142**, N19 → **BUG-143**, N14 → **BUG-144** (all `economy/shops`; N14 filed as a design question). Each was re-verified against the tree rather than transcribed, which moved two of them: N5 is also a server-side defect (the `garrison` list is written only by a save load, so the count is 0 on the host too), and **N19 is an economy bug, not a caret bug** — with no town shop stocking a weapon _and_ dealers never in `m_mTownShops`, the +10% scarcity ceiling applies permanently to a dealer's entire inventory and nothing a player buys or sells can move it. BUG-143 now carries the reason `m_bShowWeaponCarets` ships **default-off**. ✅ **The last art dependency closed 2026-08-11** — the user imported redrawn caret icons; the glyph went from 28 px to **54 px** of ink so it nearly fills the unchanged 13×13 row icon (~2× the rendered ink), and magnitude now reads. Solved differently from the plan's "chevrons side by side" prescription and better for it — **no layout change was needed**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 3   | fast-travel       | ✅ **CLOSED 2026-08-11 — built and play-tested green, MP included**                                                                                                                          | **74/74 (100%)** — 5/5 build phases + Phase 6 gate + Phase 0 MP gate                                                                                                                                                                                                                                                               | **All five build phases landed 2026-08-10 in one autorun.** The verb half of parity is closed. **F1/F2 — travel is now server-authoritative**: new `OVT_TravelRequestComponent` on `OVT_OverthrowController` validates, teleports, _then_ charges, so a refused or out-of-bounds teleport cannot take money; the client-side `TeleportPlayer` and client-side money debit are deleted. **F3** off the deprecated comms component. **F4** recruits ride along again, now opt-out via a toggle that prices and moves them from _one_ server-side list — "pay solo, arrive with a squad" is impossible by construction, not by test. **F5** bus travel migrated with **no armed mode at all** — eligibility is re-derived from live position every panel build, so BUG-069 part 2 has no flag left to survive a map close. **F6/F7** dead code deleted, double affordability check resolved. Review caught a **double-charge risk** (a second, unguarded `MapSelect` listener in `OVT_MapContext`) and a **stale-state money path**; both fixed. Gates: compile 0 (5958 files), Fast **44**, All **79**; one Logic case added, proven able to fail. ✅ **Play-test gate DISCHARGED 2026-08-10 — the user ran it and reported all green**, and regenerated all six localization exports. That is what confirms the parts no automated gate can see: the server path actually executing and charging, the recruit toggle rendering and updating live, the R6 panel guard (flagged "unverified, possibly inert" and the toggle's only gamepad path), bus travel end to end, and `KC_R` not stowing the map. ✅ **Its two remaining gaps are now closed too**: `legacy-retirement`'s Phase 7c covered the two-client scenarios this feature's single session could not — **concurrent travel by two clients and JIP travel** — and `legacy-retirement` P1 cut the legacy fast-travel path at `OVT_MainMenuContext.c:218`, so there is no longer a second, contradicting fare model to make money observations ambiguous. ✅ **Phase 0’s own two-client MP matrix was then run directly and reported green by the user 2026-08-11 (F1/F2/N9/N10/N12)**, alongside the post-ship map-stow re-test, and **the feature is CLOSED**. Two things are recorded rather than dropped: the **listen-server-host result short-circuit** is the one untested branch (MP testing used a dedicated server), and F4’s _pre-fix_ observation is permanently unobservable because the fix shipped before anyone watched the bug fail — the post-fix behaviour is what was verified                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 4   | legacy-retirement | ✅ **COMPLETE — built and play-tested green**                                                                                                                                                | **51/51 build + 27/27 verification (100%)**                                                                                                                                                                                                                                                                                        | **The legacy map is deleted.** Phases 0–6 landed 2026-08-10, each gated at **compile exit 0 / 5958 files** (the predicted −1 from `OVT_MapIcons.c`) and **Fast 44**; the final sign-off run confirmed **All 79** — every Phase 0 invariant held, under the standing rule that _a changed test count is a finding to investigate, never a number to update_. Phase 0's baseline was compile 0 / 5959, Fast 44, All 79, tree clean at `28c2f957`. **P1** cut the "Map Info" and "Fast Travel" main-menu rows and their two methods — after relocating the menu's **only** `SetFocusedWidget` call (FINDING A), which then exposed FINDING C: layout order ≠ wiring order, so the relocated focus landed on the _third_ visible row. The user chose to **reorder the menu by frequency of use** (`Place, Build, Resistance, Jobs, Real Estate, Manage Recruits, Character Sheet, Save, Options`) — a deliberate, recorded widening of a deletion-only feature. **P2** stripped `OVT_MapContext` **591 → 79 lines** to four map-_gadget_ helpers, the survivors machine-diffed byte-identical rather than retyped, which **structurally closes BUG-069 part 4** — `grep -rn "SCR_MapEntity.GetOn"` is empty, so no Overthrow context subscribes to any static map invoker and the pattern cannot be reintroduced by an existing file. **P3** deleted `OVT_MapIcons.c` (846 L), `MapIcon.layout`, `MapInfo.layout`, `MapInfo/Modifier.layout` + all three `.meta` files, the `OVT_MapIcons` config block and four dead `.et` attributes — every cut grep-proven dead _before_ it was made, with a bidirectional check catching a near-miss (the deleted block cited `overthrow_mapicons.imageset`, which has 20+ other live consumers). **P4 was a security fix, not a cleanup:** four `RequestFastTravel*` RPCs deleted from `OVT_PlayerCommsComponent` **including their `[RplRpc]` lines**, so the wire surface is gone — `RpcAsk_RequestFastTravel` was a two-line unvalidated, unpaid teleport to an arbitrary `vector` callable by any client; its recruit ring-placement half was extracted and diffed **verbatim identical** to the surviving `OVT_TravelRequestComponent.TeleportRecruits` before being cut. **P5** removed the `OVT-NeedBusStop` string item and its broadcast preset — one deletion out of a **nine-candidate orphan sweep whose default was KEEP**, with the six generated `.conf` exports verifiably untouched. **Retained by explicit decision:** `OVT_MapRestrictedAreas` (FOB rings — **BUG-070's fix must not regress**), the disabled `OVT_MapThreatGrid`, `OVT_MapPlayerLocation`, the bus-stop duplicate-suppression config block, and `HideMap()` (zero callers, kept with a `//!` block naming `map/respawn` as its future consumer — **that turned out to be wrong**: respawn shipped without touching the map _gadget_ at all, so the method is corrected-in-place and still callerless; see respawn's K13). `towns/map-info` archived to `docs/archive/towns-map-info-*.md`. **P6** also rewrote the two in-game Field Manual entries that documented the deleted click-anywhere workflow (FINDING B) — **every replacement sentence is backed by a `file:line`, and six sentences were cut for being unprovable**, including one inherited claim about "intel known about the area" that no panel has ever surfaced. ✅ **Phase 7 DISCHARGED 2026-08-10 — the user ran the full gate and reported all green**, and regenerated the six localization exports (verified in the tree: all six diffed, `OVT-NeedBusStop` absent from every one, rewritten Field Manual text present). **That gate is the only evidence that exists for most of this feature**: four of seven phases edited `.layout`/`.conf`/`.et`/`.st` files, which are invisible to `compile-check.sh` **and** to both test groups — a dangling GUID or half-deleted string block passes every automated gate and fails in the world, so 7a's clean Workbench load is what proves the deletions sound, 7d is what proves FINDING A's fix works, and **7c — two clients, JIP, concurrent travel — is what proves the map's per-player isolation survives a second client**. 🎯 **It discharged two features**: `map/location-types`' outstanding Phase 7 (V-3 … V-7) was folded in per settled decision D-4, closing the epic's stated hard gate. Known non-blockers logged rather than fixed: a **pre-existing orphaned `.meta`** for the _retained_ `OVT_MapThreatGrid` (`{B8F4C6A8C9D3E4F1}`, from `96e6da4d`), ten `Target_*` translations still describing deleted UI (deliberately not machine-translated), and an unguarded `m_aModifiers[modifierData.id]` index in `OVT_MapLocationTown.c:272` — the same defect the deleted `OVT_MapContext` had, moved rather than fixed by the rewrite |
| 5   | respawn           | ✅ **COMPLETE — built and play-tested green**                                                                                                                                                | **152/152 (100%)** — 8 build phases + the full Phase 9 gate                                                                                                                                                                                                                                                                        | Conflict-style respawn picker on a dedicated screen: choose any location you are entitled to spawn at, with **"Respawn at home" always available** even inside a QRF. Free; own eligibility rule set (fast travel's cannot be reused). **Planning corrected three premises in `requirements.md` before a line was written.** `OVT_PersistentRespawnLogic` — the class the requirements build their whole estimate on — is **dead code** (no prefab, no config references it; its own header says so), so the live path is `OVT_SpawnLogic` and the work is _deferring an automatic character creation_, not swapping a position source. `ChargeRespawn` stays exactly where it is, charged at death before any choice: "free" means the _choice_ adds nothing, and nothing in this feature touches the economy. And the identity hazard is the way this ships looking correct and doing nothing: all three map-UI copies of `GetCurrentPlayerID()` resolved the persistent id **through the controlled entity**, which a dead player may not have, and every consumer fails **silently** on the resulting `""` — houses refuse to populate (the N1 privacy fix, fail-closed by design), private camps filter out, and `OVT_Global.GetController()` returns null so the buttons do nothing. Fixed with one client-safe `OVT_Global.GetLocalPersistentId()` built on `SCR_PlayerController.GetLocalPlayerId()`, a no-controlled-entity fallback inside `GetController()`, and the three duplicate bodies routed through it. **Architecture: one rule set, two machines.** The client draws a marker when `CanRespawn` says so; the server independently re-derives the eligible set from its own managers and spawns at **its** recorded vector, never the client's — a client that lies gets home with `OK_FELL_BACK_HOME`. Both ends run the same pure predicates, which is what keeps displayed and enforced availability in agreement. The screen is a **vanilla SPAWNSCREEN map hosted in an Overthrow workspace layout** — the feature's one genuine unknown, so it was spiked first. `SCR_MapEntity.OpenMap` installs its life-state-changed and player-deleted teardown hooks **only** for FULLSCREEN, so a SPAWNSCREEN map is not destroyed by the very death that must open it. 🔴 **The spike's decisive finding: `MapContext` is activated by the MENU PRESET, not by `SCR_MapEntity`.** A workspace layout has no preset, so nothing would have activated it and the map would have rendered perfectly and then ignored every pan, zoom, cursor and click — a failure that looks exactly like "workspace hosting doesn't work" but isn't. The context now activates it by hand every frame. **Additive on `map/core`:** exactly one new virtual (`CanRespawn`, **defaulting to refuse**) and one attribute (`m_bRespawnOnly`), set only in `OverthrowMapRespawn.conf`, so the living map is byte-for-byte unchanged and there is **no runtime mode flag anywhere in the feature** — BUG-069's defect class has nothing to leave armed. **Phase 6 is the high-consequence half**: the death path defers, holds a server-side `m_aAwaitingRespawn` claim, re-asks the client every 5 s **forever rather than timing out** (self-cancelling on entry-gone / player-gone / living character), consumes the claim once so two characters for one death is impossible, takes the chosen position as a **parameter, never stored state**, and degrades to today's immediate spawn with a loud ERROR when the controller prefab lacks the request component. The corpse window it widens from one frame to minutes was audited across **seven** `GetControlledEntity()` sites — the plan listed five and mis-stated which omit the dead check; the missed one (`CreateAndJoinGroup`) uses the _inverted_ test. Gates: compile **0 / 5964 files**, Fast **54**, All **89** (+10 Logic cases, each proven able to fail across five batched inversion passes; **no tier moved**, the expected result since the autotest world has no players and `OnPlayerKilled_S` never fires in it). ⚠️ **Nothing in this feature has ever been executed.** The death path, the screen, both new `.conf`s, both new `.layout`s, three `.et` edits and the new input bindings are invisible to `compile-check.sh` **and** to both test groups — Phase 9 is the only evidence any of it works, and `respawn/context.md` now carries a consolidated **"Still unverified"** list plus a three-signature triage section to run it from                                                                                                                                                                                                                                                                                  |
| 6   | territory-overlay | ✅ **COMPLETE — play-tested green (SP + MP) 2026-08-11 and closed by the user**                                                                                                              | **133/136 rows** · the three open rows are open _on purpose_: Q-1's frame-cost numbers were never recorded, Q-4's broken-texture path never executed, and the six localization exports are the user's to regenerate                                                                                                                | Weighted-Voronoi territory shading over towns, bases, radio towers and FOBs, coast-clipped and border-smoothed. **The plan's "clipped to an influence radius" is dead** — user decision **D6** (2026-08-11, after the first render) removed the maximum influence radius outright: a ray now stops only on a rival or the coastline, so the map is a genuine partition rather than islands of colour with dead ground between them. **D7** (after the second render) then restricted neutral bands to **inter-faction** frontiers — `OVT_TerritoryStop.RIVAL` means _another site won_, which says nothing about faction, so two same-faction neighbours were getting a neutral band down the middle of uncontested ground. **Phase 1 extended the `OVT_MapCanvasLayer` contract for the whole epic** (`m_iDrawOrder` / `m_sLayerId` / `m_sDisplayName` / `SetLayerVisible`, `CacheProjection` / `ProjectWorld`, optional textures on `DrawCircle`, the removed `Count() > 0` guard) and added `OVT_MapCanvasCompositor`, which **fixes a real pre-existing defect**: every canvas layer resolved the same `CanvasWidget` and called `SetDrawCommands` with its own list, so two live layers overwrote each other — invisible only because the threat grid ships disabled. Those rows are now recorded in `map/core`'s context file. **Phase 2's render probe settled the primitive ladder by measurement, not by reasoning**: `TriMeshDrawCommand` is implemented (rung 1, one command per cell, correctness by construction) and the affine projection basis measured **0 – 1.41 px** error across four zooms — exactly `WorldToScreen`'s integer truncation, **zero basis error**, retiring the mirrored-overlay risk outright. **D8** then rewrote the hot loop after D6 invalidated the plan's cost model: the rival boundary is **solved in closed form** rather than marched, with the brute-force marcher kept as the oracle a Logic case compares against — and that oracle caught the first inversion run against it, naming both answers. Gates: compile **exit 0 / 5971 files**, Fast **66**, All **101** (+12 Logic cases, every one proven able to fail, two of them twice). I-4's boundary greps are clean — no `[RplProp]`, no RPC, no EPF, no write to any campaign record, nothing added to `OVT_PlayerCommsComponent`, every new file under `Scripts/Game/UI/Map/` or `Scripts/Game/Tests/`. **Phase 7's replication audit passed by code reading**, including the one that could have split two clients' geometry: `OVT_TownData.size` is `[NonSerialized()]`, but it is _rediscovered locally on every machine_ from world-static town controllers, so it is client-readable and both clients weight towns identically. 🔴 **Nothing this feature renders has been verified by any automated gate, and nothing has been rendered since D6.** Q-1's three performance numbers are **unmeasured** — D8's levers are justified by arithmetic and an equivalence proof, which says the answers are _right_, not that they are _fast enough_. Still owed: Phase 6's measurement (one map open with `m_bDebugTiming 1`), Phase 7's two-client/JIP gate, the hatch art, and Phase 9                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 7   | map-layers        | 🟡 **Code-complete (ALL build phases 0–8 + 4b) and ENTIRELY UNOBSERVED** — only Phase 7, the user-driven gate, is open                                                                       | **70/112 rows** · every build phase ✅ (incl. Phase 8 help sync 4/4) · verification **4/37** (the 4 are the automated gates)                                                                                                                                                                                                       | One docked panel, one list of rows; each row is simultaneously the legend entry (icon + localized plural name) and the control (a `WLib_Checkbox`). **Nothing is hardcoded** — rows come from three sources through one shared builder: the 14 configured location types, every canvas layer registered with `OVT_MapCanvasCompositor`, and one hand-built row for `OVT_MapPlayerLocation` (K5). Adding a type to `OverthrowMap.conf` or a layer to `MapOverthrow.conf` adds a row with **zero code change**. Entered through **vanilla's own map tool menu**, so it costs **no new keybinding** — which mattered, because `MapContext` carries 41 live actions and `KC_H` is taken three times over. 🔴 **R1 fired on day one (D2):** vanilla's `SCR_MapToolMenuUI` was genuinely **absent** from Overthrow's `m_aUIComponents` — the `m_aModules` merge evidence did **not** transfer — so the plan's one-line same-GUID fallback was the actual path, and **P1 is a load-bearing play-test question rather than a formality**. 🔴 **D4 (user decision) then overrode acceptance criterion Q-3** on purpose: `MapContext` declares **zero** `Menu*` navigation actions, so a vanilla tool-menu entry buys _reaching_ the panel and says nothing about _walking the rows inside it_. Phase 4b adds `ActionContext OverthrowMapLayersContext` (Priority 70, above `MapContext`'s 50), re-referencing only **existing** `Menu*` actions and **consuming no new key or pad button**, activated per frame **only while the panel is open** — copied verbatim from vanilla's own `TaskListMapContext`/`MapMarkerEditContext`, which solved this exact problem twice. Q-3's letter is broken; its reason (finding a free key is the risky part) is intact. **Additive on `map/core`:** three new `OVT_MapLocationType` members (`m_sCategoryName`, `GetCategoryName()`, `m_bPlayerVisible`/`SetPlayerVisible`/`IsPlayerVisible`) — and the toggle is deliberately the **first** gate in `OVT_MapLocationElement.SetVisible`, which early-returns, so a hidden type **skips** the zoom lookup and `ShouldShowLocation` entirely and costs _less_ than a shown one. **The `OVT_MapCanvasLayer` contract needed nothing added** — `territory-overlay` Phase 1 designed the layer half correctly and completely before its consumer existed. Preferences persist **per profile** via vanilla `ModuleGameSettings` (no EPF, no new file I/O, console-safe), flushed at panel close and map close only because `SaveUserSettings()` is engine-throttled. Two real defects were caught by building rather than by testing: `SCR_CheckboxComponent.SetChecked` **raises `m_OnChanged`**, so a row must seed its value _before_ subscribing or every initially-hidden row reports a player toggle (which would have looked exactly like "persistence doesn't work"); and `m_bMarkersVisible` survives a map close while `OnMapOpen` builds a **fresh, visible** widget set, so a player who hid player markers and reopened the map would have got visible markers parked wherever an unpositioned `FrameSlot` lands. Gates: compile **exit 0 / 5994 files**, Fast **89**, All **127** (+2 Logic cases, both proven able to fail; a third, unplanned red found an **inverted assertion** — `IsVisible` has the opposite polarity to `OVT_TutorialSeenStore.HasSeen`). Phase 6's boundary greps are clean — no `[RplProp]`, no `[RplRpc]`, no `Rpc(`, no `EPF_`, nothing added to `OVT_PlayerCommsComponent`, no write to any campaign record, every new file under `Scripts/Game/{UI/Map,Data,Global,Tests}/` or `UI/Layouts/Map/`. 🔴 **Nothing this feature draws, binds or persists has ever been executed** — the panel, every `FindAnyWidget` name, all gamepad behaviour, the settings round trip and MP isolation are invisible to all three gates. **Six incidental findings (F1–F6) are recorded for filing, none fixed here**, including **F6**, a _pre-existing_ cross-context input collision that probably leaves `map/core`'s `OverthrowCloseInfoPanel` keyboard-only on a pad                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 8   | shared-markers    | Planned (stretch)                                                                                                                                                                            | —                                                                                                                                                                                                                                                                                                                                  | Networked player/squad map markers by wiring up vanilla's existing marker stack                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 9   | influence-overlay | ✅ **COMPLETE — built AND play-tested green (SP + MP) 2026-08-12, closed by the user the same day it was built** | **95/95 (100%)** · ⚠️ two rows closed on a qualitative report rather than a transcribed number, and `tasks.md` names both: **Q-1 reported one of its four numbers** (`Draw()` ~1 ms against a stated ≤ 0.5 ms budget — the user's "no problem" is a sound call but the budget was not met *as written*, and `GetTickCount`'s integer resolution makes one figure ambiguous), and **Phase 1's spike was never run as a spike** (Phases 4–5 had replaced the stub before anything was tested, so P1–P5 were answered in final form). **The Phase 5 tunable defaults shipped unchanged — the legibility retune the plan budgeted for was not needed once.** | Faction-coloured dashed lines from the **selected** map element to every town its influence actually reaches, plus a stroke-outline reach ring for a selected tower or base. **The client re-derives the source→town relation; nothing is attributed server-side (D1)** — and the rule that makes that safe is the **cross-check (K2)**: an edge draws ACTIVE **iff** the geometry qualifies under the shared range test **and** the matching modifier is genuinely present in the target town's replicated `supportModifiers`; otherwise SUPPRESSED. The client therefore never re-implements the enemy-wins resolution at all. **The shared rule set is the feature's real deliverable**: `OVT_InfluenceRules` (pure, world-free) was **extracted from the code that already applied these rules**, and `CheckUpdateModifiers` / `UpdateAllTownMomentum` were rewritten against it **behaviour-preservingly** — filed under the campaign's own folder, not `UI/Map/`, because it is the campaign's rule set and the map is its _second_ caller. **D3** carries the suppressed class on **two independent cues, alpha AND dash rhythm** (220/70 alpha; 14/10 px vs 6/14 px), deliberately redundant so a dim line cannot read as a rendering fault — do not collapse them. Command count is bounded by construction (`32 × E + 36` worst case; the clamp **lengthens** dashes rather than multiplying them once it bites). **Additive on `map/core` in two places, both now recorded in its context file**: `GetFactionArgbByIndex` (K8 — a _promotion_, two byte-identical bodies in `OVT_MapTerritoryLayer` and `OVT_MapRestrictedAreas` collapsed into one, white fallback preserved, now serving all three canvas layers) and a new **selection-surface contract** on `OVT_OverthrowMapUI` (`GetPanelLocation()` + the `ShowLocationInfo` assignment) — because `m_SelectedElement` is **sticky** and lines driven from it outlive the panel, while `m_HoveredElement` and `m_PinnedElement` are each null in one reachable half of the interaction model. **The `OVT_MapCanvasLayer` contract needed nothing added — the second consumer in a row to leave it untouched**; a third canvas layer cost core no change at all. Two engine facts were **measured rather than assumed**, and both changed the work: `vector.Distance` is **not correctly-rounded** (exact at 1500 m, +1 ULP at 1000 m and 2000 m), which made one planned boundary case toothless and another **unreachable through world geometry** — the comparison was split into `IsWithinMomentumRange(float)` so the boundary is assertable at all; and `ScriptBitWriter`/`ScriptBitReader` **cannot be round-tripped from script** — `new` returns non-null and the first proto call **hard-crashes the process**, so the Phase 3 JIP append has **no automated coverage and cannot have any**, and its evidence was instead a slot-by-slot type diff (**26 writes, 26 reads, all matching**, the two new floats at slots 20 and 21). Gates: compile **exit 0 / 5998 files**, Fast **99**, All **137** (+10 Logic cases, **12 inversions run, every case observed red** — the plan's eight would have left cases 1 and 6 never seen). Phase 8's boundary greps are clean — no `[RplProp]`, no `[RplRpc]`, no `RpcAsk_`/`RpcDo_`, no `EPF_`, no write to any field of `OVT_TownData`/`OVT_BaseData`/`OVT_RadioTowerData`, nothing added to `OVT_PlayerCommsComponent`, every new file under `Scripts/Game/UI/Map/` or `Scripts/Game/Tests/` bar the one named exception (`OVT_InfluenceRules.c`), and **`OVT_MapRestrictedAreas`' radii, centres and geometry untouched** (BUG-070 must not regress). 🔴 **Not one pixel, alpha, dash, `.conf` entry or `.st` id has ever been rendered or executed** — every build phase is done and gated, and the feature is _entirely unobserved_. 🔴 **D-JIP makes the MP gate specific, not generic:** a client holds `Difficulty_Normal.conf`'s values rather than the attribute defaults, so client and server **agree at Normal and disagree everywhere else** — **the two-client run must be at Hard or Extreme/Insane, and a run at Normal proves nothing** (and `m_bDebugTiming 1` must be set in the layer's `MapOverthrow.conf` block first, or an absent print reads as a failed append). Still owed, all user-run: Phase 1's five spike answers, Phase 6's legibility pass, Phase 7's four measurements, Phase 8's two-client MP gate, and the whole of Phase 9                                                                                                                                                                                                                                                                                                                |

> **Features 1–4 are the epic's committed scope**: they land the rewrite and delete the legacy map.
> **Feature 5 (respawn) is new capability, prioritised** — sequenced after retirement so it is built once against a legacy-free map, but ahead of the stretch goals. **It is now code-complete (Phases 0–8 done) and awaiting its Phase 9 play-test gate.** That sequencing decision paid off: respawn extends `OVT_MapLocationType` with `CanRespawn`/`m_bRespawnOnly`, which would have had to be threaded through the legacy modes as well had retirement not gone first.
> **Features 6–9 are stretch goals.** None of 5–9 is a prerequisite for shipping the rewrite.
> **Feature 9 (`influence-overlay`) is code-complete and entirely unobserved.** It is the third consumer of
> the canvas stack and the first to need **nothing added** to the `OVT_MapCanvasLayer` contract while
> _drawing_ through it — but it did open a **third** contract in `map/core`, the container's selection
> surface (`GetPanelLocation()`), because three of `OVT_OverthrowMapUI`'s four "what is selected" references
> disagree with the info panel in ordinary, reachable states.
> **Feature 7 is code-complete but entirely unobserved, and it in turn unblocks feature 8.** `map-layers`'
> `BuildRows` iterates **three** sources — location types, canvas layers, and the one hand-built player row —
> through a **shared row builder** with a namespace-prefixed key (`"type:"` / `"layer:"`). Adding
> `shared-markers` as a fourth category is therefore **one more source, not a rewrite**: a key namespace, a
> label, and a visible/hidden pair on whatever object owns the markers. G8 asked for exactly this ("do not
> build for feature 8, but do not build a two-category structure that has to be rewritten either") and it
> held.
> **Feature 6 is code-complete but unproven at runtime.** It also **unblocked feature 7**: `map-layers`' open scope question (generic registration API vs hardcoded overlay toggles) is answered — generic, one row per `OVT_MapCanvasCompositor.GetLayers()` entry, keyed by `m_sLayerId`, toggled by **`SetLayerVisible`** and never by `SetActive` (which calls `DeactivateModule` and is one-way from script). The answer and its two caveats are written into `map-layers/requirements.md`.

> Reference any feature with the slash form `map/[feature-name]` (e.g. `/continue-feature map/core`). Task counts are pulled from each feature's own `tasks.md` and refreshed by `/update-epic`.

---

## Build Order / Dependencies

Which features come first, and why. This feeds planning and the next-step suggestions from `/continue-feature map`.

1. **core** — Foundational and mostly _discovery_. Every other feature extends `OVT_MapLocationType` or consumes `OVT_MapLocationData`/`OVT_MapLocationElement`, so the base contract (virtual method set, visibility/zoom rules, selection and info-panel lifecycle, config binding) must be documented and known-correct before anything is added on top. It is also where the element lifecycle is verified in MP/JIP — a defect here reappears in all ten types.
2. **location-types** — Depends on core's contract. Closes the _content_ half of parity: adds the one missing type (Vehicle), migrates bus stops from vanilla `MDT_BUSSTOP` descriptors onto an Overthrow marker component so they can be attached to any entity and selected like any other location, and gives the seven types currently rendering through the generic `OVT_MapInfoPanel.layout` their own panels. Must land before retirement because `OVT_MapIcons` drew vehicles and the legacy bus flow needs a destination marker. The bus-stop migration is the riskiest item here — it changes how stops are authored and discovered, so it should be sequenced first within the feature.
3. **fast-travel** — Depends on core (selection/click delegation) and on location-types (a target must exist to travel to, including bus stops). Closes the _verb_ half of parity: consolidates the fast-travel rule set and cost model into `OVT_FastTravelService` and moves bus travel out of `OVT_MapContext` so the map is the single travel surface.
4. **legacy-retirement** — Last of the committed scope. Deleting `OVT_MapIcons` and stripping `OVT_MapContext`'s three modes is only safe once features 2 and 3 have proven parity; doing it earlier removes shipped player capability. Also archives the superseded `towns/map-info` feature doc. **Build phases done 2026-08-10** — the legacy layer is deleted and `towns/map-info` now lives at `docs/archive/towns-map-info-*.md`; the combined play-test gate (which folds in `location-types` V-3…V-7) is outstanding.
5. **respawn** — First feature after retirement. Replaces the hardcoded spawn-at-home in `OVT_PersistentRespawnLogic` (`:132-138`) with a player choice on a dedicated screen. Sequenced here because it adds a _mode_ to the map, and doing that before retirement would mean adding a mode to a map that still has legacy modes in it. It also benefits from `fast-travel`'s server-authority fixes landing first, since it needs the same client-requests / server-validates pattern.
6. **territory-overlay** _(stretch)_ — First stretch goal because it is the highest-value new capability and reuses machinery feature 1 already documents (`OVT_MapCanvasLayer` + `PolygonDrawCommand`). Deliberately after retirement so the overlay is built once, against the final map, rather than maintained across the deletion.
7. **map-layers** _(stretch)_ — Follows territory because that is the point at which the map becomes genuinely crowded: eleven-plus marker types plus territory, restriction rings and possibly the threat grid, all drawn at once. It absorbs whatever temporary on/off the territory overlay shipped with.
8. **shared-markers** _(stretch)_ — Last because it is the most self-contained and the most dependent on what vanilla gives for free. Buildable in parallel with 5 and 6; only its legend/toggle registration depends on 6.

**Stretch-goal note:** features 6–8 are additive. If the epic must stop after feature 4, it stops in a complete, coherent state — the rewrite landed and the legacy map is gone.

**Dependencies between features:**

- core → location-types (the `OVT_MapLocationType` virtual contract and `OVT_MapLocationData` payload shape)
- core → fast-travel (element selection/click delegation and the info-panel hook the travel button lives on)
- location-types → fast-travel (fast travel and bus travel both need selectable destination markers, incl. bus stops)
- location-types + fast-travel → legacy-retirement (**hard gate** — nothing legacy is deleted until parity is demonstrated)
- legacy-retirement → respawn, territory-overlay, map-layers, shared-markers (all build on the finished, legacy-free map)
- core + location-types + fast-travel → respawn (map UI, the markers picked from, and the per-type ownership gates its eligibility rule reuses)
- territory-overlay → map-layers (the overlay is the main thing the panel toggles); map-layers → shared-markers (markers register as a toggleable category)
- **External:** `resistance/fob` (FOB markers), `economy/shops` + `economy/real-estate` (shop/house markers and ownership), `towns/core` (town records, and the bus-stop discovery this epic replaces), `occupying/core` (bases, radio towers, QRF state). This epic _reads_ those systems; it must not change their state models — the **one deliberate exception** is bus stops, which migrate from vanilla map descriptors to an Overthrow marker component (see below).
- location-types and fast-travel are **not** parallel-safe as written (fast travel depends on bus-stop markers); everything else is strictly sequential.

---

## Integration & Architecture

How the features fit together as one coherent system, and how this epic integrates with the rest of the project.

- **Within the epic:** One extension point does the work. `OVT_MapLocationType` (a `ScriptAndConfig` subclass) is instantiated _from config_, not from code — `Configs/Map/OverthrowMap.conf` holds an `OVT_OverthrowMapConfig` with an `m_aLocationTypes` array, and `Configs/Map/MapOverthrow.conf` binds that config into `OVT_OverthrowMapUI`. Each type supplies its own icon (imageset + icon name), visibility zoom, optional bespoke info layout, and fast-travel eligibility. Adding a location type is therefore a new `.c` subclass plus a config entry plus (optionally) a layout — no change to the core map code. All types share `UI/Layouts/Map/Core/OVT_MapLocationElement.layout` for the marker and fall back to `OVT_MapInfoPanel.layout` for the panel.
- **With other epics / features:** The map is a **read-only projection of replicated campaign state** — it populates markers from the manager singletons via `OVT_Global` (towns, bases, radio towers, FOBs, shops, houses, vehicles) and must not become a second source of truth. The one exception is the travel verbs, which are actions: those must go server-side through the controller, **not** through the deprecated `OVT_PlayerCommsComponent` (see `core/controller-migration`). Canvas-layer modules (`OVT_MapRestrictedAreas`, `OVT_MapThreatGrid`, `OVT_MapPlayerLocation`) stay as `SCR_MapConfig` modules and are _not_ being replaced by the location-type system — `OVT_MapRestrictedAreas` in particular now draws the FOB-deploy restriction rings that `resistance/fob` enforces, and the two must stay in agreement (this was BUG-070).
- **Key architectural decisions for the epic as a whole:**
  - **Build on vanilla, don't reimplement.** The system extends `SCR_MapUIElementContainer`/`SCR_MapUIElement` so vanilla zoom, pan, cursor handling, decluttering and **gamepad/console navigation** come for free. This is the main reason the rewrite exists; any change that bypasses the vanilla widget layer forfeits it.
  - **Config-driven, not code-driven.** Location types are declared in `OverthrowMap.conf`. Note that Overthrow's `Configs/Map/MapOverthrow.conf` is a **same-GUID delta over vanilla's**, not a replacement — edits there merge with vanilla's config rather than overriding it.
  - **No dedicated verification feature.** The user chose a four-feature epic, so **MP/JIP and gamepad/console verification are acceptance criteria inside each feature**, not a separate phase. Nothing in this epic has been play-tested since 2025-08-02, and JIP/multiplayer is this project's most common regression class — every feature plan must carry an explicit MP + gamepad gate. `tools/launch-server.sh` plus two `tools/launch-game.sh --profile` clients is the intended harness.
  - **Bus stops become an Overthrow component, not a vanilla descriptor.** Bus stops are currently found by proximity-querying vanilla `EMapDescriptorType.MDT_BUSSTOP` descriptors (`OVT_TownManagerComponent.c:881`). They migrate to an Overthrow marker component following the **port pattern** — `OVT_PortControllerComponent` is an empty `OVT_Component` subclass that a manager locates with `FindComponent` (`OVT_EconomyManagerComponent.c:1860`) — so a bus stop can be attached to **any** entity rather than depending on world-authored descriptors, and so it becomes a normal location type with an icon, an info panel and a registry like every other. This is the epic's one intentional write into a non-map system.
  - **Two independent rendering paths, and the stretch goals use the second one.** Markers are _widgets_ (`SCR_MapUIElement` under `OVT_OverthrowMapUI`); overlays are _canvas draw commands_ (`OVT_MapCanvasLayer` under `SCR_MapConfig.m_aModules`). The canvas path exposes `PolygonDrawCommand` with an arbitrary vertex array (`OVT_MapCanvasLayer.c:23,61`) — which is what makes a real Voronoi territory overlay feasible rather than a grid approximation — but `Draw()` runs **every frame** via `Update` (`:12-19`), so any overlay must precompute in world space at map open and only project per frame.
  - **A future intel epic is anticipated and is not this epic.** What the campaign chooses to _reveal_ to a player — fog of war, scouting, and surfacing the occupying faction's own knowledge of the player (`m_aKnownTargets` / `IsKnownTarget`, `OVT_OccupyingFactionManager.c:1361`) — is deliberately excluded here. This epic's canvas-overlay and layer-toggle machinery is what that epic will build on, so neither should be designed in a way that assumes the map always shows everything.
  - **Localization.** The merge added 14 map string ids to `Language/localization_Overthrow.st` but the generated `localization_Overthrow.<lang>.conf` exports were taken from `main` verbatim — **the exports must be regenerated in Workbench** before any of these strings render.

---

## Tech Debt / Findings

Cross-feature tech debt and review findings. **Populated and updated by `/review-epic`** (which also writes feature-specific findings into the relevant child `context.md` files). Start empty.

Seeded by discovery on 2026-08-10 (`/discover-feature` over `core`, `location-types`, `fast-travel`) — every entry began as a code-reading finding, none observed at runtime.

**Reconciled 2026-08-10:** seven of the original eight are now closed. `fast-travel`'s three were fixed and play-tested green; the `RegisterPOI` hard dependency was removed by `location-types` Phase 2; and all five `map/core` bugs (BUG-133 … BUG-137) are closed and SP-play-tested. **Two items remain open: T1, and the `file:line`-comment audit added below by `legacy-retirement`.** Two more things worth carrying forward that are _not_ bugs and so have no row of their own: the `FindAnyWidget` name sweep across the map UI is still owed (D1/D2 were caught by spot-checking, and a null lookup stays invisible to `compile-check.sh`), and BUG-136's refresh timer now destroys elements **while the map is open**, which is an unverified path under every remaining play-test.

- [x] ✅ 🔴 **Fast travel is not server-authoritative** — **RESOLVED 2026-08-10 (F1/F2).** The client-side `SCR_Global.TeleportPlayer` and `TakePlayerMoneyPersistentId` are deleted; a new `OVT_TravelRequestComponent` on `OVT_OverthrowController` runs _validate → teleport → charge only on success_, so a refused or out-of-bounds teleport cannot take money. Play-tested green.
- [x] ✅ 🔴 **Recruit accompaniment regressed** — **RESOLVED 2026-08-10 (F4).** Recruits ride along again, now opt-out, with fare and teleport driven from **one** server-side list — "pay solo, arrive with a squad" is impossible by construction rather than by test.
- [x] ✅ 💳 **Deprecated comms dependency** — **RESOLVED 2026-08-10 (F3).** The server call moved off `OVT_PlayerCommsComponent` onto `OVT_OverthrowController`, modelled on `OVT_ShopTransactionComponent`.
- [x] ✅ 💳 **Legacy static is a hard dependency** — **RESOLVED 2026-08-10 (`location-types` Phase 2).** The `EOnFrame` `RegisterPOI` branch is gone from `OVT_MainMenuContextOverrideComponent`; POIs now come from `OVT_MapMarkerManagerComponent`. Deleting `OVT_MapIcons.c` is a visual change, not a compile break — this was `legacy-retirement`'s hard gate.
- [x] ✅ 💳 **Silent widget-name mismatches** — `map/core` — **RESOLVED 2026-08-10: BUG-133 and BUG-134 are both closed.** `IconLayout` (zoom icon sizing) and `CloseButton` (panel dismissal) were looked up but absent from the layouts. The underlying lesson stands and is not a bug: `FindAnyWidget` returning null is a silent no-op the compiler cannot catch, so a layout↔code name audit remains warranted for any _new_ map UI.
- [x] ✅ 💳 **Markers never refresh while the map is open** — **RESOLVED 2026-08-10: BUG-136 is closed, and fixed rather than deferred.** It never actually blocked the Vehicle marker — legacy `OVT_MapIcons` also sampled vehicle positions once at build time (`:709-731`) and only re-projected them, so populate-at-open _is_ parity (`location-types` Decision 6). `map/core` then went past parity: `OVT_MapLocationType.m_fRefreshInterval` (seconds, `0` = never) drives `OVT_OverthrowMapUI.TickRefresh` (`:401`) → reconcile → `OVT_MapLocationElement.SetLocationData` (`:433`), which finally gives `OnLocationDataChanged()` a caller. Enabled in `OverthrowMap.conf` at 5 s for Town/Base/RadioTower/FOB/Camp and **2 s for Vehicle** (`:154`); every other type stays at 0 and costs nothing. ⚠️ **Unverified at runtime, and it introduces a new live path:** reconciliation destroys elements _while the map is open_, so `DestroyLocationElement` must clear `m_HoveredElement`/`m_PinnedElement`/`m_SelectedElement` and the base class's static `s_SelectedElement` or it re-creates BUG-135 on a timer. Exercise it during `location-types` V-3 by leaving the map open >5 s with a panel pinned.
- [ ] 💳 **Three manager-access idioms across ten types** — `map/location-types` (T1) — inherited cache vs own shadowing member vs per-call `OVT_Global` lookup.
- [x] ✅ **NOT DEBT — the threat-grid overlay is a debug tool, and the record has been corrected.** Filed as accepted debt on 2026-08-11 under `map/territory-overlay` **D3**, then **corrected the same day by the user, who wrote the layer**: _"the threat layer was actually just written as a debug layer during the development of the deployment systems. it wasn't really intended to be shipped and that's why it was disabled."_ A debug tool behind a disable flag is an ordinary thing for a codebase to contain — it is **not** the "written-but-disabled code left in the tree indefinitely" that `map-layers/requirements.md` was written to prevent, so this item is closed rather than carried. ⚠️ **The two reasons previously recorded for the disable — ~2,300 draw commands a frame, and mutual exclusivity with the restriction rings under the shared-canvas defect — are both WRONG.** They were plausible, they were labelled unproven, and they were still misleading: they survived long enough to be reasoned from twice during this feature. They are struck through in `implementation.md` §4 D3 rather than deleted, as a standing reminder that a recorded guess outlives the doubt attached to it. **Forward view, recorded but not scheduled** (the user, same day): _"if we updated it and made it performant it could be added as a switchable layer turned off by default… just leave it as is for the moment until this is finished."_ That is a **feature** for `map/map-layers` or later — and `territory-overlay`'s grid → contour → smooth → scanline-fill → batch pipeline is the machinery that would make it cheap, since a threat layer is the same pipeline with `GetThreatByLocation` as its sampler. **Extraction should wait for that second consumer to actually exist** — two real consumers is the minimum evidence an abstraction is real rather than imagined.
- [ ] 💳 **`file:line` pointers in comments rot silently — a codebase-wide audit is warranted** — surfaced by `map/legacy-retirement`, **not map-specific**. Three independent instances turned up in one feature, all in files the deletions merely passed through: `OVT_FastTravelService.c`'s `BUS_STOP_RADIUS` comment cited `OVT_TownManagerComponent.c:881` for bus-stop discovery, in a file that contains **no bus code at all** (`grep -n "BusStop\|bus"` → zero hits) and was already dangling before this feature; `OVT_JobManagerComponent.c:67` cited `RplSave`/`RplLoad` at `:780`/`:810` when they sit at `:794`/`:824` (off by 14); and `OVT_MapLocationWaypoint.c:4-5` cited `OVT_JobsContext.ShowOnMap:109` (actually `:107`) and `OVT_RecruitsContext.ShowOnMap:489` (actually `:471` — off by 18). A `file:line` in a comment is a reference with a human reader instead of a compiler: nothing ever re-resolves it, `compile-check.sh` cannot see it, and a wrong one sends the next reader — or agent — to the wrong place with full confidence. Retirement's **K-9 discipline is the cheap fix at the point of edit — keep the rationale, drop the pointer**; a repo-wide sweep is a separate task and probably a `dev-ops` one. **`map/map-layers` Phase 6 (2026-08-11) audited its own diff against this rule and introduced none** — zero `file:line` pointers on any added code-comment line across six new `.c` files and four changed ones. The discipline is holding at the point of edit; the historical backlog is untouched.

---

## Master Overview Rollup

How this epic is represented in the project's master `docs/overview.md` (one row, not its children). Kept in sync by `/update-epic` and `/update-master`.

- **Rollup status:** 🟢 **Committed scope COMPLETE — 4/4 features built AND play-tested** (`core`, `location-types`, `fast-travel`, `legacy-retirement`); ✅ **`respawn` COMPLETE at 152/152 — built and play-tested green 2026-08-11**; **3 of 4 stretch goals code-complete, 2 of them play-tested and closed** — `territory-overlay` ✅ 2026-08-11 and `influence-overlay` ✅ 2026-08-12 (built and play-tested green the same day); `map-layers` remains code-complete and **entirely unobserved**; `shared-markers` unplanned and unblocked. **The rewrite has landed, the legacy map is deleted, and as of 2026-08-11 every committed feature's play-test gate is discharged.** Two features closed their last gates on 2026-08-11: `fast-travel` ran the two-client MP matrix that had been open since it was planned (the only thing that could confirm server-authoritative travel against a real second client rather than a code reading), and `location-types` discharged V-3…V-7 — of which **V-5 is the one that mattered**, because single player can never distinguish "filters correctly" from "every id is yours", so it is the only evidence the house-privacy fix isolates per player. `legacy-retirement`'s combined Phase 7 gate (2026-08-10) had already closed the epic's long-standing verification debt, and that gate bought disproportionately: four of retirement's seven phases edited `.layout`/`.conf`/`.et`/`.st` files that neither `compile-check.sh` nor either test group can observe. **Remaining epic debt is small and none of it blocks the stretch goals:** ~~`location-types` 7b~~ — **closed 2026-08-11, filed as BUG-138 … BUG-144** (one against `map/core`, one against `resistance/fob`, five against `economy/shops`), so the only map-epic item among them is BUG-138 (per-location visibility zoom is never read, so priority FOBs are not always visible); `core`'s `FindAnyWidget` name sweep, T1 (three manager-access idioms), and the codebase-wide `file:line`-comment rot audit. One **bookkeeping** item: `core`'s own verification rows read 1/8 despite the ground being covered by the combined gate — reconcile with `/update-feature map/core`, ticking only what was observed.
- **Every committed feature in this epic is now built AND observed — including the fifth.** `respawn`'s verification profile inverted and then closed within a day: it was _built and observed by nothing_ on 2026-08-11 morning and play-tested green the same day, **with no code change required to pass the gate**. The tree at the play-test was byte-identical to the tree at the end of Phase 8. `respawn`'s eight build phases are all green on compile (**0 / 5964 files**) and tests (**Fast 54 / All 89**, +10 Logic cases each proven able to fail), but those gates cannot see one line of what the feature actually does: the autotest world has no players, so `OnPlayerKilled_S` never fires and there is no UI tier, and the feature's two new `.conf`s, two new `.layout`s, three `.et` edits and new input bindings belong to the same file classes that `legacy-retirement`'s Phase 7 had to prove by hand. **The deferred death path is the highest-consequence unexecuted code this epic has produced** — a fault there means a connected player with no character — which is why it ships with an unbounded re-ask rather than a timeout, a t=0 capability check that degrades to today's behaviour, and a one-shot claim that makes a duplicate request unable to conjure a second character. `respawn`'s Phase 9 is not bookkeeping; it is the feature's only evidence, and `docs/features/map/respawn/context.md` now holds a single consolidated **"Still unverified"** list and a **"Where to look when it doesn't work"** triage section written for exactly that session. One doc defect was also closed on the way: `legacy-retirement` retained `OVT_MapContext.HideMap()` "as public API for `map/respawn`", and respawn never wanted it (its screen never touches the map _gadget_) — the method is kept, its play-test-derived ordering rationale intact, and only the false consumer claim corrected.
- **Stretch goal 6 is code-complete and is the epic's largest body of unobserved code since `respawn`.** `territory-overlay` passes compile (**0 / 5971 files**) and **101** automated assertions, **none of which can see a pixel it draws** — canvas rendering, colour, alpha, texture tiling and frame cost are invisible to every tier. The overlay has been rendered exactly **twice**, both times by the user on 2026-08-11, and **each look produced a design change** (D6 removed the maximum influence radius; D7 restricted neutral bands to inter-faction frontiers) — so **nothing has been rendered since the current build**. Its three performance numbers are **unmeasured**: D8 rewrote the hot loop after D6 invalidated the plan's cost model, and the justification is arithmetic plus an equivalence proof — which establishes that the answers are _right_, not that they are _fast enough_. What it has already banked for the rest of the epic is real regardless: the `OVT_MapCanvasLayer` contract extension and the compositor that fixes the two-layers-overwrite-each-other defect (both now recorded in `map/core`'s context file), the probe measurements that retired the mirrored-overlay risk, and feature 7's unblocked scope answer. Owed before it can be called done: one map open with `m_bDebugTiming 1` on a populated save, the two-client/JIP gate, the hatch art, and Phase 9.
- **Stretch goal 7 is code-complete and is now the epic's most completely unobserved feature — more so than
  `territory-overlay`, which has at least been rendered twice.** `map-layers` passes compile (**0 / 5994
  files**) and **127** automated assertions, of which exactly **two** are about this feature at all (the pure
  preference store, both proven able to fail). **Nothing it draws or binds has been executed once.** The
  panel, the tool-menu entry, all thirteen `FindAnyWidget` names, the new `ActionContext`, every gamepad
  path, the `ModuleGameSettings` round trip and MP isolation between two profiles are invisible to
  `compile-check.sh` **and** to both test groups — `.layout`, `.meta`, `.conf` and `.st` are seen by none of
  them, and this project has no widget, input or rendering tier. **Phase 7 is this feature's only evidence,
  and two of its rows are load-bearing rather than confirmatory**: **P1**, because `map-layers` D2 found
  vanilla's tool menu genuinely absent from Overthrow's `m_aUIComponents` (the `m_aModules` merge evidence
  did not transfer) and had to add it back as a same-GUID delta that has never been seen to render; and
  **Q-1**, because `requirements.md` states outright that a filter panel which only works with a mouse is
  _worse than no filter panel_, and D4's new input context is the whole answer to that. What it has already
  banked regardless: two `map/core` contract extensions with their rows written (and the deliberate 🔴
  record that `m_bPlayerVisible` is a **presentation preference, never campaign visibility** — the intel
  epic must not reuse the field), the confirmation that `territory-overlay`'s layer contract needed **zero**
  extension when its first consumer arrived, feature 8's unblocked shape, and six incidental findings
  (F1–F6) — **filed 2026-08-13 with the new F7 as BUG-149 … BUG-155** (BUG-146…148 were taken by parallel
  sessions before filing) — **F6/BUG-154 being a pre-existing gamepad input collision that
  probably makes `map/core`'s own info-panel close button pad-dead**, invisible to the repo's conflict
  checker because it is cross-context.
- **Stretch goal 9 was built AND closed on 2026-08-12 — the first feature in this epic planned, built and
  play-tested green inside a single day, and it broke the epic's three-feature unobserved streak.**
  `influence-overlay` passes compile (**0 / 5998 files**) and **137** automated assertions, of which the
  **10 new Logic cases are the feature's entire automatable surface** — pure arithmetic over the extracted
  rule set, world-free, and every one of them proven able to fail (**12 inversions**, two more than the
  plan listed, because the plan's eight would have left cases 1 and 6 never seen red). Everything the
  feature actually _is_ — every line, dash, alpha, colour, the ring, the layer row, the `.conf` module
  entry, the four `.st` ids, the frame cost, the JIP append and all multiplayer behaviour — is invisible to
  both gates. **Three things it banked regardless of ever being rendered.** First, the **rule-set
  extraction is the real deliverable**: `OVT_InfluenceRules` was pulled _out of the code that already
  applied these rules_, and the server's 10-second tick was rewritten against it behaviour-preservingly, so
  the campaign and the map can no longer disagree about what influence _is_. Second, **two engine facts
  measured rather than assumed**, both of which changed the work and neither of which anyone should have to
  rediscover: `vector.Distance` is not correctly-rounded (which made one planned boundary case toothless
  and another unreachable through world geometry), and `ScriptBitWriter` **hard-crashes on first use from
  script** — so the JIP append has no automated coverage and _cannot_ have any. Third, **`map/core` grew
  two contract rows and `OVT_MapCanvasLayer` grew none** — the second consumer in a row to leave that
  contract untouched. ✅ **The gate is discharged** — SP visual pass, legibility and MP all reported green by the user, and
  the **Phase 5 tunable defaults shipped unchanged**: the legibility retune the plan budgeted for, and
  that `territory-overlay` needed after *each* of its two renders, was not needed once here. ⚠️ **One
  honest gap carried forward:** Q-1 closed on a single reported figure (`Draw()` ~1 ms against a stated
  ≤ 0.5 ms budget) rather than its four numbers, so **this epic still has no recorded frame-cost
  baseline for a canvas layer** — `territory-overlay` left the same gap and feature 9 has now repeated
  it. Whoever builds canvas layer four inherits nothing to compare against.
- **One-line summary for master:** Config-driven interactive map built on vanilla's map widgets — location markers, info panels and the fast/bus travel verbs — replacing the legacy `OVT_MapIcons` layer and `OVT_MapContext` modes. Committed scope shipped and play-tested, and the map-driven respawn picker is complete and play-tested green — 5 of 8 features done; the territory overlay is code-complete but has not been rendered since its last two design changes, the map layer-filter panel is code-complete and has never been executed at all, and 1 stretch goal remains unplanned.

---

_This is a required file — do not delete it; it marks the folder as an epic. Update it with `/update-epic map` after working on the epic's features, and run `/review-epic map` to refresh the Tech Debt / Findings section._
