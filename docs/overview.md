# Overthrow - Project Overview

**Last Updated:** 2026-08-22
**Version:** v1.37

## Project Summary

Overthrow is a dynamic, persistent revolution mod for Arma Reforger, built on EnforceScript and the Enfusion engine's entity-component architecture. Towns have populations, stability and support that shift over time; money, gear, vehicles, houses and recruits persist across sessions. Current engineering priority is the **dev-ops epic** — replacing the manual Workbench/play-testing quality gate with automated compile checks, an autotest suite and CI, built on Reforger 1.7.0's shipped tooling.

## Feature Status

| Feature | Status | Tasks | Notes |
|---------|--------|-------|-------|
| dev-ops (epic) | 🟡 In Progress (3/5 features) | 139/140 (features #1-#3; 1 optional deferred) | Automated compile, test and release pipeline built on Reforger 1.7.0's autotest framework and Workbench CLI — replaces the manual play-testing quality gate. #1 `workbench-automation` ✅; #2 `autotest-foundation` ✅; #3 `test-coverage` ✅ (30 assertions in four tiers, two group targets `{Fast}`/`{All}`, quarantined `core/persistence` acceptance gate, save-state tooling). Next: `ci-pipeline`, then `release-automation`. |
| core (epic) | 🟢 3/8 documented; **player-groups ✅ complete**; **persistence ✅ complete (shipped, MP green)**; **controller-migration ✅ complete (MP play-test green 2026-08-14)**; **damage ✅ complete 2026-08-20 (play-test green)** | 213/213 (100%) — 56/56 (persistence) · 31/31 (player-groups) · 56/56 (controller-migration) · 70/70 (damage) | Overthrow's core systems. `game-mode`, `config`, `player-manager` 📄 documented retrospectively — all 8 linked bugs closed. `persistence` — EPF → vanilla migration **built and gate-verified 2026-08-02**: acceptance gate flipped exit 1 → 0 and joined the All group, EPF/EDF fully removed, breaking save change; Phase 8 closed **GitHub #143** (a leaving player's vehicle returns on reconnect). **SP play-test green 2026-08-03 and section D (JIP, two-player isolation, dedicated restart) GREEN** across three dedicated rounds, whose headline finding — **BUG-086: a released persistence record does not outlive its entity** — replaced save-and-release with the reservation model (offline bodies and locked vehicles kept alive, tracked and hidden in place). **Feature shipped; every linked bug closed.** Post-ship **BUG-104** fixed 2026-08-08: an SP/listen-host Continue left already-connected players unmapped (HUD read $0, no `OVT_OverthrowController`) because `SetupPlayer` only runs on CONNECT and a Continue replaces the world without one — dedicated servers continue at boot before anyone connects and were immune, so no MP round could have caught it (All 67 → 68). `player-groups` — **built 2026-08-06** (issue **#147**, second half): private-by-default groups, joining another player's group through vanilla's request/approve flow behind a warning dialog, and recruits that follow their owner between groups without ownership ever transferring. No new client→server RPC and no `OVT_Global` getter (verified by empty diffs); All group 60 → **66** assertions. Diagnosis found **no residual Group-Menu fault** — every #147 symptom was downstream of BUG-088's missing `RplComponent` — but the build uncovered four unpredicted defects, two severe: **group names never displayed even in solo** (`SetName` is the wrong API) and **nobody could ever leave a group** (vanilla has no Leave action, and its stand-in was permanently disabled by a stray empty faction group). **MP play-test GREEN 2026-08-06 — the user ran the dedicated-server tests and found no problems, explicitly including a leader with joined players *and* recruits disconnecting (new leader promoted, with command of the group's recruits) — the highest-risk case in the plan and one with no automated coverage at all. Feature closed.** Only housekeeping remains: a Workbench export of 2 notification keys, and the optional AI-density measurement. `controller-migration` 🟡 **BUILT 2026-08-14 — all 10 phases in one autorun (56/56)**: `OVT_PlayerCommsComponent.c` (2,001 lines, 55 `RpcAsk_` + 4 `RpcDo_`) and `OVT_Global.GetServer()` are **deleted** and stripped from both prefabs — repo-wide grep returns 0. Ten new domain components joined the seven existing ones (**17 on `OVT_OverthrowController`**), reached only via the generic `OVT_ControllerComponent<T>.Get()`; `OVT_Global` 1,007 → 292 lines with utilities split to `OVT_WorldUtils`/`OVT_PrefabUtils`/`OVT_LoadoutUtils`. **Nine zero-validation endpoints closed** (dismiss-any-recruit, possess-any-entity, delete-any-camp — an arbitrary-entity delete — and more) and **two shipped defects found and filed: BUG-161** (item buying was FREE for remote clients, a no-op for listen hosts) **and BUG-162** (three economy notifications never fired). Gates: compile 0, Fast 112, All 154 (75-call arity audit clean). **✅ ALL GATES DISCHARGED 2026-08-14: the 21-step MP play-test passed in full, Workbench loads all three text-edited prefabs clean, listen-host Continue green.** `damage` ✅ **COMPLETE 2026-08-20 — all 8 phases in one autorun (70/70), play-test green the same evening**: structures ruin instead of vanishing (explosion/smoke/sound, ruin mesh through the engine's multi-phase destruction, inert while ruined, gear survives, persistence v2), a 20 s priced repair action difficulty-scaled via `repairCostMultiplier`, an occupying 'Base Repair Detail', admin `/ruin-structure` `/repair-structure`. Gates: compile 0, Fast 346/347, All 398/399 (one pre-existing v1.5 red). BUG-193 filed (bunker `RplComponent`). **Play-test green 2026-08-20 on all 10 gated items** after four same-evening fixes (SetDamagePhase FX funnel, vehicle-style smoke/fire + visible flame, TNT_Large blast sound); final All 405/406. Only wiki publication (draft ready) remains. |
| economy (epic) | 🟡 In Progress (fuel ✅ complete; shop-ux pending play-test) | 37/37 (shop-ux) · 35/36 (fuel) | Money, prices, shops, real estate and fuel. **New (2026-08-18): `fuel` COMPLETE — play-test green (SP + MP/JIP), committed a765bd12** — static fuel stations charge per litre delivered (server-authoritative in the vanilla support-station flow, difficulty knob `fuelPricePerLitre`, `CONFIG_STREAM_VERSION` 3→4), can't-afford gate + one-shot run-dry notification, free paths preserved (truck→vehicle, jerrycan, depot), buildable **Fuel Depot** at captured bases (5000 L, starts empty, filled from a fuel truck via the vanilla refuel action, level persists via `SCR_FuelManagerComponentSerializer`), `OVT_FuelUtils` fuel-source discovery API for high-command, **zero new RPCs**; +5 Logic + 1 persistence case; rider fix: `GiveXP(-1)` null-deref that aborted server-initiated `BuildItem`. Owed: wiki sync (auth-blocked), `en-us.conf` re-export, manual play-test. `shop-ux` (2026-08-04) still awaiting play-test — category tabs, Buy/Sell split, trunk "Sell All", server-authoritative selling. Prior discovery debt (**BUG-019…024**, restocking dead code, JIP warehouse bounds) remains in the epic's Tech Debt section. |
| occupying (epic) | 🟢 Active — **`objectives` BUILT 2026-08-21** (74/74, Ready for Review, human verification owed); `counter-attacks` **CLOSED 2026-08-20** (its proof of concept); `base-upgrades` retired + closed, `qrf` legacy + closed | objectives 74/74 (100%); counter-attacks 137/137 (100%) | The AI occupying faction: command layer (`core`: bases/resources/threat), `qrf` battles, the modular `deployments` framework, and — as of 2026-08-21 — **`objectives`**, which turns the faction's doctrine into authored data. The faction holds **one objective at a time** and works toward it in three legible phases: harassment (groups inserted by live truck from a controlled base, a stacking town-support debuff, radio-tower recapture, and base sabotage that **permanently destroys** player-built structures cheapest-first); an **unannounced forward operating base** raised between their nearest holding and the target, which can be starved (take or empty its supplying base, or simply stay close to it — **one player is enough**) or dismantled by hand once the area is clear; then a **silent siege** — the whole budget spent in one pass, an encirclement at 100–150 m, no notification until the ring closes, then **fifteen real minutes** in which nothing is scored and wiping the besieging force ends the battle in the resistance's favour. Daylight-only starts (05:00–15:00), waves biased toward the bearing they actually came from, twelve difficulty fields with the sabotage gate scaled **inverted** so easier settings give more warning. ⚠️ **Two corrections to earlier revisions of this row, both found by `objectives` Phase 8's fact-check:** the siege muster is **fifteen** real minutes (`MUSTER_TIME_MS = 900000`, halved 30→15 on 2026-08-20 after the author's first play-test), not thirty; and starving a forward base needs a player merely **close** to it (`baseCloseRange`, 220 m), not "in strength". A Field Manual body and seven translator notes were corrected to match. **`objectives` (built 2026-08-21, 8 phases in one autorun)** rebuilt the 5,201-line director as a **registry of plans**: plans → ordered phases → bags of condition/operation/abort modules authored in Workbench `.conf`, so a modder adds a doctrine by writing a config rather than editing a component. Built as a **strangler** — the framework landed first with both shipped plans running through legacy shim modules that called the old methods verbatim, then one phase of doctrine at a time was replaced and the methods it wrapped were deleted **in the same phase**, so parity was a diff against a still-present reference rather than a reading exercise. The director is now a **4,383-line runner**; the shim classes, the `m_iLegacyPhase` authoring, the phase enum and three temporary fallback paths are all gone. Every code phase gated on the **All** suite and green (417/418 → **439/440**; the single red throughout is a pre-existing `core/damage` leftover, `CompositionSlotGate`). 🔴 **The session's most consequential finding was in the tooling, not the feature: `tools/run-tests.sh` had never tested this worktree.** All three Overthrow checkouts under the shared repo parent declare the same addon ID *and* GUID, so `-addons Overthrow` always resolved to the canonical `Overthrow.Arma4`, and a path-matching fallback in the addon-load guard stamped the wrong-tree run **green**. Phase 1's recorded verdict was void. Fixed with a junction farm + `OVERTHROW_GAME_ADDONS_DIRS` and a guard that compares **file identity** rather than paths, so a mismatch now fails INDETERMINATE instead of passing. The first honest run immediately found a real serializer bug: **an Enfusion save context keys each property by the LOCAL VARIABLE'S NAME**, so reading into `readTargetKind` what was written as `targetKind` restored every field as zero and silently wiped the objective and its forward base. ⚠️ **`Overthrow.Arma4-main` still has the same tooling defect.** ⚠️ **`objectives` is not yet human-verified**: no MP/host pass on the changed GM wire (`WIRE_VERSION` 2→3), no real battle fought to a result, no real forward base raised end to end, and the **modder exercise — the acceptance test for the whole design — has not been run**. A localization re-export and a wiki publication are owed. `base-upgrades` is superseded: `Scripts/Game/Components/BaseUpgrades/` is deleted and base defence is nine deployment configs. Retrospective debt **BUG-025** (unvalidated capture RPCs) and **BUG-028** (deployment-list leak) remain open; **BUG-026/027/029/031** are closed; BUG-109 closed as obsolete. |
| skills (epic) | 📄 Documented (3/3 retrospective) | — | Player XP, levelling and config-driven skills — framework (`core`), Stealth detection skill, Trade/Diplomacy influence skills — backfilled with retrospective docs. Discovery surfaced ~35 issues headlined by three client-trust RPC seams (BuySkill, ImportToVehicle, AddSupporters) and a level-cap crash. Top 5 filed as **BUG-032…036**; cross-feature debt seeded in the epic's Tech Debt section. |
| resistance (epic) | 🟢 Active — **`high-command` DONE 2026-08-22** (77/77, play-test green, merged); `sleep` **done** (play-test passed; only the wiki sync owed), `recruit-ux` Ready for Review; 6 documented retrospective; `vehicle-storage` planned | high-command 77/77 (100%) · recruit-ux 73/73 (100%) · sleep 32/33 (97%) | The player resistance faction — command layer (`core`), mobile `fob`, base `building`, persistent AI `recruits`, `loadouts` and the `wanted-system` (retrospective docs), plus the new **`recruit-ux`** (built 2026-08-14, Ready for Review): inactive "holding" recruits with clustered defend groups, a recruit map layer with ammo-status badges, a sectioned gamepad-safe roster, and a whole-kit loadout swap — BUG-107 fixed en route, BUG-166/167 filed. **New (2026-08-19): `sleep` built — six phases plus a review-fix pass, 32/33, Ready for Review.** A single-player "Sleep" action on the seven vanilla bed prefabs (same-path overrides carrying each vanilla resource GUID) and on a new **Cot** placeable skips **8 in-game hours** — and the point of the feature is that nothing is lost to the skip: income/taxes/donations per 6-hour boundary, NPC shop buying, the 07:00 restock, midnight rent, occupying-faction resource gain/spend and 15-minute threat decay are all **replayed through the same methods the live tick calls**, so a future change to a payout changes the skip with it. The window is half-open at the start and closed at the end, and the economy asserts its three hour latches to the landing hour afterwards — which is what makes double-pay impossible. Gated to an owned house / own camp / deployed FOB / captured base (the placement system's own ordered test), disabled-with-reason during a QRF or while wanted, never shown outside single player, with a **12 in-game-hour cooldown** stored as a game-clock stamp (correct at 6× and 12×, durable across a Continue, serializer **v5**). **BUG-183 rode along and is fixed** — the economy's hour latches were never persisted and re-initialised to `-1`, so loading a save taken during an accounting hour paid the income sweep **again**: a repeatable money exploit in shipped builds, closed by the same latch assertion the time skip needed anyway. **BUG-186 filed, not fixed** — both `CheckUpdate` timers schedule off the **day** time multiplier only, so at night a tick strides ~2 in-game minutes and the occupying faction's minute-exact gain/decay gates are skipped entirely. **The cross-phase review earned its place:** §3.3 had given the *economy* replay two edge defences and the *occupying* replay neither, so sleeping from 10:00 paid the reserve **three** times instead of two (~85% of the time) and sleeping from 12:00 **lost** that payday — exactly the runs the play-test prescribes, so F7/Q1/Q2 would have failed on the first try. Fixed with two live-tick latches mirroring `m_iHourPaidIncome`, a start-boundary flush and a landing-instant assertion. The user also rejected **D16** on reading it (the cooldown was measured from lying down, so 8 of the 12 hours were consumed by the skip); **D16a** now stamps the wake instant, giving a full 12 hours on waking. Gates: compile 0 after every phase, All group **177/177** as built → **179/179** after the review fixes, 0 failures, with every new case confirmed present in `junit.xml`. **Closed out 2026-08-19: the full 26-item play-test passed and the Workbench localization re-export landed**, which closed **BUG-183** on its own three acceptance checks. Only **T6.3**, the public wiki sync, is outstanding — no wikijs MCP server was attached to the build session. `vehicle-storage` is planned next. Legacy discovery debt: **BUG-042…053** (several fixed) and FOB's **BUG-119…128**. | **New (2026-08-22): `high-command`** — player-owned, map-commanded AI groups bought at a new **barracks** (the game's dearest buildable, plus three vanilla barracks families made functional), priced per member + gear + vehicle with **warehouse stock sourced free**, ordered anywhere with **Defend/Patrol/Attack** stances on a gamepad-first exclusive input context, auto-rearming and auto-refuelling, convertible **one-way** from parked recruits (keeping bodies and gear), and **always-live with their own virtualization observer** so they fight and travel in real time whether or not their owner is online. **Retires garrisons entirely** — both serializers bumped read-and-discard, so old saves load cleanly without them — and fulfils the shipped `//To-do: factoring in warehouse` on both the HC and recruitment-tent surfaces. Gates: Logic 282 / Init 209 / PersistenceRoundTrip 43 / Persistence 13. **Play-test green** (incl. recruit conversion and save/load with both purchased and converted squads); localization re-exported, radio placed, `main` merged as `d99bb57a`. Deferred by the user: the dedicated-server/JIP pass (next week) and the wiki (until 1.5 ships).
| jobs (epic) | 📄 Documented (1/1 retrospective) | — | Config-driven quest layer — conditions/stages framework, `OVT_JobManagerComponent`, jobs menu UI and the 8-job catalog — backfilled with retrospective docs (`core`, incl. an authoring guide). Discovery catalogued ~20 issues headlined by: the tutorial chain is dead for every player but the first (global `m_iMaxTimes` caps), owner-less update-RPC identity colliding two players' jobs, declines never broadcasting, and private jobs/completion hints leaking to all clients. Top 5 filed as **BUG-037…041**; cross-feature debt seeded in the epic's Tech Debt section. |
| map (epic) | 🟢 Committed scope COMPLETE and **fully play-tested** (4/8 — **fast-travel ✅ CLOSED, MP green**; **location-types ✅ verified, MP green**; **legacy-retirement ✅ COMPLETE**; **core bugfixed (5/5)**) · ✅ **respawn COMPLETE, play-tested green** · ✅ **territory-overlay COMPLETE, play-tested green (SP + MP) 2026-08-11**; 🟡 **map-layers code-complete 2026-08-11 (Phases 0–8 + 4b) and ENTIRELY UNOBSERVED**; ✅ **influence-overlay COMPLETE, built and play-tested green (SP + MP) 2026-08-12 — planned, built and closed in one day**; 1 stretch (`shared-markers`) remains, now unblocked | **Committed 4/4 ✅** — 74/74 (fast-travel) · **42/42** (location-types) · **51/51 + 27/27** (legacy-retirement) · 19/30 (core) · ✅ **152/152 (respawn)** · **133/136 (territory-overlay)** · **70/112 (map-layers — all build phases done, verification 4/37)** · ✅ **95/95 (influence-overlay)** | Config-driven interactive map built on vanilla's map widgets — location markers, info panels and the fast/bus travel verbs — replacing the legacy `OVT_MapIcons` layer and `OVT_MapContext` modes. Lands the rewrite that has sat unmerged on `new-map` since 2025-08-02. **`location-types` built 2026-08-10 (6 phases, one autorun)**: all four parity gaps closed — Vehicle, waypoints, the POI registry and bus stops, the last two migrated onto a new `OVT_MapMarkerComponent` + registry discovered by a **client-safe world scan with no replication added** (the proven `InitializePorts` pattern). **`OVT_MapIcons.RegisterPOI` is gone** — deleting the legacy map is now a visual change, not a compile break, which is half of `legacy-retirement`'s hard gate. 🔴 **Fixed a live privacy leak**: house markers showed *every* player's property to *every* player (legacy showed only your own); the fix fails closed. Nine types that rendered a bare header gained real info panels via a single additive `map/core` contract extension, with Town/Base/RadioTower structurally unable to regress. Shop panels gained scarcity carets + a remoteness badge computed **entirely client-side from replicated state, showing no prices at all**; the gun-dealer panel was pivoted mid-run by user directive to list the **four weapons that dealer actually rolled** — the only per-dealer information that exists. Gates green at every phase (compile 0 / 5956 files, Fast 43, All 78); two tests added, each **proven able to fail**. **Three defects were caught by review, not by any gate** — POI icons hardcoded white against a white-glyph atlas (invisible markers), info rows clipping their own descenders, and gun-dealer carets that would have been pinned at ▲▲▲ forever because no town shop stocks a weapon. All three compiled clean and passed every test: `.layout` files, `.conf` entries, icon tints and prefab blocks are outside what the automated spine can see. **Phase 7 — two-client MP/JIP, gamepad, zoom sweep and save compatibility — is user-driven and outstanding**, and is what confirms the privacy fix across clients. 6 findings queued to file (N5, N6+addendum, N14, N17, N18, N19). **`fast-travel` built 2026-08-10 (5 build phases, one autorun)** — the *verb* half of parity, and the epic's security fix: travel and payment moved off the client onto a new `OVT_TravelRequestComponent` that validates → teleports → *then* charges, so a refused or out-of-bounds teleport cannot take money; bus travel migrated with **no armed mode to leave set**; recruits restored as an opt-out whose fare and accompaniment come from one server-side list. Gates green throughout (compile 0 / 5958 files, Fast 44, All 79). 🔴 **Both play-test phases (0 and 6) were never run — nothing in this feature has been observed at runtime** — and five new `.st` ids are absent from the generated exports, so labels render as raw keys until regenerated. ✅ **Play-test gate DISCHARGED 2026-08-10 — user ran it and reported all green**, with all six localization exports regenerated. That confirms what no automated gate can observe: the server path executing and charging, the recruit toggle rendering and repricing live, the R6 panel guard (flagged unverified and the toggle's only gamepad path), bus travel end to end, and `KC_R` not stowing the map. **Feature complete.** Not covered by one session: Phase 0's two-client cases (concurrent travel, JIP, listen-server short-circuit). Legacy fast travel remains reachable via `OVT_MainMenuContext.c:218` until `legacy-retirement` cuts it, so **both paths are still live in this build**. **`map/core`'s five bugs are all closed and SP-play-tested (2026-08-10)** — BUG-133 was two stacked faults rather than the predicted one-liner (wrong widget name *and* a `FrameSlot.SetSize` that could never size a `LayoutSlot` child), BUG-134 gave the info panel a real close button on a new action, BUG-135 was already fixed by `fast-travel`, BUG-137 made `OnLocationClicked` reachable, and **BUG-136 was fixed rather than deferred**: per-type `m_fRefreshInterval` polling now re-populates and reconciles markers mid-session (5 s for Town/Base/RadioTower/FOB/Camp, 2 s for Vehicle), so vehicle markers track live instead of freezing at a map-open snapshot. That was never actually a blocker on `location-types` — legacy sampled once at build time too — but it does add an unverified path to every remaining play-test, since reconciliation destroys elements while the map is open and a missed reference cleanup would re-create BUG-135 on a timer. **With `location-types` and `fast-travel` both landed, `legacy-retirement`'s hard gate was satisfied and the legacy map is now DELETED** — build Phases 0–5 complete and gated on 2026-08-10, Phase 6 (documentation) landing. Phase 0 captured the baseline every later gate is checked against — compile 0 / 5959 files, Fast 44, All 79, tree clean at `28c2f957` — under the standing rule that a changed test count is a finding to investigate, never a number to update; **it held at every phase (compile 0 / 5958 files after the deletions, Fast 44, All 79)**. What went: the "Map Info" and "Fast Travel" main-menu rows and their two methods (with the menu then reordered by frequency of use on a user directive — `Place, Build, Resistance, Jobs, Real Estate, Manage Recruits, Character Sheet, Save, Options` — after the relocated `SetFocusedWidget` call, the menu's *only* one, turned out to land on the third visible row); `OVT_MapContext` stripped **591 → 79 lines** to four map-*gadget* helpers, which **structurally closes BUG-069 part 4** (no Overthrow context subscribes to any static map invoker any more); `OVT_MapIcons.c` (846 lines), three layouts and their `.meta` files, the `OVT_MapIcons` config block and four dead `.et` attributes; **four unvalidated `RequestFastTravel*` RPCs deleted from `OVT_PlayerCommsComponent` — a security fix, not a cleanup** (a two-line unpaid teleport to an arbitrary vector, callable by any client, whose recruit ring-placement half was proven verbatim-identical to the surviving server-side one before being cut); and one orphan string id + its broadcast preset. **Retained deliberately:** `OVT_MapRestrictedAreas` (the FOB rings — BUG-070's fix must not regress), the disabled `OVT_MapThreatGrid`, `OVT_MapPlayerLocation` and the bus-stop duplicate-suppression config block. The superseded `towns/map-info` docs are archived to `docs/archive/towns-map-info-*.md`. 🔴 **Phase 7 — Workbench load, single-player sweep, two-client MP/JIP, gamepad and save compatibility — is user-driven and has NOT been run**, and it is the *only* gate that can see `.layout`/`.conf`/`.et`/`.st` integrity, which is where four of the seven phases did their work. The epic's remaining risk is therefore **entirely verification, not construction**: that gate folds in `location-types` Phase 7 and `map/core`'s MP/gamepad rows, none of which have ever been run, so per-player marker isolation — including the house privacy fix — is still unconfirmed across two clients. Still owed inside Phase 6: the in-game Field Manual still documents both deleted menu entries verbatim. **✅ 2026-08-11 — the epic's last two play-test gates closed.** `fast-travel` ran the two-client MP matrix open since it was planned (the only evidence server-authoritative travel works against a real second client, not just in a code reading), and `location-types` discharged V-3…V-7 — **V-5 being the one that mattered**, since single player cannot distinguish "filters correctly" from "every id is yours", making it the only proof the house-privacy fix isolates per player. The redrawn price carets also landed. **Remaining debt is small:** seven findings written up but unfiled (`location-types` 7b) and `core`'s `FindAnyWidget` sweep. 🟡 **`map/respawn` is built end to end (Phases 0–8, 126/152) and has zero runtime evidence.** A Conflict-style picker on a dedicated SPAWNSCREEN map: on death the player is shown every location they are entitled to spawn at and chooses, with **"Respawn at home" always available** — true by construction, because home routes through the untouched `CreateFreshCharacter` and never goes through the eligibility rules at all. The server never uses the client's vector as a position, only as a lookup key against its **own** enumeration, spawning at its own recorded vector and falling back to home rather than failing. Gates green at every phase boundary (compile 0 / **5964 files**, Fast **44 → 54**, All **79 → 89**, ten Logic cases each proven able to fail), with the Campaign and Persistence tiers measured before *and* after the spawn-path edit and identical. **Four of the plan's premises were wrong and were caught during the build:** `MapContext` is activated by the *menu preset*, not by `SCR_MapEntity`, so a workspace-hosted map would have rendered perfectly and ignored every input; `keyboard:KC_H` is taken three times over; risk R8's claim that neither `gamepad0:x` nor `y` is spoken for is wrong on both counts; and `array<vector> out` does not compile, `out` being a reserved parameter modifier. The corpse-window audit — the one that matters, since deferral stretches "the controller still references the corpse" from one frame to minutes — found **seven** guards where the plan listed five. ✅ **Play-test gate DISCHARGED 2026-08-11 — the user ran the full §6 matrix (Workbench load, SP sweep, two clients, disconnect-while-awaiting, gamepad-only, localization) and reported all green — and NO code change was required to pass it**, the tree at the play-test being byte-identical to the tree at the end of Phase 8. That matters more than usual here, because none of this feature is observable to `compile-check.sh` or to either test group: the autotest world has no players, so `OnPlayerKilled_S` never fires, no RPC crosses a wire, there is no UI tier, and the two `.conf`, two `.layout`, three `.et` and one `.st` file are invisible to both. Three findings are now measured rather than inferred, and all three are reusable: **a workspace-hosted vanilla map does take input** provided the context activates `MapContext` itself every frame (so `Overthrow` still needs no `ChimeraMenuBase` menus, and the recorded fallback was never used); **a dead player keeps ownership of its `OVT_OverthrowController`**, which the whole client→server path rested on as a source reading; and **the persistent id resolves with no controlled entity**, retiring the risk that this feature ships looking correct and drawing nothing. Carried forward as config dependencies rather than defects: `gamepad0:x` is inert only while `MapRespawn.conf` omits `SCR_MapRadialUI`/`SCR_MapDrawingUI`, and Overthrow may have exactly one SPAWNSCREEN config. **`territory-overlay` built 2026-08-11 (feature 6, first stretch goal, one autorun)** — a faction-coloured Voronoi partition over towns, bases, radio towers and FOBs, drawn beneath the markers as a *display of existing control and never a mechanic* (DoD I-4's four boundary greps all clean: no replicated state, no RPC, no persistence, no write to any campaign record). Phase 1 fixed a **latent defect on the base class every canvas layer inherits** — each layer called `SetDrawCommands` with its own private list, so the last module to run silently discarded every earlier layer's commands; invisible only because `OVT_MapThreatGrid` ships disabled. `OVT_MapCanvasCompositor` composites one ordered list and **unblocks `map/map-layers`** with the generic-registration answer (toggle via `SetLayerVisible`, never `SetActive`, which is one-way from script). A throwaway Workbench probe settled three engine unknowns with **zero vanilla usages** — textured polygons work, `TriMeshDrawCommand` works (⇒ one draw command per cell, correctness by construction), and the 3-call affine projection basis is **exact** (max error 0–1.41 px, pure integer truncation), retiring the mirrored-overlay risk. **Two user looks at the rendered map produced two design changes the plan had wrong**: D6 removed the maximum influence radius (cells now stop only at a rival or the coast — the plan's "clip to influence radius" rendered as splotches of colour with dead ground between), and D7 restricted neutral bands to *inter-faction* frontiers (the solver's `RIVAL` meant another **site**, not another **faction**, so bands were drawn through uncontested friendly territory). D6 also invalidated the plan's entire cost model, answered in Phase 6 by a closed-form takeover radius that removes the per-step rival test and makes boundaries exact rather than bisection-approximated — **kept honest by retaining the old brute-force march as an independent oracle**, which caught a dropped factor of two in the quadratic. 12 Logic cases, every one proven able to fail (Fast 54 → 66, All 89 → 101). ✅ **Play-test gate DISCHARGED 2026-08-11** — the user ran SP and MP and reported *"play-test and MP all green, no issues, looks great"*, then closed the feature. **The overlay reached that state through ten user decisions (D6–D15) made while looking at renders, not through any gate**: the plan's "clip cells to an influence radius" rendered as splotches of colour with dead ground between (**D6**), neutral bands were drawn through uncontested friendly ground because the solver's `RIVAL` meant *another site* and never *another faction* (**D7**), per-town alpha fragmented a one-faction island into noise (**D9**), and the overlay was ultimately reframed to draw **occupier ground only** so liberated territory reads as clean map — a progress bar for the campaign rather than a who-holds-what key (**D10**). **The core geometric representation was replaced twice.** The original ray-march rested on "each cell is star-shaped about its own site", which holds for rival boundaries and **breaks against an indented coastline** — a ray stops at the first water, so land across a bay never fills; that assumption produced every artefact reported. It gave way to an **ownership grid** (**D12**, topology stops mattering), then to **marching-squares contours + Chaikin smoothing with a trapezoid scanline fill** (**D13**) so the fill and the border are cut from **one** boundary instead of two. Automated coverage went **54 → 74** Fast / **89 → 109** All, every case proven able to fail, with seven deleted alongside the ray-march code they pinned rather than left as orphans. ⚠️ **Three rows are open on purpose and are not oversights**: Q-1's three frame-cost numbers were never *recorded* (the play-test discharges Q-2, "no visible hitch", not Q-1's "recorded numbers"), Q-4's broken-texture degrade path was never executed, and the six localization exports are the user's to regenerate. **`help-docs-sync` was deliberately not run** — the agent lives on `main`, which is 15 commits ahead of `new-map`, and the branch that added it also added the tutorial/Field-Manual surface it would target; running it here would author player-facing text against the wrong baseline. It is owed after the user merges `main`. **`map-layers` (feature 7) — built end to end in one autonomous run and not executed once.** A single docked panel on vanilla's map tool strip: one row per thing that draws — 14 location types, the territory and restricted-area canvas layers, and one hand-built player-marker row — each row simultaneously the legend entry and the on/off control, generated from config so a new location type or canvas layer adds a toggle with **zero code change**. Preferences persist per **profile** (not per campaign) through vanilla `ModuleGameSettings`. Gates: compile **0 / 5994 files**, Fast **89**, All **127** (+2 Logic cases, both proven able to fail), input-conflict checker exit 0. 🔴 **Two of the plan's load-bearing assumptions were wrong and were caught by building, not by testing.** **(1)** Risk R1 — rated *Low* — fired on day one: vanilla's `SCR_MapToolMenuUI` was genuinely **absent** from Overthrow's `m_aUIComponents`, because the evidence that made it look safe was about `m_aModules` and did not transfer; the plan's one-line fallback became the actual path, and P1 is now a load-bearing play-test question rather than a formality. **(2)** Settled decision D1 held that a vanilla tool-menu entry *"closes the gamepad requirement for free"* — true for *reaching* the panel, false for *walking its rows*, since `MapContext` carries **zero** `Menu*` navigation actions. Vanilla hit this twice and solved it identically both times (`TaskListMapContext`, `MapMarkerEditContext` — dedicated contexts re-activated every frame). The user chose to **build the fix** (**D4**, Phase 4b): a new `OverthrowMapLayersContext` at priority 70 re-referencing only actions that already exist — **zero new keys or pad buttons**, which is what acceptance criterion Q-3 was actually protecting even though its letter (an empty `chimeraInputCommon.conf` diff) is deliberately broken. Its three costs are documented rather than discovered: **D-pad Left untickes the focused row instead of returning to the tool strip**, the left stick stops panning the map, and character movement freezes while the panel is open. ⚠️ **Nothing this feature draws, binds or persists has ever run** — `.layout`/`.meta`/`.conf`/`.st` files are invisible to `compile-check.sh` **and** to both test groups, and the project has no widget, input or rendering tier, so **Phase 7 is the feature's only evidence** and its **Q-1 gamepad pass is decisive**: `requirements.md` states a filter panel that only works with a mouse is *worse than no filter panel*. `help-docs-sync` **did** run here (unlike `territory-overlay` — the Field Manual surface now exists on this branch) and **cut five unverifiable claims rather than ship them**, including the whole gamepad walkthrough. Six incidental findings (**F1 – F6**) are written up in `docs/features/map/map-layers/findings.md` for filing from **BUG-146**; F6 is new and notable — `OverthrowCloseInfoPanel` from BUG-134 is probably **gamepad-dead**, sitting on `gamepad0:b` at priority 50 beneath vanilla's `MapEscape` at 55, a cross-context collision the repo's conflict checker structurally cannot see. Also owed: the user must regenerate the six localization exports — this feature adds **21** pending ids. |
| towns (epic) | 📄 Documented (5/5 retrospective) | — | Towns — populations, stability & support modifiers, gun dealers and the map UI — backfilled with retrospective docs (`core`, `stability`, `support`, `gun-dealers`, `map-info`). Discovery catalogued ~100 concrete issues, headlined by: permanent town modifiers silently wiped on the tick rebuild (with permanent client chip desync), hour-gated handlers only ever processing the first town, fully client-trusted supporter conversion (one RPC flips every village), unvalidated town RPCs, dealer weapon stock that has never restocked, and a map whose icon-validation pass runs every frame. Top 17 filed as **BUG-055…071** (BUG-054 was taken by a concurrent economy fix); cross-feature debt seeded in the epic's Tech Debt section. |
| new-player-experience (epic) | ✅ **Complete** (**5/5 built, 4 fully signed off**; #1 discharged in practice) | **242/242** (tutorial-system 61/61 · field-manual 56/56 · tutorial-content 24/24 · first-spawn 49/49 · starter-jobs-retirement 52/52) | Replacing a hostile new-player onboarding — one 20-second hint that repeats every session, and five "tutorial" starter jobs. ⚠️ **Correction (2026-08-09):** earlier revisions of this row said the starter jobs "only ever reach the first player on a server (BUG-037)". That was **stale from 2026-08-03**, when BUG-037 and BUG-040 were fixed **in place**; both have been `closed` since. The jobs were ultimately retired for **redundancy** against the shipped tutorial entries, closing no bug. The same revisions gave the jobs' rewards as "$100 and 10 XP" — also wrong; `m_iRewardXP` defaults to 5 and is paid unconditionally, so the true total is **$100, 30 XP and two field dressings**. **New (2026-08-07): `tutorial-system` built — the framework the other four features run on.** Config-driven tutorial entries fire off the player's own actions via ten existing server-side manager invokers, are delivered to **only** the acting player's client (`RplRcver.Owner` on `OVT_OverthrowController` — structurally incapable of the broadcast that broke the starter jobs), and show at most once **per machine, ever**, via a mod-owned `ModuleGameSettings` store. Two presentations (non-modal HUD overlay + modal with a multi-page sequence for `first-spawn`), a per-machine "don't show tips again" toggle, and a "Learn more" deep link into the field manual. All 66 → **75** assertions, 9 new cases, **3 proven red by genuine defects**. Two design risks **discharged by measurement, not assumption**: R1 (the nested seen-array really does round-trip — block quoted from the on-disk profile) and R2 (the same-GUID field-manual delta really does merge — 6 categories / 8 tile backgrounds / 141 entries, and the merge mechanism *demonstrated* by deliberately colliding an element GUID). One criterion retired: **F5**, the escalation keybinding — risk R3 fired twice, there is no free gamepad input during gameplay, so the route is HUD prompt → main menu → Tips and **no gameplay binding was added**. Owed: a Workbench export of **17** string ids, then the play-test and two-client MP checklists. Surfaced five pre-existing defects outside its own scope, filed **BUG-090…094** — headlined by a listen-server host never receiving its own owner-targeted RPCs, and `check-input-conflicts.py` being blind to 197 inline-declared actions. **All five were fixed on `main` and merged back into the branch 2026-08-08**; the checker rewrite plus the `pad_down` rebind took the input report from 23 warnings / 12 pre-existing to **0/0**, which retired the one caveat standing on this feature's Q5. **New (2026-08-08): `field-manual` built — 56/56 tasks, 9 phases in one autorun.** The one-page Overthrow manual is now **twelve entries under four sub-categories**, every one written from a reading of the source rather than from memory, with **102 new string ids already exported**. The twelve `m_sTitle` keys are the deep-link ids and are **frozen and published** (`field-manual/implementation.md` §3.3), so `tutorial-content` has its "Learn more" targets and `first-spawn` has `#OVT-FieldManual_Welcome_Title`. One `.c` file was touched, test-only: three branches added to the existing Init guard (**+164/-0**, zero assertions weakened) catching the silent-pruning trap, a missing sub-category and a duplicate title key, each **proven red** against a realistic breakage. The feature also swept **~35 public wiki pages** and the sweep is where the value compounded: the wiki carried **nine wrong difficulty numbers** (Extreme told players death costs 100 when it costs 25), described `realEstateCostMultiplier` as a working discount when it is applied nowhere, omitted an entire fifth difficulty preset (**`Insane`** ships, documented nowhere), had the resistance tax applied after the income split instead of before it, and priced camps at $150 when `placeables.conf` says 250. Writing both surfaces from one reading is what surfaced all of it. **Verifying before writing killed a well-formed lie mid-draft:** an entry had already been drafted saying "nothing raises a wanted level unless somebody can see you" when the source shows violence escalates with **no** witness check and **no** faction check at all. **16 open questions for a gameplay owner** were logged rather than guessed, including an integer-division bug that makes shop prices a binary step instead of the advertised curve, and recruit XP hardcoded to faction keys `"US"`/`"USSR"` so recruits silently never level on a compatibility-mod faction. **Closed 2026-08-08: play-test passed, all twelve hand-made ink tiles delivered and wired, 24/24 Definition of Done.** Seven of those rows (rendering, tone, wiki agreement) are the owner's attested verdict rather than a machine measurement, which is recorded as such. **New (2026-08-09): `tutorial-content` built — 24/24 tasks, 4 phases in one autorun.** Ten action-triggered entries now ship: six a player meets in their first hour (home, money and shops, gun dealers, the map, the wanted system, skills) and four at their first escalation (recruits, placing and camps, building, base capture). **The framework's central claim held at scale** — all ten were added with **zero gameplay EnforceScript**, nine `.conf` files and 18 string ids and nine prefab lines, the only `.c` touched being a test file. Nine hang off per-player events; the tenth (base capture) is a documented 300 m proximity fan-out with no acting player, so its body was written to read correctly for a bystander *and* for the occupiers **re-taking** a base, and contains no second person at all. **Rule 0 was a phase-front task rather than a review step, and it earned its place three times:** a sentence the plan itself specified was **cut** because nothing in the code makes the occupying faction "come looking" at a wanted level (only a perception override exists); "a base weakens the occupiers *in that area*" was narrowed because `m_iThreat` is a single **global** counter; and — the finding that generalises — **the plan's own pre-loaded trap table contained a false row**, claiming gun dealers have no dedicated map icon when they do, via a separate enumeration path and their own sprite. Left standing it would have driven the wiki pass to "correct" a page that was already right; it was caught, verified independently and struck in three places. **A trap list is not evidence.** The one `.c` change is test-only and closes the feature's single silent-failure mode: `SHOP_GUNDEALER` is the lone filter produced by an engine call rather than authored, so a `+60/-0` branch now fails the Init tier by name if it stops matching any `OVT_ShopType`, **proven red** by breaking it (exit 1, `FAILED (1 of 47)`) and reverted byte-exact. That check also settled the risk in the affirmative: the enum does return the bare member name, so the tip was never dead. **`starter-jobs-retirement` (#5) is now unblocked** — its precondition, the as-built coverage mapping, is written into both features' docs, and it carries a finding of its own: the map already marks every shop and dealer **ungated by any discovery flag**, so the five jobs' unique contribution was a marker on one *named instance* plus $100 and 10 XP. Owed: a Workbench export of **18 new ids + 1 rewritten body** (until then every tip draws its raw `#OVT-` key), the play-test, and **one wiki edit that could not be applied** because `wikijs_get_page` fails on every page and writing blind would have destroyed the `**Tutorial Jobs**` paragraph #5 owns. Also reported, not fixed: **two Field Manual strings are now known wrong** while the public wiki is right on both. **New (2026-08-09): `first-spawn` built and closed — 49/49 tasks, 7 phases in one autorun, play-test passed the same day.** The opening two minutes are no longer a random teleport plus a 20-second hint that repeats forever. A **four-page dismissable welcome modal** fires on the player's own first spawn and never again on that machine, and the campaign-setup screen finally explains what its three choices do. **This is the first feature to extend the framework with code rather than config**, and the reason is a fact the client provably cannot derive: `FinalizePlayerPreparation` has two branches — one grants a house, a car and cash, the other spawns at a bus stop with none of it — and `home != vector.Zero` is true in **both**, because the fallback path calls `SetHomePos` too. So the server authors a per-player spawn context and pushes it over one `RplRcver.Owner` RPC, and **two** entries filtered `house`/`nohouse` on the same `PLAYER_SPAWNED` event select between themselves, so the player ever sees exactly one modal. Three of four pages **share string keys deliberately**, because two copies of a sentence can drift and no gate can see it, and the houseless variant is precisely the one nobody play-tests. Failure degrades rather than disappearing: a lost context costs one page of accuracy, never the welcome. **The legacy `#OVT-IntroHint` is retired**, and its "dead since the 1.6 spawn rework" code comment was re-verified and found **only two-thirds true** — it genuinely still fired on single-player respawn-after-death and after a Continue. Both its string items were **kept** (six languages of translation) with a retirement record naming the three claims in its own text that were or became false. **Rule 0 bound this feature twice over** — eight pages of prose plus nine setup descriptions — and the phase-front fact-check **cut nine sentences for want of a `file:line`**, including one the plan itself specified. Three "obvious" things to write about were disqualified by evidence: `realEstateCostMultiplier` is declared, replicated and **read by nobody**; `ChargeRespawn` only charges when `money > 500`, a hardcoded gate unrelated to the setting; and the supporting faction **sends no troops, supplies no equipment and touches no price**, besides being neither replicated nor persisted. All three recorded as findings, none fixed — they are balance and save-format changes wearing bug-fix clothes. Two failure modes were caught that no gate in this project can see: **`TextWidget` does not wrap unless `Wrap 1` is set**, so every setup description would have clipped to a single line; and **an image carries a claim exactly as a sentence does** — the first art wiring pointed *both* entries' page 2 at the same car-in-a-garage header, so the houseless entry illustrated "you own nothing" with a picture of a car in a garage, the exact contradiction the two-entry design exists to prevent, reintroduced through the artwork after the prose had been fact-checked to death. **The feature ships with the first page art any tutorial entry has ever carried**, which also means `LoadImageTexture` ran in a shipped configuration for the first time. Gates: compile 0, Fast **51**, All **88** — and the plan's own `+3` target was arithmetically wrong and was corrected rather than chased, because one of the three cases was always specified as a *branch* on an existing guard and a branch adds assertions, not executed cases. Four mutations proven red, the Init guard's two branches separately. Owed: one small string re-export for `OVT-FieldManual_Welcome_Text2`, and **the MP pass — now the epic's single highest-value outstanding verification**, since two-client per-player isolation has never been observed passing and this feature adds a new owner RPC to that same path. Next: `starter-jobs-retirement` closes the epic, and it inherits one concrete liability — `welcome-intro-3-ui.edds` is a screenshot of the Overthrow menu **with Jobs visible**, so removing that entry silently makes the image wrong. **New (2026-08-09): `starter-jobs-retirement` built — 52/52 tasks, 7 phases in one autorun. The epic is now fully built.** The five starter jobs are gone, and the inherited `.edds` liability was **discharged by inspection, not work**: seven jobs survive, so the Jobs *menu entry* stays and the screenshot stays true. **The feature was never really about deleting five files — it was about the int that named them.** `OVT_Job.jobIndex` is a **persisted array position**, and the five doomed configs sat at positions 2, 4, 5, 6, 7 of twelve, so a naive delete would have silently re-attached five saved records to *different* jobs at valid-looking stage indices and paid out those jobs' rewards ($750 instead of $0), while the lifetime-counter maps capped the wrong jobs — no error, no log line, no crash. So the order was: stable id → migrate the save format onto it → **prove it** → only then delete, with a real decision gate in front of the deletion. `OVT_JobConfig.m_sId` now gives every job an identity independent of list order, the payload is version 2 keyed on it, and a **frozen** twelve-entry legacy table migrates pre-feature saves, dropping each retired record with a WARNING that **names** it. **The plan's own mitigation contained the bug it was mitigating**, and only measurement caught it: D4 specified reading v1 payloads through *newly named* shadow classes, but the binary container writes the concrete class name as a `$type` discriminator and instantiates from it, so the rename would have made every v1 read return an **empty-but-non-null** array — board wiped, every counter wiped, silently. Proven with round-trips through the real save context (control read *last*, ruling out single-read artefacts) and fixed by inversion: the **existing** names are frozen as v1-only and the new v2 records take new names. Two further "belt-and-braces" notes were promoted to load-bearing by the same measurements — the local variable name **is** the property name and **is** validated, and an empty collection reads back `true` while only a real failure reads `false`. Verified end to end on a **captured pre-migration save fixture** (the one artifact with a deadline — impossible to produce after the migration landed): three retired ids dropped by name, two survivors restored on their correct configs, matching a byte-level decode field for field, twice, idempotently — and through a **real `SaveGameManager` world transition**, a path §7 had listed as structurally uncoverable. Zero wire surface changed, proven by diff rather than asserted (BUG-090: `Rpc()` arity compiles clean and dies at the wire, so the design gave that hazard no surface at all). **Closes no bug** — BUG-037/040 were fixed in place on 2026-08-03 — but discharges the **jobs epic's positional-`jobIndex` tech debt**, and the plan's top risk (whether Eden's world-layer override merges or replaces) was settled by direct headless measurement: it **merges**, Eden always ran all twelve, so no bug was filed and the block was deleted as pure cleanup. Gates: compile 0, Fast **54**, All **92**. **Closed 2026-08-09: play-test passed, all eleven checks.** U1 — Continue a campaign save taken *before* this build — **passed on a real campaign**, verifying the migration at a scale the seeded fixture could not reach. **And M1-M5 passed, giving the epic its first two-client observation ever**: M3 saw an owner-filtered send reach A and *not* B with two real clients, and M4 saw a job board survive JIP through `RplSave`/`RplLoad`. That is the jobs surface, **not** the tutorial-popup surface — `tutorial-system`'s F7 is still unobserved — but it is the same engine mechanism, so the epic's last open verification is now both smaller and better understood. **The epic is complete.** |

| logistics (epic) | 🟡 **In Progress (2/4 closed, 1 built)** | 214/218 (98%) | Mid-game supply sub-game, reorganized 2026-08-20 from six features to four: `ui` → `storage` → `resources` → `building-repair`. **`ui` is built and play-tested GREEN (user, 2026-08-21)** — one shared **List + Cart + Destination** transfer screen (`OVT_TransferContext`) over two Logic-tier-tested pure models and a tab-host class shared with the shop; the port Import and warehouse Take screens became thin subclasses and the two old screens, their row layouts and handlers, six input actions and two `ActionContext`s were deleted. **`storage` is built AND play-tested green the same day (user sign-off)** — all 10 phases, a cross-phase review and a B5 fix pass, every automated gate green. Overthrow stored stockpiles as **spawned entities** (a full ammo box was several hundred replicated entities the engine streamed, simulated and serialized — the direct cause of the network spikes and the save-time frame collapse); it now stores an **item ledger** on one `OVT_StorageComponent` authored on three shared prefab bases, so every wheeled vehicle, ammo box and warehouse building has one. Contents never leave the server except to the one player who opened a holder — only **count + name + capacity** replicate, as three ordinary `RplProp`s that are free for JIP. Port import and warehouse take are now **zero-spawn** ledger arithmetic; box Load/Unload, truck Loot and FOB undeploy run on one per-player chunked job engine with a real progress bar; port **Export** is new; and the old warehouse vocabulary — `OVT_WarehouseData.inventory`, `isLinked`, ten warehouse methods, four warehouse RPCs and `OVT_WarehouseContext` — is **deleted**, not adapted. Nine defects came out of that play-test and are all fixed (P1-P9), including a shipped-base crash: `ItemPreviewManagerEntity` renders an icon by spawning a **worldless** instance, so any `OVT_Component` on a previewable prefab null-dereferenced in `OnPostInit` - a latent bug `main` still carries. Owed on `storage`: one more string-table re-export (two keys added after the user's export), the wiki pass, and **multiplayer, which is entirely unproven at runtime** - no dedicated-server session was ever run, so F12/JIP/concurrent-batch/disconnect are proven by reading only; owed on `ui`: a wiki pass (blocked — no `wikijs` MCP server). **`resources` is BUILT (2026-08-21, Ready for Review)** — 11 phases plus a cross-phase review, 95/98 tasks. It adds the second axis to Overthrow's mid-game: buildings that cost only money now cost **materials you have to physically move**. A config-driven catalogue (`resources.conf` — Timber/Cement/Steel/Hardware) is bought at the port from a new **Resources tab** in both Import and Export at a **drifting, war-biased, persisted** price — the first price Overthrow has ever stored, replicated and drifted rather than reading once from config. Every truck in the game hauls, via a **same-GUID delta of vanilla `Wheeled_Truck_Base.et`**, which settles the epic's biggest open risk: it is the narrowest prefab reaching both vanilla truck families, so it excludes cars and APCs *by construction* rather than by a runtime check. Contents ride **one packed `RplProp` string**, which makes JIP free and turns the cargo HUD, the pile label, the map rows and the Build shortfall text into local reads with no protocol at all. Materials drop as **merging crate piles** that self-register on the map with no registry (`OVT_MapMarkerComponent` already did that), and Guard Tower / Helipad / Garage / the new buildable **Warehouse** now raise a **construction site** — money charged at placement — that Build finishes by consuming nearby piles nearest-first. The `BuildItem` split is the delicate part and it held: the moved `:872-922` region diffs to **exactly one delta**, the `if (charge)` wrapper, with the handler-failure path still returning before any charge, so a buildable with no requirements is byte-identical to today. **`storage` was never touched** — its seven files are `git diff` clean, the epic's "two ledgers, deliberately" wall intact. Three defects the plan did not anticipate were caught while building: a **money exploit** (nothing bounded a cart line's *quantity*, so 16 lines at `int.MAX` overflow the litre and money totals **negative**, and a negative total passes `PlayerHasMoney()` after which `TakePlayerMoney()` *pays the player*); a **silent build failure** (client and server disagreed on the town gate, so a Warehouse at a held base inside an enemy town was offered then refused with no message at all); and the **drift readout describing no drift** on any preset but Normal, because it divided the difficulty-multiplied price by the raw base. Binding 4 also back-fills a latent `storage` gap — the Buildable persistence config carried no `OVT_StorageComponentSerializer`, so any *built* holder would have lost its item ledger on reload. Gate: `compile-check` 0 (6252 files) · Logic **247/247** · Init **174/175** (the one red pre-existing) · PersistenceRoundTrip **40/40** · Campaign **18/18** · Persistence **13/13** — 492 cases, 50 new. **Owed: three play-tests (SP / gamepad-only / dedicated + JIP), the `.st` re-export (81 keys render raw until then), a Workbench re-import of the map atlas `.edds` (the pile marker draws blank without it), resource icons (unbuilt §3.1 deliverable), and the wiki pass (blocked — no `wikijs` MCP server).** `building-repair` remains formalized requirements, and is now **unblocked** — `resources` ships the position-based `NearbyAvailability` / `Consume` helpers it was planned against. **Deliberately NOT the vanilla resource system** — `EResourceType` is a hard engine enum (`SUPPLIES`, `ELECTRICITY`) a mod cannot extend, so `SCR_Resource*` cannot back a multi-type engine; Conflict's UX patterns are reused, its types are not. The epic's biggest open risk is **settled** — see `resources`' D19 for the truck-delta approach actually shipped and the two alternatives rejected. |
| virtualization (epic) | ✅ Complete (5/5 features) | 242/242 (core + civilians + movement + integration + base-defense-migration) | The unified AI virtualization layer (issue **#100**, a thin registry over Reforger 1.8's engine-native group lifecycle): **core COMPLETE 2026-08-17** (registry/API, survivor masks, ambient seam, Route B persistence, frozen `api.md`), **civilians COMPLETE 2026-08-17** (town-civilian ambience: config density, opt-in QRF despawn, parked vehicles; BUG-179/180 fixed), **movement COMPLETE 2026-08-17 incl. user play-test** (infantry-only virtual movement: 2 s round-robin tick, stateless projection resume + `GetCurrentPlanIndex` live-handoff, water-veto writes) and **integration COMPLETE 2026-08-17** (the first real consumers: the three deployment configs plus radio-tower garrisons — now `Deployment_TowerGarrison.conf` — run on the layer; every ad-hoc proximity toggle and the 460-line `OVT_EntitySpawningAPI.c` deleted; deployment serializer v2 with a persisted virtual key; an additive entity-observer API so a parked recruit squad holds content awake; **dead members stay dead across despawn AND save/load**, patrols keep walking unobserved, and a tower flips only on a real wipe). and **base-defense-migration COMPLETE 2026-08-18** (the closer: the base-upgrades system replaced by **nine deployment configs** so a base fortifies concern by concern; the evaluator learned to escalate and the per-faction ceiling was raised to 400; exact placement generalized behind a provider seam for tower/sniper posts; **one funding path**, i.e. the per-base spender deleted, 80 % of every tick credited to the deployment pool, legacy saves converted by value refund; specops **dropped with a written cost** (D3); `BaseUpgrades/`, `m_iMilitarySpawnDistance` and **`OVT_VirtPlaytestKillSwitch.c` deleted**, so `grep -rn "OVT-VIRT-PLAYTEST-ONLY" Scripts/` returns nothing, the epic's final acceptance). Gates: All **278 green 2026-08-18**. Every occupying-faction AI force now rides one layer. User play-tests + an MP pass tracked in the five `context.md`s. |
| gm (epic) | 🟡 In Progress (4/5 features — all four built features closed at 100%, every owed verification discharged 2026-08-16) | 113/113 (gm-state 28/28 + overthrow-panel 26/26 + hud-icons 30/30 + waypoint-viz 29/29; gm-map not yet planned) | Game Master tooling for server owners, in 3 epic phases; epic Phase 1 (read-only campaign inspection, ships with v1.5.0) is scaffolded as 5 features: `gm-state` → `overthrow-panel` → `hud-icons` → `waypoint-viz` (∥) → `gm-map`. **`waypoint-viz` — 3D route drawing for a selected AI group — is BUILT + code-complete 2026-08-15 (one autorun, Phases 0–3+5):** selecting an Overthrow group in the GM editor draws its full waypoint route in the 3D view (group → WP1 → … , cycles closed, current leg highlighted) via a new `GROUP_WAYPOINTS` request on the gm-state seam — forced by the engine (vanilla waypoint prefabs have no `RplComponent`, so waypoints exist only on the server) — a server-side cycle-flattening walk immune to Overthrow's known waypoint leaks, and a widget-free `Shape`-API renderer hosted from the modded EDIT-mode root; purely additive into the seam (384 insertions, arity table hand-checked per BUG-090), zero GUIDs minted (`{6B0A…}` still free for gm-map), zero prints, read-only grep-proven. Gates: compile 0 ×3, All group **190/190 green** (2 clean runs; a third finished 190/190 then wedged at client shutdown — environment, not regression), 2 new proven-can-fail cases (Logic classifier incl. the Defend/Patrol ordering trap + Campaign cycle-recursion fixture). ✅ **Verification DISCHARGED 2026-08-16** — Workbench render rounds (two live-feedback fixes: selection resolves via `GetAIGroup()` since a group click lands a CHARACTER editable; per-leg `CreateLine` after `CreateLines` from a member fixed array silently rendered nothing) + dedicated-server GM client + non-admin negative path, all user-confirmed. Found **BUG-175** (high, vanilla): assigning a GM waypoint to a cycled group crashes with recursive Invoke — every Overthrow patrol is cycled; fix parked with epic Phase 3. **`hud-icons` — the epic's riskiest base-game integration — is BUILT + user-verified 2026-08-15 (one autorun, Phases 0–4+6, descoped to tooltips-only on same-day user feedback, then verified incl. MP/JIP and both big tower variants):** every town, occupying-faction base and radio tower now carries a faction-tinted icon in the GM view via an `SCR_EditableSystemComponent` subclass on Overthrow's own prefabs (SYSTEM type, 48 px slot, 2 km vanilla-like draw distance — lowered from 20 km on 2026-08-16 user feedback: whole-map icons were too busy — non-deletable + non-movable via an `IsReplicated()→false` lock), and hovering renders live campaign state in the vanilla tooltip through a per-instance `GetDescription()` (town: support+stability; base: "N Resources - N Garrison Groups", seam-gated with an honest no-data fallback; tower: online/sabotage countdown) — **zero base-game forks, zero config deltas, zero new networking (grep-proven)**. The click-driven detail surface built in Phases 2–3 was **removed at the user's direction after live testing** (it stretched the panel); group origin and player money/level readouts went with it and have no surface until epic Phase 2's popup actions. The build's headline finding: the plan's tower-LOCAL flag was **wrong** — the tower prefabs are same-GUID deltas inheriting vanilla `RplComponent`, so LOCAL would have silently nulled every tower icon; all five prefabs ship flags 2052 (verified against the vanilla tree before acceptance, plan annotated in place). Gates: compile 0, Fast **145/145**, six new test cases (5 Init incl. a HasDescription-trap guard + 1 Logic) each proven able to fail by real red runs with named failures. ✅ **Verification DISCHARGED same day — user confirmed all checks green incl. MP/JIP and both big tower variants** (auth-path detail still unrecorded — the epic's one lingering bookkeeping item). gm-state's whole remaining MP checklist was discharged 2026-08-16 — user confirmed the batched epic test session covered it (log measurements not recorded separately). **`overthrow-panel` — the first renderer over the seam — is BUILT + user-verified 2026-08-15** (Workbench visual check with live feedback rounds + MP pass on the user's own server): a read-only panel injected into the GM editor's EDIT mode (top-left 33,250 — moved off the bottom-left stack because the base game's contextual help overlapped it) showing threat/OF pools/countdowns with suppression reasons, live resistance funds and QRF, plus an empty documented detail seam for `hud-icons`; zero new networking, one modded (empty) base class, Fast 139 green. **`gm-state` — the data spine every other feature consumes — is BUILT 2026-08-14** (one autorun, Phases 0–4+6): a GM-gated, versioned, strictly read-only snapshot fan on `OVT_OverthrowController` (`OVT_GMRequestComponent`, seq-guarded Begin…End framing, `RplRcver.Owner` only), a tag-and-sweep group-origin registry (17 spawn-site insertions, all pure), a non-mutating resource-gain predictor extracted from `GainResources()`, and a documented sibling consumption contract. Fast 114 / All 157, three new proven-can-fail cases. gm-state's MP gate is now **fully discharged (2026-08-16)** — user confirmed the batched epic test session (panel + hud-icons + waypoint-viz together) covered the remaining checklist; feature closed at 28/28. |

> Epics are tracked as a single row; per-feature detail lives in each epic's `epic-overview.md` (`docs/features/dev-ops/`, `docs/features/core/`, `docs/features/economy/`, `docs/features/occupying/`, `docs/features/skills/`, `docs/features/resistance/`, `docs/features/jobs/`, `docs/features/towns/`).

## Integration Points

- **dev-ops → core/persistence:** the epic's future `test-coverage` feature writes behaviour-level persistence tests (no EPF/vanilla API in assertions) that become core/persistence's acceptance gate on resume. The compile check already guards its API assumptions (~5s feedback).
- **dev-ops feature #1 → #2/#4/#5:** `tools/README.md` is the stable contract (commands, exit codes 0/1/2/124, `KEY=value` launcher output, `OVERTHROW_*` env overrides). Sibling features consume it without reading script source.

## Changelog

- v1.37 (2026-08-22): **resistance/high-command BUILT — the resistance can command AI groups, and GARRISONS ARE RETIRED. ⚠️ SAVE COMPATIBILITY: PERSISTED GARRISONS ARE DROPPED ON LOAD, with no migration.** All 13 phases (6 ADVANCED) in one autorun, 77/77, plus 8 play-test riders from a live session mid-build. **The resistance previously had no way to assign force to a place** — you bought a garrison that stood at a base forever and could never be redirected. High Command replaces it with groups you own, order and keep. Buy them at a **barracks**: a new buildable (deliberately **the most expensive in the game** — 20000 + timber 170 / cement 280 / steel 170 / hardware 70, sized so the materials are *two almost-full truck loads*), plus **three vanilla barracks families made functional** through same-GUID `_base.et` deltas, interacted with via a **radio** authored into same-GUID **furniture** overrides — seven of them, because Workbench cannot open a prefab the addon does not own, so positioning the radio against furniture required overriding the furniture itself. Price = per-member recruit cost + each member's required gear + **the vehicle at full shop price**, where gear already sitting in a **registered warehouse within 150 m is free** and the screen shows how much it covered; supporters come out of the nearest town; a server-side **member cap** (48, configurable) is the AI budget knob. **This fulfils the shipped `//To-do: factoring in warehouse` on BOTH surfaces** — High Command purchases *and* the recruitment tent's Buy Equipped Recruit, which now also sources gear free and shows the coverage. **The architecture is deliberately expensive (D1/D2):** HC groups are **always-live `SCR_AIGroup`s, never virtualization-registered**, each carrying its own **virtualization AI observer** behind its **own** gate — because there is no virtual-vs-virtual combat resolution, so a dormant group would be a token that loses every fight it is not present for. The accepted cost is real-time travel and a permanent AI budget per group; the payoff is that every engagement is real, wherever it happens and **whoever is online** — groups keep marching, arrive and hold their stance **while their owner is offline, indefinitely**. Commanded from the map: green NATO symbols on **every** player's map (own bright, others at 0.45), a cursor magnet, an info panel, **Defend / Patrol / Attack** stances, a dashed destination line that vanishes on arrival using *the same arrival test the status flag uses*, and contact/ammo badges — fed by a **change-filtered heartbeat** that reports every 2 s while a group is moving and stays **completely silent** for a parked one. **Gamepad was mandatory from day one and nearly failed:** both intended verbs collide with vanilla `MapContext` **itself** (`gamepad0:x` is the radial contextual menu, `KC_B` the protractor), proven live rather than assumed, and the fix is an **exclusive** input context (`Flags 0x2e` — the `0x26` recipe plus the runtime-proven `0x8` bit) **plus** a second, independent lock refusing the lease while a menu context is blocking, because Overthrow menus do not disable the character tier and a player can raise the map over the purchase screen. `Menu*` was deliberately **left out** of that context against the plan's letter: adding navigation actions to an exclusive context would claim `a`, both sticks and the d-pad away from the map — the exact "ships controller-dead" failure the requirement names. Also shipped: a **Manage Groups** roster with `n / cap`, Show on Map and confirmed Dismiss; **one-way recruit conversion** with the holding roster split by group, where converted recruits keep their **bodies and equipment**; automatic **rearm** from registered warehouses and **refuel** charging the owner the same price a manual fill costs (a built fuel depot is free), both reusing the shipped call chains rather than a second pricing implementation. **Garrison retirement** removes the purchase paths, spawn helpers, base/FOB menu halves, the map's Garrison row and the GM registry's three garrison origins — and the base and FOB menus, which were **100% garrison**, were deleted outright along with their layouts and the Manage Base action. **A campaign continued from an older save comes back with all of its camps, FOBs and bases and WITHOUT their garrisons:** `OVT_ResistanceManagerSerializer` goes 1 → **2** and `OVT_OccupyingFactionManagerSerializer` 2 → **3**, both **read-and-discard rather than field deletions** — the `garrison` arrays stay declared and are still read, because a binary save context is **positional** and dropping a field would change each record's length and desynchronise every record after the first. Money spent on a garrison is not refunded. **The build closed four defects the plan itself asserted** — a wrong authored vehicle GUID (`{16E32C3ABEAFC2C6}` is the *uncovered* Ural), a non-existent `SCR_AIGroup.GetMembers`, an `AddAIToSlaveGroup` that **early-returns to a silent no-op** on a non-playable group (empty squads, no error anywhere), and a `GivePatrolWaypoints` that would have **leaked 9 waypoints per re-order** — **and two gaps the plan never assigned to any phase**: the save-load walk (so "groups survive save/continue" could never have passed), and HC member bodies being **untracked the instant they join a group**, which also silently killed the conversion's entire gear promise. That second one led an agent to the shipped `recruit-ux` defect where **parking a recruit untracks its body**, independently reported by a player the same day and fixed on `main`. Gates all green: **Logic 282 / Init 209 / PersistenceRoundTrip 43 / Persistence 13**, compile-check 0, input checker at the shipped baseline (verified with the *committed* checker, so the green is not bought by the two waiver entries added). ⚠️ **Two self-inflicted incidents during the build, both caught and both fixed, same root cause:** deleting a structured block **by line count** instead of by matching braces — 969 `.st` entries destroyed and rebuilt (from the committed file plus 89 surviving session blocks plus 11 re-authored keys), and four prefabs left one brace unbalanced, which swallowed the components after it and was invisible to compile-check until `OVT_TEST_InitSuite` caught it via three GM-icon cases. ✅ **CLOSED the same day: play-test green** — *"it works great"* — including **recruit conversion** and **save/load with both purchased and converted squads**, the two riskiest paths, and the gamepad ordering scheme confirmed working, which vindicates the exclusive-input-context design (`X` does not open the vanilla radial menu). Localization re-exported and the radio positioned in Workbench by the user; the map's destination line widened 2 → 4 px on play-test feedback. **`main` merged into `v1.5` as `d99bb57a`**: its one conflict resolved by adopting main's shared `EntityMustStayTracked()` predicate and **folding the High Command exclusion into it**, so all three protected body categories are named in one place — while keeping v1.5's `NotifyMemberSpawned` call, which taking main's side wholesale would have silently dropped. Post-merge gates: compile-check 0 · Logic 282 · Init 209 · PersistenceRoundTrip 43. **Deferred by the user, not outstanding:** the dedicated-server/JIP pass (next week, with the rest of 1.5 — still the only possible proof of `RplSave`/`RplLoad`) and the wiki (not updated until 1.5 ships).

- v1.36 (2026-08-21): **occupying/objectives BUILT — all 8 phases in one autorun (74/74); Ready for Review. AND: `tools/run-tests.sh` was found to have never tested this worktree — every prior verdict from a non-canonical checkout was another tree's.** The 5,201-line `OVT_ObjectiveDirectorComponent` is now a **4,383-line runner** and the occupying faction's doctrine is authored data: an `OVT_ObjectiveRegistry` of **plans** → ordered **phases** → bags of **condition / operation / abort modules** written in Workbench `.conf`, with target **selectors** scoring one shared candidate set and **resolvers** answering "where". A modder adds a doctrine by writing a config. Built as a **strangler**: Phase 2 landed the framework with both shipped plans running through *legacy shim modules* that called the old methods verbatim, then Phases 3–6 replaced one phase of doctrine at a time and **deleted the methods that shim wrapped in the same phase**, so parity was a diff against a still-present reference. Phase 3's `PlanDrivenSelectionReproducesTheSingleListPick` proved the fork from one candidate list into two plans while both paths still existed — the last moment it was provable. Phase 5 moved ~1,500 lines of forward-base siting/budget/starvation/teardown/restore into one operation module behind a **thin director facade**, so the dismantle user-action and the server validator never learn which module owns an asset. Phase 6 killed both shims, the `m_iLegacyPhase` authoring, the phase enum and **three temporary fallback paths** that had accumulated one per phase (the Definition of Done greps only for `OVT_LegacyPhase` and would have passed with any of them stranded). 🔴 **The session's headline finding is a tooling defect, not a feature one.** All three Overthrow checkouts under the shared repo parent declare the **same addon ID *and* the same GUID**, so `-addonsDir <parent> -addons Overthrow` always resolved to the canonical `Overthrow.Arma4` — and a hardcoded path fallback in the addon-load guard (added earlier to stop a *false INDETERMINATE*) stamped the wrong-tree run **green**. Phase 1's recorded "All 416/417" was void; the suites had never once loaded this branch. Fixed with a junction farm outside the repo parent + `OVERTHROW_GAME_ADDONS_DIRS`, and a guard that resolves every listed gproj through `wslpath -u` + `readlink -f` and compares **file identity** — a junction matches, a sibling does not, and a mismatch now fails INDETERMINATE rather than passing. ⚠️ **`Overthrow.Arma4-main` still has the same defect.** The very first honest run then found a real production bug the false-green had been hiding: **an Enfusion save context keys each property by the LOCAL VARIABLE'S NAME**, so version 2's `Deserialize` reading into `readTargetKind` what `Serialize` wrote as `targetKind` restored every field as zero — `targetKind == 0` = NONE — and silently wiped the objective *and* its forward base. The same trap was already documented in `OVT_JobManagerSerializer` on 2026-08-09 and cost a full gate cycle to rediscover; a per-instance field loop is impossible under name-keying, so the record is now one `array<ref OVT_PersistedObjective>` with every `Read()` checked. **Phase 2's assertions repaid the strangler design three times over**: Phase 4's gate produced three reds and **none was a Phase-4 defect** — two were stale shim-shape assertions, and the third exposed that the validator fixture's *"wholly valid"* control plan had been a **wedge** (a phase with an empty module bag, able neither to advance nor to end) since Phase 2. The rule was right, the fixture was wrong, and repairing it added the first assertion anywhere that **the runtime module set holds clones, never the config's own template objects** — something Phases 5 and 6 both depended on with no cover. Phase 8's fact-check corrected a Field Manual body (**one player** close to a forward base starves it — `baseCloseRange` 220 m — not "in strength") and caught a C5-class landmine in a translator note asserting in capitals that the siege muster is **thirty** real minutes when the shipped constant is **fifteen** (halved 2026-08-20); left standing it would have talked the next editor into "correcting" a true sentence, and the same wrong number is still queued in the closed `counter-attacks` wiki brief. Gates: compile 0 after every phase; **All 417/418 → 439/440**, the single red throughout being the pre-existing `core/damage` `CompositionSlotGate` leftover. Two acceptance criteria are **honestly unmet, not deferred**: the director's "below 3,000 lines" target (4,383 — the number was written against a 5,201-line file that Phases 2–4 grew to 6,225 before doctrine started leaving), and Phase 5's before/after parity check, which is **structurally impossible** because the acceptance grep requires `TickFOB` deleted in the same phase that authors its replacement. Also recorded: a narrow, named test-harness exception — the alphabetically **first** case of a suite can report a bogus `timeout` with `Output: <none>` at suite startup (seen twice, in two different suites, green on re-run). ⚠️ **Not yet human-verified**: no MP/host pass on the changed GM wire (`WIRE_VERSION` 2→3, `Rpc()` arity hand-counted because BUG-090 makes it a compile-check blind spot), no real battle fought to a result, no real forward base raised end to end, and **the modder exercise — the acceptance test for the whole design — has not been run**. A localization re-export (3 keys removed, 1 English body changed) and the wiki publication (draft at `wiki-draft.md`; wikijs MCP not attached) are owed. Everything uncommitted; user owns git.
- v1.35 (2026-08-21): **logistics/resources BUILT — buildings stop costing only money.** All 11 phases in one autorun plus a cross-phase review; 95/98 tasks. Overthrow's mid-game had no logistics: `BuildItem` took cash out of a wallet and spawned a garage in the same frame, and there was nothing a truck could do that a wallet could not do faster. This adds the second axis. A config-driven catalogue (`Configs/Resistance/resources.conf` — Timber / Cement / Steel / Hardware) is bought at the port from a new **Resources category** in *both* shipped Import and Export modes, at a **live, drifting, war-biased, persisted** price — genuinely new ground, since Overthrow has never stored a price at all (`SetPrice` was a plain write called once from `BuildResourceDatabase`). **Every truck in the game hauls**, through a **same-GUID delta of vanilla `Wheeled_Truck_Base.et` `{E03D5609EEA6E03D}`** whose parent line is proven byte-identical — the narrowest prefab reaching both vanilla truck families, so cars and APCs are excluded *by construction* rather than by a runtime capacity check. That settles the epic's biggest recorded open risk (D19; two alternatives rejected). Contents ride **one packed `RplProp` string**, and that single decision deletes the pull-on-open protocol, the async first frame, the listen-host re-entrancy trap and the server round trip for a requirements readout — the cargo HUD, the pile label, the map rows and the Build shortfall text are all **local reads**. Materials drop as **merging crate piles** that appear on every machine's map with **no registry invented** (`OVT_MapMarkerComponent` already self-registers runtime entities), and Guard Tower / Helipad / Garage / the new buildable **Warehouse** raise a **construction site** — money charged at placement (D2) — that Build finishes by draining nearby piles nearest-first on **squared** distances. The delicate part held: `BuildItem` splits into `BuildItem` / `FinishBuild(…, bool charge)` / `CompleteSite`, and the moved `:872-922` region — the one part of the build path with a recorded ordering-bug history — diffs to **exactly one delta**, the `if (charge)` wrapper, with the handler-failure path still returning before any charge. A buildable with an empty requirement list is therefore byte-identical to today, and the untouched majority of `buildables.conf` is the standing regression proof. **`storage` was never touched** — all seven walled files `git diff` clean, the epic's "two ledgers, deliberately" wall intact — and `OVT_EconomyManagerComponent` / `OVT_RealEstateManagerComponent` / every `core/damage` file likewise, so a **built** warehouse registers through the shipped `SetOwnerPersistentId` purchase path with **zero** real-estate lines changed. Four defects the plan never anticipated came out of the build. 🔴 A **money exploit**: nothing bounded a cart line's *quantity*, so 16 lines at `int.MAX` overflow both the litre and the money total **negative** — a negative money total passes `PlayerHasMoney()`, after which `TakePlayerMoney()` **pays the player**. 🔴 A **silent build failure**: `OVT_BuildContext.CanBuild` returns out of its base branch before the town branch runs, while the server applied the town gate to every town-buildable, so a Warehouse ordered at a resistance-held base inside an enemy town radius was offered by the client and refused by the server with **no notification, no money taken and nothing placed** — DoD F14 and F15 collide exactly there and the plan never settled precedence. The **drift readout described no drift** on any preset but Normal, dividing the difficulty-multiplied live price by the raw base so Hard and Insane pegged everything at "above base" forever. And `Consume` now **coalesces duplicate ids**, because a `.conf` typo listing one resource twice would otherwise pass the availability check against the full total and then half-drain — the one way all-or-nothing could break from data. Two incidental wins: persistence **binding 4** back-fills a latent `storage` gap (the Overthrow Buildable config carried no `OVT_StorageComponentSerializer`, so any *built* holder would silently have lost its item ledger on reload), and **BUG-194** was filed for a pre-existing shipped defect the localization audit turned up — `#OVT-MobileFOBDescription` exists in neither the `.st` master nor any export, so the Mobile FOB's description has been rendering as a raw key. The cross-phase review returned 17 findings (5 fixed, 12 logged), and the **final suite run caught one the review missed**: a stale Phase-8 assertion that exactly 3 buildables carry requirements, which Phase 9's Warehouse made 4. Gate: `compile-check` 0 (6252 files) · `check-input-conflicts.py` 0 at the shipped baseline · Logic **247/247** · Init **174/175** · PersistenceRoundTrip **40/40** · Campaign **18/18** · Persistence **13/13** — **492 cases, 50 new**, the single Init red pre-existing (`CompositionSlotGate_AcceptedTypesMatchTheCompositions`, owned by `occupying`/`virtualization`). Also confirmed: an Init timeout seen mid-run was **machine load, not code** — the same 173-case suite ran 69 s idle and 125 s with Workbench open, and the case's budget is 1200 *frames* against a 60 *second* gate. **Owed: three play-tests (SP mouse / gamepad-only / dedicated + JIP — MP is entirely unproven), the Workbench `.st` re-export (81 keys render raw until then), a re-import of the map-atlas `.edds` (the crate glyph is in the `.png` only, so the pile marker draws blank — the one hard blocker on F7), resource icons (an unbuilt §3.1 deliverable), and the wiki pass (blocked — no `wikijs` MCP server; a 7-item hand-off is in the feature's `context.md`).** Everything uncommitted; user owns git.
- v1.34 (2026-08-21): **logistics/storage PLAY-TESTED GREEN and CLOSED — nine defects out of the session, one of them a crash `main` still has.** User sign-off: *"everything looks great now"*. The fixes, in the order they were found: the destination picker drew vanilla `WLib_SpinBox`'s placeholder **`m_sLabel "SpinBox"`** verbatim (nobody had seen it because `ui`'s two consumers each offer one destination, so the picker had never been shown); a **gamepad focus trap** — with an empty cart nothing focusable sits above the picker, so d-pad up had no target and a pad player could not get back to the list; the **cart panel had no fixed height**, because `RightColumn` put an auto-height details panel above a `SizeMode Fill` cart, so every description length change resized the cart under the player; **Open Storage had no category tabs at all**, and since `RefreshHeader` hides the header on `showModes || showTabs` and storage declared one mode *and* one category, the entire top bar vanished — the plan's §3.8 specified `GetCategoryLabelKey → ""` and never followed the consequence through; **click/MenuSelect now adds 1** on a list row and **removes 1** on a cart line, wired at the row's single `m_OnClicked` invoker because mouse and pad both funnel through it; the **Add 1 button now advertises MenuSelect**, which required subscribing to `m_OnClicked` rather than `m_OnActivated` — `SCR_InputButtonComponent` registers a **global** action listener that does not gate on the button being focused, so the obvious version added **two** per press and no same-frame de-dupe was possible (the row's `Activate` is deferred a tick); 🔴 a **`NULL pointer to instance` crash opening the real-estate screen**, which is a **shipped-base defect, not this feature's**: `ItemPreviewManagerEntity.SetPreviewItemFromPrefab` renders an icon by **spawning a throwaway instance with no world**, so `OVT_Component.OnPostInit`'s `GetOwner().GetWorld().GetTimeAndWeatherManager()` null-dereferenced — it fires for any `OVT_Component` on a previewable prefab and Overthrow already puts them on `ShopHouse_*`, `FuelStation_*` and `Garage_*`; **`main` still carries it**; and finally the **100-per-item import cap**, whose BUG-033 rationale ("a single click could ask the server to spawn an unbounded number of prefabs") **died with the spawn loop** — raised to 10000 as a plain sanity bound, with `ValidateCart` now disabling Accept when the cart exceeds the destination's free space instead of letting the server clamp the purchase and charge for a partial delivery. Also merged `main` into `v1.5` as `9a26fae2` (interaction-menu-over-dialogs fix, vehicle-menu `MenuSelect`, a de_de string) — auto-merged clean, verified by counting rather than trusting: conf `ActionContext`/`Action`/`ActionRefs` 24/54/24 unchanged, `.st` 968 entries and balanced braces before and after. Final gate: `compile-check` 0 (6226 files) · conflict checker 0 plain and `--warnings` · Logic **217/217** · Init **162/163** · Campaign **16/16** · Persistence **13/13** · PersistenceRoundTrip **34/34**. ⚠️ Recorded while gating: **the suites are not deterministic under load** — a back-to-back five-suite sequence produced Init 6/163 in 102 s where an isolated re-run of the same commit gave the usual 1/163 in 54 s, and all five extra reds were world-time-dependent `Virtualization_*` / `VirtualMovement_*` cases. Run Init on its own. **Still owed: one string-table re-export, the wiki pass (no `wikijs` MCP server), and multiplayer — which is entirely unproven at runtime**, since no dedicated-server session was ever run. Everything uncommitted since `9a26fae2`; user owns git.
- v1.33 (2026-08-21): **logistics/storage BUILT — spawned-entity stockpiles are gone, replaced by one server-side item ledger.** All 10 phases in one autorun plus a cross-phase review and a B5 fix pass; 79/80 tasks. Overthrow stored a stockpile as **spawned entities** — a full ammo box was several hundred replicated entities the engine streamed, simulated and serialized, which is the direct cause of the network spikes, the kicked clients and the save-time frame collapse. The mod already knew the answer (the warehouse has been pure data forever) but that answer was welded into `OVT_RealEstateManagerComponent`, addressed by an **array index each client reconstructed independently**, broadcast to every client and JIP-streamed in full. This feature extracts it properly: a pure `OVT_StorageLedger` on an `OVT_StorageComponent` authored on exactly **three shared prefab bases** — `Wheeled_Base.et`, `OVT_AmmoBox_Base.et` and a new same-GUID delta of vanilla `Warehouse_01_Base.et` — so every wheeled vehicle spawned by *any* system, every ammo box and all the warehouse variants inherit one. Capacity resolves from data the economy already owns (truck unlimited, legal car 300, illegal/armed 0). **Contents never leave the server** except to the one player who opened a holder, pulled on open in a `Begin…Line…End` fan framed by a client sequence number; only **count + name + capacity** replicate, as three `RplProp`s that are free for JIP. Every holder is an `RplId` — **no `warehouseId` integer crosses any RPC anywhere**, verified by grep. One `OVT_StorageRequestComponent` on the controller carries **13 RPCs** (hand-audited 13/13 against their handlers, because `Rpc()` is untyped variadic and the compiler cannot) over a **per-player chunked job engine** with six ops and a real progress bar. Ordering is the point: capacity-check-**before**-delete on every sweep, spawn-**then**-debit on materialisation, and a `TO_HOLDER` remainder returned to the source — so the worst outcome of a mid-transfer crash is **one item, never a stack**. Player-visible: an **Open Storage** screen (a `logistics/ui` consumer on the closed eight-hook surface — the base is byte-identical, I1 held) with a destination picker that is the mod's **first** with ≥2 entries; five new actions (`Storage (N)`, Transfer all, Rename, officer-only Clear, plus rewritten Load/Unload); **port import with zero spawns**, charging only for what fitted; **port Export**, priced below every shop buy price so there is no shop→port loop; and vanilla's supply actions hidden globally. Deleted, not adapted: `OVT_WarehouseData.inventory` and `.isLinked`, ten warehouse methods, four warehouse RPCs, and `OVT_WarehouseContext` — ~570 lines out of two managers and the container-transfer component, with `OVT_RealEstateManagerSerializer` bumped to v2 carrying a **live one-shot migration** so an existing campaign's warehouse stock lands in the building's component. Five defects the plan never anticipated were caught while building: `TryDeleteItem` **cascades into a container's contents**, so the sweep as planned would have deleted a full backpack and credited one line; the shipped loot filter compared class-name **strings** and two of its eight names *are not vanilla classes at all* (so vests, backpacks and helmets are now looted as items); EXPORT's port and illegal gates had to be pulled forward out of the UI phase because `opKind` comes from the client and a screen-only gate was a **one-packet money faucet**; `DeployFOB` moved only the vanilla inventory, so undeploy→deploy would have **destroyed the entire stockpile**; and — the review's headline — on a listen host a **refused** order reported *"Accepted"*, because `Accept()`'s own `Refresh()` wiped the refusal and posted the success line over it. The review also found B5 unmet for the six action-driven verbs (press *Transfer all* twice and the second was refused **silently**) and routed `SEQ_NONE` refusals to the hint channel. Gates: `compile-check` 0 (6225 files) · `check-input-conflicts.py` 0 at the shipped baseline plain and `--warnings` · `OVT_TEST_LogicSuite` **217/217** · `OVT_TEST_InitSuite` **162/163** · `OVT_TEST_CampaignSuite` **16/16** · `OVT_TEST_PersistenceSuite` **13/13** · `OVT_TEST_PersistenceRoundTripSuite` **34/34**. The single Init red, `CompositionSlotGate_AcceptedTypesMatchTheCompositions`, is **pre-existing** and belongs to `occupying`/`virtualization` — nothing in this feature touches deployment configs. Also established: the Fast/All **group** hang recorded after `logistics/ui` does **not** reproduce when suites are launched individually by class name, which is now the working gate. Owed: the Workbench string-table re-export (~43 new keys render raw until then), two Workbench prefab checks, three play-test sessions (SP mouse, gamepad-only, dedicated + JIP), and the wiki pass (**blocked** — no `wikijs` MCP server attached; a 7-item hand-off is in the feature's `context.md`). Everything uncommitted; user owns git.
- v1.32 (2026-08-21): **logistics/ui BUILT — one transfer screen replaces two, and both old screens are deleted.** All 6 phases in one autorun plus a cross-phase review fix pass; 43/44 tasks. `OVT_TransferContext` is an abstract **List + Cart + Destination** screen over two pure, Logic-tier-tested models (`OVT_TransferListModel`, `OVT_TransferCartModel` in `Scripts/Game/Data/`) and a new `OVT_TabHostContext` intermediate class that lets the shop's tab component be reused by a second host for an 18-line shop diff. A consumer overrides **eight** hooks and writes no widget code; the hook list is deliberately **closed**. `OVT_PortContext` (284 L → 154 L) and `OVT_WarehouseContext` (365 L → 229 L) are now thin subclasses sharing **one** layout and **one** `ActionContext`, mechanically pinned by a new Init case. Deleted outright: `PortMenu.layout` + `WarehouseMenu.layout` + their row layouts, `OVT_PortItemComponent` + `OVT_WarehouseInventoryItemComponent`, six input actions and two `ActionContext`s — which retires the pre-existing pair of **duplicated `InputSource` GUIDs** the two screens shared. Player-visible: a multi-line cart with Add/Remove 1/10/All and a running total committed by one Accept (was eleven presses of "Buy 10"), the shop's category tabs and steppers over a 200-row import list, a destination picker, port-only affordability refusal and — closing two silent returns — a visible "you are not in a vehicle" message. **No server change, no new RPC:** Accept loops the existing `ImportToVehicle` / `TakeFromWarehouseToVehicle` per line; a batched checkout is `storage`'s. The cross-phase review found **2 gamepad blockers** — `Refresh()` decided whether to restore focus *before* the rebuild, so the screen opened with nothing focused (focus was still on the vehicle-menu button about to be destroyed) and a pad Accept stranded focus on the button Accept then disabled — plus five should-fixes: `array.Remove()` is **swap-with-last** (`Types.c:258-263`), so all four cart drop sites became `RemoveOrdered` or a removed middle line teleported the last line under the player's cart index; `Reconcile` now re-copies unit value and display name (a tab change re-priced the model but not the cart, so the affordability gate ran stale); category tabs sort ascending instead of first-seen; `Accept` re-resolves the destination (a deleted `IEntity` does not null its handle); and a zero-cap entry can no longer insert a phantom "x0" line. Gates: `compile-check` 0, `check-input-conflicts.py` 0 at the `0/0/3/0/1` baseline plain and `--warnings`, `OVT_TEST_LogicSuite` **203/203**, the new Init case green — both new cases **fail-proven** by observed mutation. ⚠️ The Phase-5 Fast-group run **hung** on `OVT_TEST_Init_VirtualMovement_StationaryPlanIsNeverAdvanced` (unrelated to this feature; `crash.log` also carries a pre-existing `"Entity is already deleted"` VM exception in `OVT_DeploymentComponent.DestroyDeployment` from the ObjectiveDirector teardown), so the group has **no verdict** and targeted runs stand in — a clean Fast run is owed. **Play-test came back GREEN the same day** at both consumers, with three polish items applied after it (single empty-cart message, a background on the cart panel, and a real Accept binding on `KC_F`/`left_trigger` that supersedes D5). Still owed: a re-check of those three, the `.st` re-export, and the wiki pass (**blocked** — the `wikijs` MCP server is not attached). Everything uncommitted; user owns git.

- v1.31 (2026-08-20): **core/damage COMPLETE — play-test green** - all 10 gated MP/Workbench checks confirmed; four same-evening fixes (SetDamagePhase FX funnel so GM Neutralize/weapon kills raise effects + disable stations, vehicle-style smoke plume + ground fire + visible flame, TNT_Large/Fuel_Large explosion sound, sound bank signals); final gates Fast 346/347 / All 405/406 (pre-existing red only); core epic 213/213.
- v1.30 (2026-08-20): **core/damage built — Damage & Destruction** - structures ruin instead of vanishing (explosion/smoke/sound, ruin mesh, inert while ruined, gear survives), 20 s priced repair action difficulty-scaled via repairCostMultiplier, occupying repair detail, admin commands, persistence v2; 8 phases, 60/70 tasks (10 user-gated MP/Workbench checks owed), Fast 346/347 / All 398/399; BUG-193 filed.
- v1.29 (2026-08-20): **occupying — `counter-attacks` CLOSED (137/137) and promoted to proof of concept for the new `objectives` feature (requirements written); `base-upgrades` and `qrf` closed (retired / legacy); open-bug sweep.** The play-tested director stays as-is until `objectives` rebuilds it as a modular, declarative, config-driven brain in the deployment framework's shape — an `OVT_ObjectiveRegistry` of plans, each an ordered list of named phases, each a bag of condition / operation / abort modules authored in Workbench `.conf`; the director shrinks to a runner (tick, reserve floor, anchor, blacklist, idle clock, persistence, read API). Six design decisions settled with the author: phases over a behaviour tree, plan-selects-target, assets as operation modules (FOB now, checkpoints next), difficulty `-1` fallback, N-designed/1-shipped, full ownership transfer with `.conf`-only authoring. Sweep: BUG-109 closed as obsolete (specops dispatch deleted; residual camp-raid gap handed to `objectives`), BUG-186 scoped down (its counter-attack gate no longer exists).
- v1.28 (2026-08-18): **economy/fuel BUILT — all 5 phases in one autorun (27/28 tasks; T5.2 wiki sync auth-blocked); Ready for Review.** Money wired into the vanilla fuel stack, nothing parallel built: a `modded SCR_FuelSupportStationComponent` charges the performing player **per litre actually delivered** inside `OnFuelAddedToVehicleServer` (charge-before-`super`, so the null-node backup-flow path still charges), gated through the vanilla `IsValid` seam (`OVT_CANNOT_AFFORD_FUEL = 550` → localized greyed-out reason) with the client answering from its own replicated money — **zero new RPCs** (grep-proven). Per-litre pricing (user decision D1, superseding requirements' per-time fee) rides a new difficulty knob `fuelPricePerLitre` (Easy 0.5 → Insane 4, 0 = kill switch) replicated via **`CONFIG_STREAM_VERSION` 3→4**. Fractional ticks accrue in a deliberately never-settled `OVT_FuelChargeLedger` (max $0.99 float per player per pump); the one-shot "can no longer afford fuel" toast fires on the ordinary run-dry path via the same `GetTickCost` basis as the gate (orchestrator amendment — spec'd trigger was a race-only safety net that made F4 unobservable), and its wording deliberately avoids "ran out of money" because the gate strands ~one tick's worth (R10). Free paths preserved structurally, not by whitelist: vehicle-mounted stations, no-range jerrycans, and the `OVT_FuelSourceComponent` marker. New buildable **Fuel Depot** (`OVT_FuelDepot.et`, 17 hand-minted GUIDs, derives the FIA mobile fuel tank): base-only ($2000, handler backstops the client-side location gate), 5000 L `SCR_FuelNode` starting **empty**, filled by parking a fuel truck alongside and holding the depot's own Refuel action, dispenses free, level persists via `SCR_FuelManagerComponentSerializer` added to the buildables persistence group. `OVT_FuelUtils` + a snapshot-copying `OVT_GetSupportStationsOfType` registry accessor give `resistance/high-command` its fuel-source discovery API (the vanilla accessor **aliases** its internal array — trap documented). Rider fix: `OVT_SkillManagerComponent.GiveXP/TakeXP` null-deref that aborted every server-initiated `BuildItem(-1)` before persistence tracking. Coverage: +5 Logic cases + 1 depot persistence case (full reload round-trip proven **structurally impossible** in the harness — `ReapplyLatestSaveData` only re-applies the game-mode record — so the case asserts prefab facts + save, reload half owed to play-test F18). Field Manual page "Fuel" + `fuel-depot-built` tutorial popup, every claim code-cited. Gates: compile 0 throughout; All **284/284** (Phase 2), All **285/285** (Phase 3), Fast **234/234** (Phase 4); one Phase-1 red was the documented `Setup_Checkpoint` 500 ms I/O flake, green on targeted re-run. **Same-day play-test riders (A1+A2, 34/35):** depot model re-based onto the industrial `FuelTank_02_green` (the `_Pump_` variant was rejected — it embeds a vanilla 50,000 L PAID pump at VERY_HIGH priority that would shadow the free depot; depot is now destructible, ruin swap loses stored fuel); a fast **Fill** hold-action (~5 s radial, real clamped cost in the label, hidden with no source in range) on every vehicle's fuel cap, the truck cargo nozzle AND the depot, server-authoritative via new `OVT_FuelRequestComponent` (`RplId + fuelTankId` wire format — cargo tanks are slotted children with no RplComponent, and the naive `FindComponent` lookup finds only the ~40 L engine tank); fill **scoped to the tank you're holding** (vanilla's own owner-entity rule — user caught the fuel-cap Fill draining a 5000 L depot to 21% by silently filling the cargo tank too); depot capacity **10,000 L** (two truck cargo loads; Ural 360 L chassis / 4,500 cargo, M923 306/4,550); free-before-paid source ranking (D13). Rider save: a Workbench `.st` re-save fixed the file's old unbalanced brace but silently deleted the two keys nested inside it (`OVT-StartUprising`, `OVT-SupportTooLow`) — restored; an unbalanced `.st` brace is data loss on the next save, not cosmetics. **CLOSED 2026-08-18: user confirmed every play-test item green (A–H + the Fill-action step I list incl. MP/JIP and the depot→truck→depot round trip), re-exported `en-us.conf`, hand-tuned the depot action point in Workbench, and committed (`a765bd12`); A3 rider added vanilla Check Fuel to the depot.** Only open item: wiki sync (wikijs auth). Separately noted: a pre-existing tutorial-settings test race (`SettingsStoreRoundTrips` hangs when the prior case's settings transaction is still in flight — surfaced under Workbench/OneDrive I/O contention). Everything uncommitted; user owns git.
- v1.27 (2026-08-17): **virtualization/movement BUILT + PLAY-TESTED — all 4 phases in one autorun, then 3 same-day play-test fixes; 22/22 tasks, user confirmed all §6 items green.** The epic's missing tick: `OVT_VirtualMovementManagerComponent` on the game-mode prefab walks every **dormant** registered infantry group along its own waypoint plan — straight lines, fixed configurable speed (default 1.5 m/s), one 2 s `CallLater` with an 8-handle round-robin slice and **per-group dt** so effective speed is registry-size-independent (D5). `IsSpawned()` is the whole gate (vehicle groups never move virtually — huge `spawnDistanceOverride` keeps them live, D2); **the plan is the opt-in** (empty/DEFEND-only = stationary, D10); progress is **transient and re-derived** by projecting position onto the plan polyline — no serializer, no payload, no conf entry (D3); every write is ground-snapped and water-vetoed (accumulator advances, origin holds at the shore, D6); waypoint entities never touched (D11 — the resume-from-index-0 handoff artefact F-B is documented, not engineered around). Frozen core extended by **exactly one** method (`GetAllHandles()`, +23/-0, api.md §3/§10 + dated context note); the shared persistence fixture was made stationary **before** the tick existed (F-A: 10 `RegisterGroup(` test sites swept with verdicts) and gained the epic-level claim that a `SetPosition`d group round-trips at its **moved** position. Coverage: 7 Logic cases (projection/classification/stepping/sort), 4 Init cases (seam, tick-advances, DEFEND-never-moves, no-leak) — one real gate red fixed (Init worlds never run `PostGameStart`, so tick-dependent cases now install the tick themselves — a lesson for any future tick assertion) plus one identified vanilla `Setup_Checkpoint` 500 ms I/O flake. Gates: compile 0 (6128 files), Fast 186→187→**190**, All **233/233**. The `integration` seam (auto-adoption: register at any position, next pass adopts; D9) is live and documented in movement's `context.md` "For integration". Play-test round (same day): user confirmed the Workbench prefab and then surfaced 2 defects, whose fixes exposed a third — (1) waypoint entities spawned at raw plan Y (buried/floating; now surface-snapped at entity spawn, plan payload untouched), (2) despawn mid-return-leg resumed the WRONG direction on coincident-leg routes (core additive #3 `GetCurrentPlanIndex(handle)` matches the live current waypoint; projection stays the load/teleport fallback), and (3) **`spawnDistanceOverride = 0`/Manual groups materialised anyway** — vanilla's `m_bSpawnImmediately` init request is dispatch-checked against OBSERVERS only (GM/autotest cameras qualify), so the modded `SCR_AIGroup` now refuses unarmed dispatches for masked Manual groups (`ArmOVTManualSpawn` via `ForceSpawn`). Final gates: Fast **190/190**, All **236/236**. Process note: per user instruction, dev-phase defects are no longer filed as bugs (IDs collide with main) — they live in the feature docs. Everything uncommitted; user owns git.
- v1.26 (2026-08-17): **virtualization/civilians BUILT — all 6 phases + 2 mid-run user amendments in one autorun (39/39 tasks); Ready for Review.** Town civilians retired from `OVT_TownControllerComponent` (net −56 lines; the 10 s `CallLater`, single-frame crowd bursts, waypoint/character leaks and global QRF despawn all gone) and rebuilt as declarative content on core's ambient spawn-source seam: two additive core hooks (`IsEntityDead`/`OnEntityPruned` — core's only change, I1 held), `Configs/Civilians/CivilianAmbience.conf` (registry: `town_civilians` + `town_vehicles`), per-town runtime source configs, `OVT_CivilianAmbienceManagerComponent`, live-population density maths (Logic-tier tested) with **3 new server keys** (`civilianDensityMultiplier`, `maxCiviliansPerTown`, `despawnCiviliansDuringQRF`). Three shipped defects fixed en route: despawn leaked every civilian body (**BUG-179**), the civilian wanted-disable was a silent no-op (**BUG-180**), and `SpawnWaitWaypoint` dropped its time (PERIMETER patrols now really pause 45–75 s). **User amendments mid-run:** QRF civilian despawn became opt-in (default: crowds stay — players recruit them to fight the QRF), and the 5 per-type prefab pairs were dropped as redundant (all types spawn `Group_CIV.et`; looks come from 5 per-type clothing confs; 6 types curated per-town via `m_aCivilianTypes` on 13 Eden towns). Parked-vehicle ambience: kerb-first placement (road-derived fallback), `UntrackTransient` at spawn keeps ambient cars out of saves, claim-on-entry re-tracks + hands to the vehicle manager. Wiki (`overthrow-config`, `customizing-overthrow`) + Field Manual synced (3 new localization keys need a Workbench re-export). Gates: compile 0 (6124 files), suite runs Fast 171 → All 221/222/224/225 → Fast 179, all green (one 300 s no-verdict timeout traced to concurrent docs-agent conf writes during client load — lesson recorded). Owed (civilians `context.md`): Workbench prefab/conf opens, §6 play-test list (crowd feel, leaks, interactions, QRF locality, density knobs, vehicle claim). Everything uncommitted; user owns git.
- v1.25 (2026-08-17): **virtualization/core BUILT — all 6 phases in one autorun (50/50 tasks); the epic's foundational registry layer is Ready for Review and its consumer contract (`api.md`) is FROZEN.** Phase 1's engine-adoption spike (user play-test + engine-source forensics) reshaped the plan with hard evidence: the 1.8 dormancy ratchet **corrupts survivor counts on any mid-refill despawn** (watched live: a 6-man squad "died" to 2 with zero kills — the slot-mask design went from nice-to-have to mandatory), observers are **not just players** (deploy-point preload observers, cameras, optics far-observers), `SetEliminateWhenReached` + the 150 m very-near block can **delete a dormant group outright** (now only stamped post-wipe), and vanilla group persistence is **unusable for runtime groups** (BUG-118 untrack + `SelfSpawn 0`; class-wide opt-in would duplicate lazily-registered orphans) → **Route B ruled**: `OVT_VirtualizationManagerSerializer` persists full re-creation state (composition, live position, waypoint plan, slot mask; frozen append-only payload) and the manager rebuilds group entities synchronously on load. Shipped: `OVT_VirtualizationManagerComponent` (registry/API/reclaim/wipe bookkeeping), mask-driven `ExpandOneMember`/`IsExpandComplete`/`SpawnMembers` overrides + post-despawn count re-assertion on the modded `SCR_AIGroup`, the declarative **ambient spawn-source seam** (round-robin frame-spread tick over `ObserversSystem`, ownership-transfer release), scale affordances, and 16 new proven-can-fail cases across Logic/Init/Persistence. Two real bugs caught by the gates: an All-group **client freeze** traced to a null-entity `InsertObserverSP` in a test (zero vanilla precedent — candidate upstream report) and Phase 6's restart audit fixing 4 latent teardown bugs (stale `s_Instance`, cross-campaign double ambient tick, restore-latch re-fire, kill-hook mis-resolution). Gates: compile 0 (6117 files), Fast 163, **All 210/210**; consumers untouched (I1), one persistence-conf entry (I4). Owed (core `context.md`): Workbench prefab check, §6 play-tests (highest value: quit→Continue slot accuracy; 40-group fast-travel probe), fail-proof red executions. `civilians` + `movement` are now unblocked.
- v1.24 (2026-08-16): **gm epic Phase 1 verification fully discharged — all four built features closed at 100% (113/113 tasks; only gm-map remains).** waypoint-viz user-verified (Workbench render rounds with two live-feedback fixes, dedicated-server GM client, non-admin negative path; found vanilla **BUG-175** — assigning a GM waypoint to a cycled group crashes with recursive Invoke, fix parked with epic Phase 3). gm-state (28/28) and overthrow-panel (26/26) closed: the user confirmed their owed MP checklists (record counts, negative path, JIP, stale-discard) were all exercised in the one batched epic test session (individual log measurements not recorded separately). hud-icons post-ship tweak: `m_fMaxDrawDistance` lowered **20000 → 2000** on all five prefabs after user feedback — whole-map icons made the GM view too busy; 2000 matches the vanilla GROUP/VEHICLE fade (plan F-2 revised; user-verified same day, all tests green).
- v1.23 (2026-08-15): **gm/waypoint-viz BUILT code-complete in one autorun (Phases 0–3+5; 27/29) — read-only 3D waypoint routes for a selected AI group in the GM editor.** New `GROUP_WAYPOINTS` fan on the gm-state seam (purely additive, 384 insertions, arities hand-checked), server-side `AIWaypointCycle` flattening walk (bounded, leak-immune, strictly read-only), and a widget-free Shape-API renderer (amber route, cyan current leg, loop closed wp[n−1]→wp[0]) hosted from the modded EDIT-mode root. All group 190/190 green ×2 (+1 run green-but-wedged at shutdown); 2 new proven-can-fail cases. No GUIDs, no prefabs, no layouts, no base-game forks. User's Workbench/MP verification pass deferred; upstream `occupying` findings (SpawnWaitWaypoint drops `time`; SpawnGetInWaypoint spawns GetOut) recorded in the epic. gm epic now 4/5 — only `gm-map` remains in Phase 1.
- v1.22 (2026-08-15): **gm/hud-icons BUILT + user-verified same day — the gm epic's riskiest base-game integration, Phases 0–4+6 in one autorun, descoped to tooltips-only on user feedback, then verified green (30/30; MP/JIP + both big tower variants confirmed by the user).** *(Late edit: the click-detail surface described below was subsequently removed at the user's direction — it stretched the panel; per-entity info is hover-tooltip-only, base tooltip = "N Resources - N Garrison Groups", and group/player readouts have no surface until epic Phase 2 popups.)* The GM view now shows the campaign: faction-tinted icons over every town, base and radio tower (SYSTEM-typed `SCR_EditableSystemComponent` subclass on Overthrow's own prefabs, 20 km draw distance, non-deletable + non-movable), click detail for towns/bases/towers/AI-groups/players rendered into overthrow-panel's detail seam with the honest local-vs-seam split, and live hover tooltips via a per-instance `GetDescription()` — zero base-game forks, zero config deltas, zero new networking (grep-proven). Headline finding: **the plan's tower-LOCAL flag was wrong** — Overthrow's tower prefabs are same-GUID deltas inheriting vanilla `RplComponent`, so LOCAL would have silently nulled every tower icon; all five prefabs ship flags 2052 (verified against the vanilla tree, plan annotated). Gates: compile 0, Fast **145/145**; six new cases (5 Init incl. a HasDescription-trap guard, 1 Logic) each proven able to fail via real red runs with named failures; a transient Fast-group client shutdown hang (2×, all tests green in logs) did not recur and was closed as environmental. Owed: §6 verification — Workbench visual pass, `_base`/`_medium_base` tower variants by eye, MP/JIP, auth-path record.
- v1.21 (2026-08-19): **resistance/sleep BUILT — six phases plus a review-fix pass (32/33) in one autorun.** A single-player "Sleep" action on the seven vanilla bed prefabs and on a new **Cot** placeable skips **8 in-game hours**, and the whole feature is about what does *not* get lost to the skip: income/taxes/donations per 6-hour boundary, NPC shop buying, the 07:00 restock, midnight rent, occupying-faction resource gain/spend and 15-minute threat decay are **replayed through the same protected methods the live tick calls**, so there is exactly one implementation of each payout and a future change to `CalculateIncome` changes the skip with it for free. The skipped window is **half-open at the start and closed at the end** — the economy flushes `CheckUpdate()` once before replaying and asserts all three hour latches to the landing hour afterwards, which is what makes double-pay structurally impossible rather than approximately unlikely. The occupying-faction replay is **chronological**, 15 minutes at a time, because resource gain scales with a threat value that decays between gains; the counter-attack roll and the town-uprising scan are deliberately excluded and the exclusion is written into the method so nobody "fixes" it. Gated to an owned house / own camp / deployed FOB / captured base (the placement system's own ordered test, constants attributed to their source rather than silently copied), disabled-with-reason during a QRF or while wanted, never shown outside single player, with a **12 in-game-hour cooldown** stored as an absolute game-clock stamp so it is correct at both 6× and 12× acceleration and survives a Continue for free (serializer **v5**, append-only, `< 5` clear on read). **BUG-183 rode along and is fixed:** the economy's three hour latches were never persisted and re-initialised to `-1`, so loading a save taken during an accounting hour paid the income sweep **a second time** — a repeatable money exploit in shipped builds — closed by the same "assert the latches to the current hour" operation the time skip needed anyway, at zero serializer cost, and it fixes the live re-apply path too. **BUG-186 filed, not fixed:** both `CheckUpdate` timers schedule at `FREQUENCY / GetDayTimeMultiplier()` — the **day** multiplier only — while the engine swaps in a night acceleration at dusk, so at night a tick strides ~2 in-game minutes and the occupying faction's minute-exact gates are skipped entirely (the economy is immune because it latches on the hour). **The cross-phase review is what this run turned on.** Five agents each held their own contract faithfully, and the one real defect was invisible from inside any single phase: the plan had given the *economy* replay two edge defences and the *occupying* replay **neither**, while the schedule class's own header justified the closed end by appealing to a latch only one of the two managers actually had. Traced by hand: sleeping from **10:00** paid the reserve **three** times instead of two (~85% of the time, since the resumed tick re-observes minute 0 at the landing hour) and sleeping from **12:00 exactly** *lost* that payday ~85% of the time — and both are the exact runs the play-test checklist prescribes, so F7/Q1/Q2 would have failed on the tester's first attempt with no way to tell which half was at fault. Fixed with two live-tick latches mirroring the economy's own shipped `m_iHourPaidIncome` idiom, a start-boundary flush and a landing-instant assertion, plus a third pure extraction so the flush is not a second copy of the payload. **The user also rejected D16 on reading the behaviour** — the cooldown was measured from lying down, so the 8-hour skip consumed 8 of the 12 and the player woke with 4 hours left; **D16a** stamps the wake instant instead, giving a full 12 hours on waking and 20 door to door, chased through the Logic case, the plan, the Field Manual sentence and all seven localization exports. Two risks were **retired before their phase ran** by recovering all seven bed resource GUIDs from inbound references up front (R4) and confirming beds in houses are prefab *instances*, not baked children (R5). Help synced: a nine-piece "Sleeping" Field Manual entry and a cot paragraph, every sentence carrying a `file:line` citation in its string `Comment` — and **one drafted claim was cut** for asserting that wanted level decays on its own, which this feature's code only ever *reads*. Gates: `compile-check.sh` exit 0 after every phase; the suite gate was **deferred** through the whole build (the permission classifier denied the Reforger client launch) and then discharged on approval — All group **177/177** over the as-built tree, **179/179** over the post-review tree, both exit 0, zero failures, with every case this feature added confirmed present in `junit.xml` rather than assumed. **Closed out the same day: the user's full 26-item play-test passed — every item, including the R5 gate, the Q1/Q2 payout-counting runs from 10:00 and 12:00, the midnight date wrap, the pad-only pass and the multiplayer F11 check — and the Workbench localization re-export landed, so BUG-183 is CLOSED on its own three acceptance checks.** The single outstanding item is **T6.3, the public wiki sync**: genuinely undone, because no wikijs MCP server was attached to the build session and faking it was the alternative.
- v1.20 (2026-08-15): **recruit-ux Phase 9 built same-session (73/73) — buy equipped recruits at the tent — plus two play-test rider fixes.** A new held action on the recruitment tent opens the loadouts screen in **purchase mode**: pick one of your own saved loadouts, get a **server-computed quote** (each item's live local shop price — town stock and trader perk included — × a difficulty-carried convenience fee, default 1.5, plus the normal tent recruit cost and a town supporter), and buy; the recruit spawns **beside the tent** wearing the kit. The money path is fully server-authoritative: quote and charge come from one routine, an unpriceable item **refuses the purchase by name** (the economy's `GetPrice` returns 500 for unknown ids and `GetInventoryId` maps unregistered prefabs onto *some other item's* price, so `IsRegisteredResource` is the only safe gate), a kit that lands nothing is never charged the gear fee, and **nothing new enters the config JIP bitstream** — loadout items don't replicate to clients, so clients display prices the server hands them and `CONFIG_STREAM_VERSION` stays at 2. The legacy tent-recruit RPC now **delegates into the same validate/spawn internals** (placement hardened: tent-forward anchor, ground clamp — `FindSafeSpawnPosition` computes a ground height it never uses — and `skipSpawnPointSearch=true`, without which the 15 m spawn-point shortcut ignores the collision box). The user's "recruits spawn ~100 m away" report was **investigated and reclassified**: spawn is provably within ~23 m; the recruit *walks* — `Idle_ReturnToFormation` anchors on the slave group's index-0 agent, uncapped, a vanilla-documented regroup quirk (bug to file, with a one-minute falsifiable check). Play-test riders, same session: **BUG-170** — parked recruits barked/glitched in a defend-waypoint re-order loop; fixed by swapping to a plain **wait waypoint** (86400 s, effectively infinite for session-scoped groups; town patrols prove waiting AI still returns fire) — and **hold durations shortened** (park 3→1.5 s, recall 2→1 s, swap 5→2.5 s). Discrepancy caught between agent reports and settled on-disk: difficulty presets carry **scaled** fee multipliers (Easy 1.3 → Insane 1.7), not a flat 1.5 — kept, surfaced to the user to flatten if unwanted. Help synced with a full citation ledger (tutorial tip on `PLAYER_BUILD "Recruitment Tent"` — the one unused per-player trigger that fires at the honest moment; wiki pages 29/30 updated, defend-wording and stale durations corrected everywhere). Gates: compile 0 throughout; **final All group 165/165 green**. Owed: Workbench re-export (Phase 9's ~17 new string ids), the pad/MP play-test checklists, and the LT-vs-ADS binding check on the non-exclusive loadouts context.
- v1.19 (2026-08-14): **resistance/recruit-ux BUILT — all 8 phases (57/57 tasks) in one autorun.** Recruits gain the missing half of squad management. **Inactive ("holding") recruits:** a 3 s held action (or roster button) parks a recruit where it stands — out of the group, still owned, still counted against the 16 cap, still persisted (serializer v3, backward-compatible with v1/v2 saves) — defending its position in an Overthrow-owned, non-playable, delete-when-empty AI group; parked recruits within 50 m cluster into one group, and a 2 s action or the roster recalls them. Parking a recruit in a vehicle is refused client- and server-side. Fast travel and group-sharing now move **active recruits only** — including the fare preview, whose record-side count was drifting from the server's entity-side gather until Phase 2 caught it. **Recruit map layer:** one marker per owned recruit (dimmed when holding, live per-frame dim with the map open), an ammo-status badge (armed+ammo / armed+dry / none), a filter row with per-profile persistence, and last-known-position fallback for unstreamed bodies fed by a 10 s server-side status sweep pushed owner-only (inactive recruits are by definition unstreamed, so client-side reads were structurally wrong). **Sectioned roster:** Active/Holding sections with the sibling-walk selection model replaced by a flat entry array (gamepad walks the section boundary with no special cases), `X / 16` capacity header, per-row armed/ammo/wounded icons, and a park/recall toggle on **G / Left Trigger** — the plan's `shoulder_left` was VON-reserved and the conflict checker caught it. **Loadout swap:** a 5 s held action exchanges the player's entire kit with an active recruit's by **moving real item entities** — the Q9 grep proves no spawn/delete identifier exists in the routine, so duplication and loss are impossible by construction; pair-swaps first, rollback journal, world-drop as last resort, every failure logged with the prefab name. The phase's load-bearing discovery: the engine **forbids one character's storage manager removing items from a living character** (`ShouldForbidRemoveByInstigator`), so removals are instigated by the storage's own manager — the naive one-manager design would have silently no-opped on every move. Rode along: **BUG-107 fixed** (recruit XP now checks the configured occupying faction, closed in tracker) and the `FindRecruitEntity` mid-iteration map mutation. Filed: **BUG-166** (`RpcAsk_DismissRecruit` validates nothing — any client can dismiss anyone's recruit) and **BUG-167** (join-group dialog counts parked recruits the transfer will exclude). Help synced: 2 tutorial popups, 3 Field Manual sections + 1 corrected claim (two pre-existing unqualified "recruits follow you" sentences were made false by this feature and narrowed), wiki pages 29/55/30 updated with a full `file:line` citation ledger. Gates: All 143 (P1) → All 148 (P2) → Fast 108 (P3) → **final deferred All-group run 159/159 green** (fired automatically once the user's game session ended — every gate this feature owns is green). Owed: a Workbench export of 24 new string ids, and the play-test checklists (roster pad-nav, map layer, swap item-count invariant, MP/JIP, save/continue).
- v1.18 (2026-08-12): **map/influence-overlay BUILT END TO END — every build phase in one autorun (52/95), and the campaign's spatial influence rules now have exactly ONE implementation.** The map could show *that* a town had a `Nearby Radio Tower` modifier but never **which** tower, how far its reach went, or that a friendly tower was being out-shouted by an enemy one. Selecting a location now draws dashed, faction-coloured lines to every location it is in an influence relationship with, plus a stroke-outline reach ring for a selected tower or base. **The feature's real deliverable is the extraction, not the render:** `OVT_InfluenceRules` (pure, world-free) was pulled *out of* the code that already applied these rules, and `OVT_TownManagerComponent.CheckUpdateModifiers` — the server's 10-second campaign tick governing four permanent modifiers — plus `UpdateAllTownMomentum` were rewritten against it **behaviour-preservingly** (diff-verified: same branch structure, same operators, the tower branch's `else` intact and the base branch's deliberate absence of one preserved). **K2's cross-check is what makes client-side re-derivation safe**: an edge draws ACTIVE **iff** the geometry qualifies **and** the modifier is genuinely present in the target town's replicated list — so the overlay cannot assert an influence the campaign has stopped applying, whatever the reason, and a sabotaged tower renders dim from the general rule with **no special case**. One wire change in the whole feature: two floats appended to the existing config JIP bitstream with `CONFIG_STREAM_VERSION` bumped 1→2 (26 writes, 26 reads, verified slot by slot). **Two engine facts were measured rather than assumed, and both changed the work**: `vector.Distance` is **not** a correctly-rounded square root (exact at 1500 m, +1 ULP at 1000 m and 2000 m), which made one planned boundary case toothless and another unreachable through world geometry; and `ScriptBitWriter`/`ScriptBitReader` **cannot be round-tripped from script** — `new` returns non-null and the first proto call hard-crashes the process — so the JIP append has **no automated coverage and cannot have any**. Also promotes `GetFactionArgbByIndex` onto `OVT_MapLocationType`, collapsing two byte-identical bodies so all three canvas layers share one colour implementation; adds **zero** rows to the `OVT_MapCanvasLayer` contract (the second consumer in a row to leave it untouched); and ships a Field Manual section plus four wiki pages. Gates: compile **0 / 5998 files**, Fast **99**, All **137** (+10 Logic cases, **12 inversions run, every case observed red**). ✅ **Play-tested green (SP + MP) and closed by the user the same day** — and **the Phase 5 tunable defaults shipped unchanged**, so the legibility retune the plan budgeted for (and that `territory-overlay` needed after each of its two renders) was not needed once. A late `m_eRingMode` attribute (DASHED / SOLID / NONE) was added at the user's request — SOLID is **one** draw command via `m_bShouldEnclose` and is the cheap degradation, while the ring was never the expensive part (a flat 36 commands against up to 32 **per edge**). ⚠️ **Q-1 closed on one of its four numbers** — `Draw()` ~1 ms against a stated ≤ 0.5 ms budget, judged fine by the user but not the measurement the criterion asked for — so **the epic still has no recorded canvas frame-cost baseline**, the same gap `territory-overlay` left.
- v1.21 (2026-08-15): **gm/overthrow-panel BUILT + user-verified same day (25/26) — the gm epic's first visible surface, and the seam's first real readout.** A GM in EDIT mode now sees the campaign's hidden numbers on a chrome-matched panel (top-left at exactly 33,250 — repositioned live during the user's Workbench check because the base game's contextual help overlaps the bottom-left stack the plan originally targeted): threat at float precision, both OF pools, next-distribution/payout amounts with once-per-second local countdowns that re-sync on each ~8 s poll, **suppression reasons in accent colour** (QRF running / no players — a countdown that will not fire says so), live resistance funds (invoker-driven, not polled) and the QRF row — with "Waiting for campaign data..." as a first-class state instead of a screen of zeros. Injection is one `modded class` over the base game's **empty** `SCR_EditModeEditorUIComponent` (EDIT-mode-only structurally — Photo/Building/Screenshot never reach the hook); zero new networking (grep-gated); an empty, Doxygen-documented detail seam (`GetDetailSlot`/`ShowDetail`/`ClearDetail`/`GetInstance`) is `hud-icons`' contract. Ships `OVT_GMPanelFormat` pure statics with 14 Logic assertions (Fast 139 green; the one red diagnosed as a pre-existing case wedged by a 3m44s Steamworks stall, passes in isolation). Three engine findings recorded: overlay measure ignores `AlignableSlot` vertical padding (spill fixed by moving spacing to child slots), slot align enums are 0=left/1=center/2=right, and `FrameWidgetSlot` positions via `OffsetLeft/Top` (PositionX/Y is a Workbench mirror). gm-state's MP gate partially discharged via the panel; owed: log-based checks (record counts, non-admin negative path, JIP). Same-session amendments: **the panel's first live use caught a real gm-state bug — polling could NEVER start** (`Event_OnEditorManagerInitOwner` fires at connect, before the controller component subscribes; fixed with a `GetInstance()`+`IsOpened()` catch-up in `OnPostInit`) — plus a `#ifdef WORKBENCH` authorize-all (user decision: the panel is a Workbench debug tool; define absent from shipping builds) and final position 22,90. After amendments: campaign data user-confirmed live end-to-end in Workbench Play, Fast 139/139.
- v1.20 (2026-08-14): **gm/gm-state BUILT — the gm epic's data spine, in one autorun (26/28; MP play-test owed).** A server owner opening Game Master can now be streamed the campaign's hidden numbers: threat (float precision — `GetThreatLevel()` truncates), both OF resource pools, next-distribution and next-payout amounts with locally-ticking countdowns plus **suppression flags** (QRF running / zero players — so a countdown reaching zero with nothing happening is explained, not a bug report), and one record per base / non-empty base upgrade / active deployment / tagged AI group with its origin ("why does this group exist" — base, town, tower, QRF, deployment, job — via a tag-and-sweep registry that **never untags**, routing around four pre-existing group-cleanup leaks now recorded as `occupying` debt). Strictly read-only: the "next distribution" amount comes from a pure `PredictResourceGain` **extracted** from `GainResources()` (calling the original would have handed the OF a free income tick per GM poll). Gate is server-side on every handler (`GAME_MASTER` role OR admin; refusals silent + throttled); wire is versioned, seq-guarded, `RplRcver.Owner`-only, max arity 6, listen-host short-circuited at all 9 RPCs. Gates: compile 0, Fast 113→114, All 157; Logic/Init/Campaign cases each proven able to fail (the Init one by a real red run with the prefab block removed — the only gate that catches that silent failure). Sibling contract written into the feature's `context.md`; `overthrow-panel` (next) carries the epic's help-docs phase. Owed: §7 MP verification checklist (gate under real roles, JIP, host, lifecycle, measured record volume). **core/controller-migration BUILT END TO END — all 10 phases in one autorun (56/56), and the 2,001-line `OVT_PlayerCommsComponent` monolith no longer exists.** Every client→server request now rides a domain component on `OVT_OverthrowController` (17 components, 10 new), reached only via the compile-verified generic `OVT_ControllerComponent<T>.Get()` — `OVT_Global` gained no getter, lost six, and shrank 1,007 → 292 lines (utilities split to `OVT_WorldUtils`/`OVT_PrefabUtils`/`OVT_LoadoutUtils`). Every migrated handler resolves the caller from its own controller entity — **no RPC accepts a client-supplied identity any more** — and validation rides each one: nine zero-validation endpoints were closed on the way, the worst being `DeleteCamp` (an **arbitrary-entity delete** for any client), dismiss-any-recruit and possess-any-entity. **Two shipped defects were confirmed from the code path and filed: BUG-161 — buying items was FREE for remote clients (dropped server-side `Rpc()`, no debit, no stock decrement) and a total no-op for listen hosts — and BUG-162 (PlayerDonated/SentFunds/SentMoney notifications never fired).** A ~20-strong family of "unconditional `Rpc()` from the authority" listen-host dead buttons (buy, save-from-menu, lock vehicle, radio-tower sabotage, …) got fixed as a class. Continue ≠ connect (BUG-104 family) settled with a proven-to-fail Campaign case plus two real lifecycle traps fixed (`IsDeleted()` revalidation; `DeleteRplEntity` over raw `delete` — first use in the tree). Gates green at every phase boundary: compile 0, Fast **101 → 112**, All **142 → 154**, 75-call `Rpc()` arity audit clean, persistence conf/scripts byte-untouched. **✅ Play-test gate DISCHARGED the same day — the user ran all 21 §6 steps and reported green** (including steps 2 & 8, BUG-161/162's acceptance evidence, and the rejection probes), Workbench loaded all three text-edited prefabs clean, and the listen-host save→quit→Continue (F15) passed — after `origin/main` was merged in (`275172b3`, main's BUG-161/162/164 monolith hotfix superseded by the migration; its BUG-163/165 road-spawn work ported into `OVT_WorldUtils`). The plan's own numeric ceilings drifted five times (all mentions-vs-calls arithmetic, each documented as P2-1…P10-1, none a missed re-point). **Feature complete.** — every build phase in one autorun (52/95), and the campaign's spatial influence rules now have exactly ONE implementation.** The map could show *that* a town had a `Nearby Radio Tower` modifier but never **which** tower, how far its reach went, or that a friendly tower was being out-shouted by an enemy one. Selecting a location now draws dashed, faction-coloured lines to every location it is in an influence relationship with, plus a stroke-outline reach ring for a selected tower or base. **The feature's real deliverable is the extraction, not the render:** `OVT_InfluenceRules` (pure, world-free) was pulled *out of* the code that already applied these rules, and `OVT_TownManagerComponent.CheckUpdateModifiers` — the server's 10-second campaign tick governing four permanent modifiers — plus `UpdateAllTownMomentum` were rewritten against it **behaviour-preservingly** (diff-verified: same branch structure, same operators, the tower branch's `else` intact and the base branch's deliberate absence of one preserved). **K2's cross-check is what makes client-side re-derivation safe**: an edge draws ACTIVE **iff** the geometry qualifies **and** the modifier is genuinely present in the target town's replicated list — so the overlay cannot assert an influence the campaign has stopped applying, whatever the reason, and a sabotaged tower renders dim from the general rule with **no special case**. One wire change in the whole feature: two floats appended to the existing config JIP bitstream with `CONFIG_STREAM_VERSION` bumped 1→2 (26 writes, 26 reads, verified slot by slot). **Two engine facts were measured rather than assumed, and both changed the work**: `vector.Distance` is **not** a correctly-rounded square root (exact at 1500 m, +1 ULP at 1000 m and 2000 m), which made one planned boundary case toothless and another unreachable through world geometry; and `ScriptBitWriter`/`ScriptBitReader` **cannot be round-tripped from script** — `new` returns non-null and the first proto call hard-crashes the process — so the JIP append has **no automated coverage and cannot have any**. Also promotes `GetFactionArgbByIndex` onto `OVT_MapLocationType`, collapsing two byte-identical bodies so all three canvas layers share one colour implementation; adds **zero** rows to the `OVT_MapCanvasLayer` contract (the second consumer in a row to leave it untouched); and ships a Field Manual section plus four wiki pages. Gates: compile **0 / 5998 files**, Fast **99**, All **137** (+10 Logic cases, **12 inversions run, every case observed red**). ✅ **Play-tested green (SP + MP) and closed by the user the same day** — and **the Phase 5 tunable defaults shipped unchanged**, so the legibility retune the plan budgeted for (and that `territory-overlay` needed after each of its two renders) was not needed once. A late `m_eRingMode` attribute (DASHED / SOLID / NONE) was added at the user's request — SOLID is **one** draw command via `m_bShouldEnclose` and is the cheap degradation, while the ring was never the expensive part (a flat 36 commands against up to 32 **per edge**). ⚠️ **Q-1 closed on one of its four numbers** — `Draw()` ~1 ms against a stated ≤ 0.5 ms budget, judged fine by the user but not the measurement the criterion asked for — so **the epic still has no recorded canvas frame-cost baseline**, the same gap `territory-overlay` left.

- v1.17 (2026-08-12): **`logistics` epic planned (6 features, requirements only) — and a repo-wide correction of stale EPF persistence guidance.** The **logistics** epic was scoped from scratch and scaffolded: `resource-core` → `resource-transport` → `resource-trade` → `resource-construction` → `resource-storage` → `building-repair`, each with a `requirements.md`, plus `epic-overview.md` / `epic-requirements.md`. Planning was grounded in code reads rather than assumption, and three findings reshaped the design: **the vanilla resource system cannot back it** (`EResourceType` is a hard engine enum — `SUPPLIES`, `ELECTRICITY` — that a mod cannot extend, so `SCR_ResourceComponent`/`Container`/`Generator`/`Consumer` are out of the data path and only Conflict's *UX patterns* are reused); **Conflict does not tie supply weight to vehicle handling** (no mass code anywhere in `scripts/Game/Sandbox/Resources/`), so weight is config data only and volume is the sole constraint; and **Overthrow already has ~70% of a bulk ledger** in `OVT_WarehouseData`, so warehouses hold resources *and* become buildable rather than a parallel store being built beside them. Also decided: import-only sourcing, merging crate piles with map markers, export-anything-for-cash, a war-biased bounded price drift ticking on `OVT_EconomyManagerComponent`'s existing hour-gated `CheckUpdate`, deliberate speculation, and **four new `OVT_DifficultySettings` multipliers** (construction resource cost, repair fraction, price level, price volatility) all following the shipped `buildableCostMultiplier` → `GetBuildableCost()` pattern. `building-repair` was added after planning: a destroyed building is disabled and its **ruin becomes a construction site**, re-entering `resource-construction`'s machinery rather than reversing engine destruction (`GoToDamagePhase` is one-directional in practice and `m_bDeleteAfterFinalPhase` prefabs leave nothing to heal). 🔴 **Separately, a documentation defect was found and fixed: every "how to persist state" instruction in the repo still described EPF**, which `core/persistence` retired on 2026-08-02 — `Scripts/` has zero `EPF_` references, `addon.gproj` declares only the base game, and `tools/launch-server.sh` already said so. Corrected across **CLAUDE.md** (persistence section + External Dependencies → a ⚠️ Retired block), **6 skill files** (`enforcescript-patterns/persistence.md` rewritten wholesale from 481 lines of EPF guidance to the vanilla `ScriptedComponentSerializer` contract — config binding, version-first positional payloads, `OVT_PersistenceTracking.Track()` for spawned entities, no console guards — plus `SKILL.md`, `common-pitfalls.md`, `component-patterns.md`, `overthrow-architecture/managers.md`, `controllers.md`, `file-structure.md`), **`docs/technical-design.md`** (§7 rewritten; dependency table, reference trees, and the stale "Paused: core/persistence" + `main` code-freeze status) and **`docs/mission-statement.md`**. Lesson recorded: *the feature that retires a system should own deleting its guidance* — `core/persistence` shipped 53 tasks and none was "update the docs that tell agents to use EPF". Housekeeping: **`virtualization` was missing from the feature table entirely** and now has a row; `map`'s counts refreshed (`location-types` 42/42, `map-layers` 70/112). ⚠️ Changelog version numbers below overlap by date — `new-map` and `new-player-experience` incremented in parallel and merged, so v1.10–v1.13 (2026-08-10/11) are *newer* than v1.14–v1.16 (2026-08-09). Entries are ordered newest-first by **date**; the numbering is not renumbered because it is an audit trail.

- v1.13 (2026-08-11): **map/respawn BUILT END TO END — 8 phases in one autorun (126/152), and the first feature in this epic with zero runtime evidence.** Death no longer spawns you automatically at home one frame later; it opens a dedicated SPAWNSCREEN map listing every location you are entitled to spawn at, and waits. **"Respawn at home" is always available, even inside an active QRF** — true by construction rather than by a special case, because HOME routes through the untouched `CreateFreshCharacter` and never goes through the eligibility rules at all. **A client cannot spawn itself anywhere it likes:** the server treats the client's vector purely as a lookup key against its **own** enumeration of eligible positions, spawns at *its* recorded vector, and falls back to home rather than failing when the pick has gone stale. The chosen position is a **parameter**, never a member and never a map — the four pre-existing `CreateFreshCharacter` callers are unedited, so there is no state for a stale override to leak from. New: `OVT_RespawnService` (7 pure predicates + the server enumeration), `OVT_RespawnRequestComponent` on `OVT_OverthrowController` (**not** the deprecated comms component), `OVT_RespawnMapUI`, `OVT_RespawnContext`, `OVT_RespawnScreenHandlerComponent`, two configs, two layouts, three prefab edits and 11 string ids. Gates green at every phase boundary: compile **0 / 5964 files**, Fast **44 → 54**, All **79 → 89**, with ten Logic cases each **proven able to fail**, and the Campaign and Persistence tiers measured before *and* after the spawn-path edit and identical. **Four of the plan's premises were wrong and were caught during the build**, the worst being that `MapContext` is activated by the *menu preset* and not by `SCR_MapEntity` — without the fix the map would have rendered perfectly and ignored every input, and the spike would have "failed" for a reason that had nothing to do with workspace hosting. The corpse-window audit found **seven** `GetControlledEntity()` guards where the plan listed five. ✅ **Play-test gate DISCHARGED the same day — the user ran the full matrix and reported all green, with NO code change required to pass it.** That is the whole of the runtime evidence for this feature and it had to be: the autotest world has no players, so `OnPlayerKilled_S` never fires, no RPC crosses a wire, there is no UI tier, and six file classes are invisible to both automated gates. The gate settled the feature's one genuine unknown in its favour — **a workspace-hosted vanilla SPAWNSCREEN map does take input**, provided the context activates `MapContext` itself each frame, so the `ChimeraMenuBase` fallback was never needed and Overthrow still has no `ChimeraMenuBase` menus. It also confirmed two things that were only source readings: a dead player keeps ownership of its `OVT_OverthrowController`, and the persistent id resolves with no controlled entity. **Feature complete.**

- v1.12 (2026-08-10): **map/legacy-retirement SHIPPED — all 7 build phases in one autorun, verification gate discharged same day; the legacy map is deleted, and the map epic's committed scope (4/4) is COMPLETE.** The last of the map epic's committed scope. `OVT_MapIcons.c` (846 lines), three layouts and their `.meta` files, one config block, four RPCs, one string id, one broadcast preset and two main-menu rows are gone; `OVT_MapContext` went **591 → 79 lines**. There is now exactly **one** map surface, **one** travel path and **one** place a location's data is rendered. **No player-visible capability was removed** — every deletion was preceded by a grep proof that a live path already did the job, recorded before the cut rather than after. **Caller-first ordering made it bisectable**: P1 cut the two menu rows first, so every legacy mode was *provably unreachable* before a line of map code was deleted. **P4 was a security fix, not a cleanup** — the four `RequestFastTravel*` RPCs were deleted **including their `[RplRpc]` attribute lines**, so the wire surface is gone, not merely its callers; `RpcAsk_RequestFastTravel` was a two-line unvalidated, unpaid teleport to an arbitrary `vector` callable by any client, and it survived P1's unreachability only because a registered RPC does not need shipped UI to be callable. Its recruit ring-placement half was extracted and **diffed verbatim-identical** to the surviving `OVT_TravelRequestComponent.TeleportRecruits` before being cut. **BUG-069 part 4 is structurally closed** — `grep -rn "SCR_MapEntity.GetOn"` is empty, so no Overthrow context subscribes to any static map invoker and the per-respawn listener leak cannot be reintroduced by an existing file. **The plan was wrong in five places, and each was caught by refusing to treat a `file:line` as a `sed` argument**: the `.st` block to delete is 22 lines, not the "~18" quoted (a range would have left four metadata lines dangling — the exact silent string-table corruption this project has already suffered once); P5's own acceptance grep can never pass, anchoring on a `./` path prefix this `grep` build does not emit; its brace-delta check ignores that GUID literals contain braces; Q-4 expected nine `OVT_MapIcons` prose mentions in six files when the true set is **eight in five**; and P5's "two surviving test-suite mentions" was three, so the criterion would have passed **by coincidence** — corrected properly instead. **Two findings the requirements never anticipated, both player-visible if missed**: the main menu's **only** `SetFocusedWidget` call was hiding inside the "Map Info" block, so a naive deletion would have opened the Overthrow menu with **nothing focused** and left a controller player unable to navigate at all; and the in-game **Field Manual documented both deleted entries verbatim**. The Field Manual was rewritten rather than deleted — the map is *more* capable than the text described, only the route and the interaction were false — with **every replacement sentence backed by a `file:line` and six cut for being unprovable**, including an inherited claim about "intel known about the area" that no info panel has ever surfaced, and a close-button instruction that would have documented open **BUG-134**. A third finding surfaced from the first: layout order ≠ wiring order, so the relocated focus landed on the *third* visible row; the user chose to **reorder the menu by frequency of use** (`Place, Build, Resistance, …`), a recorded, deliberate widening of a removal-only feature, executed as pure byte-identical block moves. **Prose rot proved broader than the plan's model** — three independent stale `file:line` comments turned up in files the deletions merely passed through, one citing bus-stop code in a file containing none; a codebase-wide audit is now open epic tech debt. Coverage deliberately unchanged: this feature deletes no covered behaviour and adds no assertable logic, so **the invariant *is* the test** — Fast **44** and All **79** held at every gate, compile 0 across a set that dropped exactly 5959 → 5958. `towns/map-info` archived to `docs/archive/towns-map-info-*.md` and the towns epic no longer claims the map. ✅ **Gate DISCHARGED the same day — the user ran the full Phase 7 and reported all green**, then regenerated the six localization exports (verified in the tree: all six diffed, `OVT-NeedBusStop` absent from every one, rewritten Field Manual text present). **That gate carries almost all the evidential weight for this feature**, because **four of the seven phases edited `.layout`, `.conf`, `.et` and `.st` files, which are invisible to `compile-check.sh` *and* to both test groups** — a dangling GUID, an orphaned config entry or a half-deleted string block passes every automated gate and fails in the world. So the clean Workbench load is the *only* evidence the deletions were sound; the gamepad pass is the *only* evidence FINDING A's fix works; and **7c — two clients, JIP, concurrent travel — is the only evidence the map's per-player isolation survives a second client**. 🎯 **It discharged two features at once**: `map/location-types`' outstanding V-3…V-7 was folded in per settled decision D-4, closing the epic's stated hard gate, and it also covered the two-client cases `map/fast-travel` had explicitly recorded as beyond a single session — **so this epic has now been play-tested in multiplayer for the first time since 2025-08-02.** ⚠️ **The plan's per-phase revert path was never available** — it assumes a commit per phase (Q-10) and this run wrote no git state, so all seven phases arrived as one uncommitted change; moot now the gate is green, but the reason to commit per phase was precisely the case that did not occur. Logged rather than fixed: ten `Target_*` translations still describing deleted UI (deliberately **not** machine-translated), a pre-existing orphaned `.meta` for the *retained* `OVT_MapThreatGrid`, and an unguarded `m_aModifiers[modifierData.id]` index at `OVT_MapLocationTown.c:272` — the same defect the deleted `OVT_MapContext` had, **moved rather than fixed** by the rewrite.

- v1.11 (2026-08-10): **map/fast-travel SHIPPED — 5 build phases in one autorun, play-test gate discharged same day.** The map's *verb* half reached parity, and the epic's worst authority hole closed. **Execution and payment left the client.** A new `OVT_TravelRequestComponent` on `OVT_OverthrowController` (modelled on `OVT_ShopTransactionComponent`, **not** the frozen `OVT_PlayerCommsComponent` — F3) implements one RPC pair for both verbs and runs *validate → resolve destination → compute fare at the origin → teleport → charge only on success*, so "charged but did not move" is impossible by ordering rather than by a refund path, and a crash mid-routine loses the fare in the **player's** favour. The client-side `SCR_Global.TeleportPlayer` and the client-side `TakePlayerMoneyPersistentId` — which mutated only the calling machine's copy of the balance and was silently overwritten by the next authoritative sync — are **deleted** (F1/F2). `OVT_FastTravelService` was made **entity-parameterized** first (Phase 1): the actor is a parameter, so client and server run byte-identical rules and "displayed availability matches enforced availability" becomes satisfiable at all; `CanGlobalFastTravel` survives as a documented **client-only** wrapper with its signature deliberately frozen so `location-types`' five per-type overrides compiled untouched. **F4 — recruits ride along again**, and are now **opt-out**: the server takes the recruit entity list **once** and uses that same list for both the fare and the teleport, so "pay solo, arrive with a squad" is structurally impossible rather than merely tested-for; the toggle defaults ON because defaulting OFF would have re-shipped the exact regression F4 was filed for. **F5 — bus travel migrated, and the headline is a deleted flag**: BUG-069 part 2 (an engine-side map close left `m_bBusTravelActive` armed and the next click silently charged a fare) **cannot recur by construction** — eligibility is re-derived from the player's live position on every panel build and the verb is a pure function of the marker clicked, so there is no state to survive a close; the server independently re-derives the origin *and* validates the destination, so a client naming an arbitrary vector gets `BAD_DESTINATION`. **Three defects were found that no gate could see**, two of them by adversarial review rather than by any agent's own work: an out-of-range `verb` matched neither branch in `ValidateTravel` and therefore skipped the wanted, QRF **and** distance checks before charging the ordinary fare (*"teleport anywhere, at any wanted level, mid-QRF, for the standard price"* from a modified client); `OVT_MapContext` registers a **second, independent** `MapSelect` listener that never got the info-panel guard the new UI received, so one click on the travel button could fire both handlers — two debits at two *different* fare models and two conflicting teleports; and `m_HoveredElement` was the one member surviving a map close, able to pin a stale element from a dead world onto a money path. All three compiled clean and passed every test. **Phase 0 — the two-client MP session the plan required *before* fixing — was never run**, because an autonomous session cannot drive two Reforger clients; two of its three branch decisions were instead settled from static config evidence (the `gamepad0:x` collision with vanilla's `MapContextualMenu` is **real** — `SCR_MapRadialUI` survives Overthrow's delta override — so fast travel moved to `pad_right`, the only free pad input), and the third was made moot by taking the branch that is correct either way. A `KC_R` collision scare was **disproven at the source**: `GadgetContextToggleable` only activates for gadgets whose use-mask contains `FROM_ACTION`, which no map prefab sets. Likewise a suspected localization-rendering bug was **disproven by a live probe** rather than by inference — `#key` embedded in a larger string does resolve — though the labels were moved to `WidgetManager.Translate` with `%1`/`%2` anyway, since concatenation hard-codes English word order. Coverage: +1 Logic case (Fast 43 → **44**, All 78 → **79**), proven able to fail twice — once for the fare maths, once for the verb→km-floor mapping after review showed the original assertions would have stayed green through a swap of exactly that argument. 🔴 **Nothing in this feature has been observed at runtime**: Phase 0 and Phase 6 are both user-driven and both outstanding. Two gates before play-testing: the five new `.st` ids are in the master and in **none** of the six generated exports (and because the fare now lives *inside* the string values, an unresolved key drops the price as well as the words), and **legacy fast travel is still live** via `OVT_MainMenuContext.c:218` — the plan wrongly listed it as dead — running the old client-side teleport at a *flat* fare that contradicts the new distance-scaled one, so any money observation is ambiguous until `legacy-retirement` cuts it. The dead-code hand-off written for that feature was line-corrected and its `.st` guidance rewritten to forbid deletion by line range — the quoted range would have corrupted the string-table master mid-block, the exact failure the project has already suffered once. **Gate DISCHARGED the same day: the user regenerated all six localization exports, play-tested, and reported all green** — which is the only evidence that exists for the parts the automated spine is structurally blind to (the server routine actually executing and debiting, the recruit toggle rendering/repricing live, the R6 `IsSelectionOnInfoPanel` guard that was explicitly flagged "unverified at runtime, may be inert" and is the toggle's only gamepad path, bus travel end to end, and the `KC_R` keybind not stowing the map — the one FIX-4 conclusion resting on inference rather than a shipped example). **Feature complete.** Two things a single-session green does *not* cover, recorded rather than assumed: Phase 0's two-client dedicated-server cases (concurrent travel by two players, JIP travel shortly after joining, and the listen-server host's result short-circuit), and the fact that **both travel paths remain live** until `legacy-retirement` cuts `OVT_MainMenuContext.c:218`. **One post-ship defect came straight out of that play-test and is already fixed**: after travelling, the map view closed but the character was left holding the map item raised. Root cause is a project-wide inversion, not a fast-travel bug — Overthrow closes the map with `SCR_MapGadgetComponent.SetMapMode(false)`, which closes the *view* only, where vanilla stows the gadget and lets the view close as a consequence. Both `HideMap()` methods now clear focus (`ToggleFocused(false)`) and then stow through a guarded helper; the same change fixed a quieter second half nobody had noticed — `SCR_PlayerController.SetGadgetFocus` was never cleared on **any** map close. The trap that invites the wrong fix is real and worth knowing before touching `map/respawn` or `legacy-retirement`: `SetGadgetMode`'s hand→storage branch **early-returns for `EGadgetType.MAP`**, so stowing alone never closes the view synchronously.
- v1.10 (2026-08-10): **map/location-types built — 35/41 tasks, 6 phases in one autorun, awaiting play-test.** The map's *content* half reached parity with the legacy `OVT_MapIcons` layer it replaces. All four gaps closed: **Vehicle** markers (your own only, rotated to heading), **job + recruit waypoints** (which previously shared one field and silently clobbered each other), the **POI registry**, and **bus stops**, the last two migrated off vanilla `MDT_BUSSTOP` descriptors and a legacy static onto one new `OVT_MapMarkerComponent` + `OVT_MapMarkerManagerComponent`, discovered by a **client-safe world scan with no replication and no persistence added** (the proven `InitializePorts` pattern; static world entities are identical on every machine). `OVT_TownManagerComponent.GetNearestBusStop`/`FindBusStop` deleted and `OVT_MapContext` re-pointed at the registry, which also unblocks `map/fast-travel` F5. **`OVT_MapIcons.RegisterPOI` — a *compile-level* dependency, not just a visual one — is eliminated**, so `legacy-retirement` can now delete the legacy file without breaking a call site. 🔴 **A live multiplayer privacy leak was fixed**: `OVT_MapLocationHouse` iterated every player's owned/rented maps, so everyone saw everyone's property; it now reads `GetOwned(persId)`/`GetRented(persId)` and **fails closed** (shows nothing) when the local persistent id cannot resolve. Nine types that rendered only a panel header gained **real info panels** through one additive `map/core` contract extension (`m_SharedInfoLayout` + `BuildInfoRows` + row helpers) — Town/Base/RadioTower keep the bespoke path and are structurally unreachable from the new branch. Shop panels gained per-item **scarcity carets** and a shop-level **remoteness badge**, split by pricing *term* rather than statistically normalised, computed **entirely client-side from already-replicated state** and **never showing a price, percentage or stock count**. Mid-run the user redirected the gun-dealer panel to list **what that dealer stocks** rather than what is dear; research confirmed the premise for four categories (rifle/sniper/MG/launcher are per-dealer rolls) and **corrected it for pistols** (identical at every dealer, so excluded). Coverage: +2 cases, **each proven able to fail** before shipping (Init: the bus-stop prefab block, which `compile-check` structurally cannot see; Logic: the caret banding maths, mutated twice). Fast 42 → **43**, All 77 → **78**. **Three defects were caught by review rather than by any gate, all of which compiled clean and passed every test**: POI icons hardcoded white against a white-glyph atlas (invisible markers on a light map, on a false premise that `.conf` cannot express a `Color`); info rows clipping their own glyph descenders and icon, found only by the user's play-test; and **N19** — gun-dealer carets pinned at ▲▲▲ at every dealer forever, because `ShopConfig.conf` stocks no weapon type at any town shop and `RegisterGunDealer` never populates `m_mTownShops`, so those carets ship default-off. The through-line: `.layout` files, `.conf` entries, icon tints, prefab blocks and the string table are **outside** what `compile-check.sh` and both test groups can observe — "green" meant "the script compiles", never "it looks right". **Phase 7 is user-driven and outstanding**: the two-client MP/JIP gate (the only thing that confirms the privacy fix and per-player isolation across clients), gamepad pass, zoom sweep and save compatibility. Six findings queued to file against other features (N5 FOB/camp garrison not replicated, N6 + addendum `GetTownStock`/`GetShopByRplId`/`DistanceToNearestPort` null-derefs, N14 gun-dealer weapons re-roll every load, N17 sniper ammunition likely never stocked, N18 `RegisterGunDealer` has no broadcast RPC, N19), plus `map/core` D8. Art landed mid-run (bus icon, caret set); the caret glyphs need a **horizontal redraw** to make 1-vs-2-vs-3 legible at row size — direction reads, magnitude does not.
- v1.16 (2026-08-09): **new-player-experience/starter-jobs-retirement built AND closed — 52/52 tasks, 7 phases in one autorun, play-test passed the same day. THE EPIC IS COMPLETE (242/242 across all five features).** ✅ **All eleven play-test checks passed.** U1 — Continue a campaign save taken *before* this build — **passed on a real campaign**, which is the observation the entire phase order existed to make possible: the migration is now verified at a scale the seeded fixture structurally could not reach. **And M1-M5 passed, giving this epic its first two-client observation ever.** The epic had recorded from the start that per-player isolation had **never** been seen working with two real clients; M3 saw an owner-filtered send reach client A and **not** client B, and M4 saw a job board survive JIP through `RplSave`/`RplLoad` — the one replication path this feature deliberately did not touch, which is exactly why watching it was worth doing. ⚠️ Recorded precisely rather than generously: that is the **jobs** delivery surface, **not** `tutorial-system`'s popup delivery surface, whose F7 remains unobserved. Same engine mechanism, so the epic's last open verification is now smaller and better understood — but it is not the same test. The build details follow. The five "tutorial" starter jobs are gone from the shipped build — configs, prefab registrations and a stale Eden world-layer override all deleted. **But the feature was never about deleting five files. It was about the int that named them.** `OVT_Job.jobIndex` is a **persisted array position**, and the five doomed configs sat at positions 2, 4, 5, 6, 7 of twelve, so a naive delete would have shifted every later job down — and `FindRestorableJobConfig()` only rejects records that are *out of range*. Five saved records would have come back attached to **different** jobs at stage indices that happened to be valid, been ticked, and paid out those jobs' rewards (**$750 instead of $0**), while the lifetime-counter maps, keyed by the same int, capped the wrong jobs. No error, no log line, no crash — visible to a player only as "jobs behaving oddly on a continued campaign". So the phase order *was* the feature: give every job a stable id, move the save format onto it, **prove it**, and only then delete, with a real decision gate standing in front of Phase 4. `OVT_JobConfig.m_sId` now gives every job an identity independent of list order, the job payload is version 2 keyed on that id, and a **frozen** twelve-entry legacy table migrates pre-feature saves — dropping each retired record with a WARNING that **names** it, because a record that cannot be restored is dropped *and logged by name*, never silently reassigned. **The plan's own mitigation contained the exact bug it was written to prevent, and only measurement caught it.** Decision D4 specified reading v1 payloads through *newly named* frozen shadow classes. The advanced agent tested that against the real binary container before writing it and found the container writes the concrete class name into every record as a `$type` discriminator and **instantiates from it on load** — so the rename would have made every v1 read return an **empty-but-non-null** array, which then poisons the rest of the stream and which `ApplyPersistedJobs()` would have applied: **board wiped, every lifetime counter wiped, no error**. Proven with throwaway round-trips through the real save context, ordered so the control read **last** to rule out "a container can only be read once", and fixed by **inversion rather than redesign**: the *existing* class names are frozen as v1-only and the *new* v2 records take the new names. Two more of the plan's own "belt-and-braces" notes were promoted to load-bearing by the same measurements — the local variable name **is** the property name and **is** validated, and an empty collection reads back `true` while only a genuine failure reads `false`, which is what makes the new read guard safe. Verified end to end against a **captured pre-migration save fixture** — the one artifact in the plan with a deadline, because after the migration the tree can no longer *write* a v1 payload: three retired ids dropped by name, two survivors restored on their correct configs, matching a byte-level decode of the fixture field for field, run twice to confirm idempotency. The broken fixture-check recipe the plan shipped turned out to be broken in **three** ways, not the two already known, and the replacement is *better than what was asked for*: a dedicated server auto-continues, so the migration now runs through **`SaveGameManager` and a real world transition** — the path the plan's own testing strategy listed as structurally uncoverable by automation. **Zero wire surface changed, proven by diff rather than asserted**: exactly one line in the whole feature diff matches the RPC/JIP pattern and it is a comment explaining why nothing else does (BUG-090 records that `Rpc()` arity compiles clean and dies silently at the wire, so the design deliberately gave that hazard no surface at all). Three assertions added and **every one proven able to fail** — the round-trip case twice, via two independent breaks — and the Phase-1 tripwire then fired **for real, by accident**, when a test run still in flight caught the config deletion before the guard was flipped. **The feature closes no bug, and says so loudly.** Its own stated premise was **stale on arrival**: BUG-037 and BUG-040 were fixed *in place* on 2026-08-03 and have been `closed` since, so removal discharges neither — what justifies it is redundancy against the shipped tutorial entries. Two more inherited "facts" fell to re-verification: the rewards lost are **$100, 30 XP and two field dressings**, not the "$100 and 10 XP" four documents carried (`m_iRewardXP` **defaults to 5** and is paid unconditionally, and only one of the five configs declared it), and the plan's top risk — whether Eden's world-layer job override merges or replaces — was settled by direct headless measurement rather than by trusting a documented prior: it **merges**, Eden always ran all twelve jobs, so no bug was filed and the block was deleted as pure cleanup. The epic's inherited `welcome-intro-3-ui.edds` liability was **discharged by inspection, not work** — seven jobs survive, so the Overthrow menu's Jobs entry stays and the screenshot stays true. Also discharged: the **jobs epic's positional-`jobIndex` tech debt**, ticked off with the mechanism that replaced it. Ten stringtable items were retired *in place* with retirement records rather than deleted (six languages of translation), and Q3 was verified mechanically — every changed line under `Language/` is a `Comment` line, the ten `Text` fields byte-identical. Gates: compile-check 0, Fast **54**, All **92** (delta **+2/+3**, not the plan's arithmetically wrong "+3 to both"). Owed: **the U1 play-test — Continue a campaign save taken from before this build and read the log for one named WARNING per dropped starter job.** It is the single most important observation in the feature and the one thing no automation here can reach, because the fixture is seeded and small and a real campaign is neither. The epic's two-client MP pass remains outstanding.
- v1.15 (2026-08-09): **new-player-experience/first-spawn built AND closed — 49/49 tasks, 7 phases in one autorun, play-test passed the same day.** The epic's third fully signed-off feature, and the first to extend the tutorial framework with **code** rather than config. A new player's opening two minutes are no longer a teleport, a car, and a 20-second corner hint that repeats every session: a **four-page dismissable welcome modal** now fires on their own first spawn and never again on that machine, and the campaign-setup screen finally describes what its three choices actually do. **The design turns on a fact the client cannot derive.** `FinalizePlayerPreparation` has two branches — one grants a house, a car and starting cash, the other spawns at a bus stop with none of it — and `home != vector.Zero` is true in **both**, because the fallback path calls `SetHomePos` too. Three candidate client-side discriminators were checked and all three fail, so the server authors a per-player spawn context and pushes it over one `RplRcver.Owner` RPC; **two** entries filtered `house`/`nohouse` on the same `PLAYER_SPAWNED` event then select between themselves, so the player ever sees exactly one modal. Three of the four pages **share string keys deliberately** — two copies of a sentence can drift and no gate can see it, and the houseless variant is precisely the one nobody play-tests. Every failure mode degrades rather than disappearing: a lost context costs one page of accuracy, never the welcome. **The legacy `#OVT-IntroHint` is retired**, and its "dead since the 1.6 spawn rework" code comment was re-verified rather than inherited and found **only two-thirds true** — it genuinely still fired on single-player respawn-after-death and after a Continue. Both string items were **kept** rather than deleted (six languages of translation) with a retirement record naming the three claims in the hint's own text that were or became false. **Rule 0 bound this feature twice over** — eight pages of player-facing prose plus nine campaign-setup descriptions — so the fact-check was Phase **0**, before a word was written, and it **cut nine sentences for want of a `file:line`**, including one the plan itself had specified. Three "obvious" candidates were disqualified by evidence: `realEstateCostMultiplier` is declared, replicated and **read by nobody**, so its authored 0.4→2.0 spread has zero effect; `ChargeRespawn` only charges when `money > 500`, a hardcoded gate unrelated to the `respawnCost` setting, making "dying costs $N" false for exactly the players it would matter to; and the supporting faction **sends no troops, supplies no equipment and touches no price**, besides being neither replicated nor persisted. All three recorded as findings and **none fixed** — they are balance and save-format changes wearing bug-fix clothes. **Two failure modes were caught that no automated gate in this project can see.** `TextWidget` does not wrap unless `Wrap 1` is set, so every setup description would have clipped to a single line — the shipped `DifficultyDescription` had survived only because its placeholder was 28 characters. And **an image carries a claim exactly as a sentence does**: the first art wiring pointed *both* entries' page 2 at the same car-in-a-garage header, so the houseless entry illustrated "no house was free, you own nothing" with a picture of a car in a garage — the exact contradiction the two-entry design exists to prevent, reintroduced through the artwork *after* the prose had been fact-checked to death. Caught by reading the images against the shipped copy rather than trusting the filenames. **The feature ships with the first page art any tutorial entry has ever carried** (four 2048×512 headers, and the popup geometry re-shaped from square to a 4:1 band to hold them), which also means `LoadImageTexture` and its `Is Visible` branch ran in a shipped configuration for the first time. Gates: compile-check 0, Fast **51**, All **88** — and the plan's own `+3` case target was **arithmetically wrong** and was corrected rather than chased, because one of the three was always specified as a *branch* on an existing guard, and a branch adds assertions rather than executed cases. Four mutations proven red, with the Init guard's two branches proven **separately** because one mutation only exercises one of them. Owed: one small string re-export for `OVT-FieldManual_Welcome_Text2` (the manual's Welcome page gained a houseless clause so it no longer contradicts the entry that deep-links to it), and **the two-client MP pass — now the epic's single highest-value outstanding verification**, since per-player isolation has never been observed passing for this framework and this feature adds a new owner RPC to that same delivery path. `starter-jobs-retirement` is the last feature, and it inherits one concrete liability: `welcome-intro-3-ui.edds` is a screenshot of the Overthrow menu **with Jobs visible**, so removing that entry silently makes a shipped image wrong and nothing will flag it.

- v1.14 (2026-08-09): **tutorial-content CLOSED — play-test passed, string table exported, wiki paragraph applied.** The epic's second fully signed-off feature. The play-test verdict was "all the tips seem fine", and the docs record honestly what that does and does not cover: tips firing, rendering and behaving in a live campaign are attested; the two-tip queue, two-client per-player isolation, `bases-first-capture` and the Learn More spot-checks were never individually exercised and are marked **unverified rather than passed** — `bases-first-capture` carrying the most residual risk as the only entry delivered by the 300 m proximity fan-out rather than a per-player RPC. **The first play-test reported "no tips at all" and was a false negative**: Workbench was running stale compiled scripts. Three pieces of evidence — `Loaded 11 tutorial entries` in the log, `m_bTipsDisabled 0`, and a seen store holding only the two old proof ids — ruled out the data, the settings and the seen store *before* any cause was proposed, and the fourth (an unexported string still renders its raw key, so "nothing at all" is a different failure from "raw keys") ruled out the export. A recompile fixed it. The same class of error the feature spent three phases guarding against in *content* arrived instead through the *build*. **The owed wiki edit was diagnosed rather than waited out.** `wikijs_get_page` had been failing on every page with an opaque `RetryError`; the cause was the MCP server process having lost its auth, so it issued **unauthenticated** GraphQL — `pages.list`/search are public and worked, while `pages.single` returned *"You are not authorized to view this page"* at HTTP 200. Three further hazards surfaced while making that single 468-character edit, all now recorded: **search returns wrong `pageId` values** (it reported id 1 for "Wanted System"; page 1 is `home`), so an update by search id would overwrite the wrong page; **`pages.update` fails without `tags` and fails *after* saving**, so a mutation reporting `succeeded: false` had in fact written — caught only by a pre-write guard asserting the text was not already present; and **a failed update leaves the rendered HTML stale**, so the source carried the paragraph while the public page did not until `pages.render` was called. Verification is the live page, not the stored source. The two `**Tutorial Jobs**` paragraphs owned by `starter-jobs-retirement` were asserted intact before the write and confirmed in the rendered page after. Still open and reported, not fixed: **two Field Manual strings are known wrong** while the public wiki is right on both.

- v1.13 (2026-08-09): **new-player-experience/tutorial-content built — 24/24 tasks, 4 phases in one autorun.** Overthrow now teaches itself. Ten action-triggered entries ship: six in the first hour (home, money and shops, gun dealers, the map, the wanted system, skills) and four at the first escalation (recruits, placing and camps, building, base capture). **The framework's central claim held at scale** — all ten were added with **zero gameplay EnforceScript** (nine `.conf` files, 18 string ids, nine prefab lines, one adopted proof entry), which turns `tutorial-system`'s "adding an entry needs no script" from an assertion into a demonstration. Nine hang off per-player events; the tenth, base capture, is a documented 300 m proximity fan-out with **no acting player** and fires in **both** directions, so its body was written to read correctly for a bystander *and* for the occupiers retaking a base — it contains no second person at all. **Rule 0 was a phase-front task rather than a review step, and it earned its place three times.** A sentence the plan itself specified was **cut**: nothing in the code makes the occupying faction "come looking for you" at a wanted level, only a perception override exists. "A base weakens the occupiers *in that area*" was narrowed because `m_iThreat` is a single **global** counter. And — the finding that generalises past this feature — **the plan's own pre-loaded trap table contained a false row**, claiming gun dealers have no dedicated map icon when they do, via a separate enumeration path and their own sprite; left standing it would have driven the wiki pass to "correct" a page that was already right. **A trap list is not evidence.** The single `.c` change is test-only and closes the feature's one silent-failure mode: `SHOP_GUNDEALER` is the lone filter produced by an engine call rather than authored, so a `+60/-0` branch now fails the Init tier **by name** if it stops matching any `OVT_ShopType`, with a message that spells out that the tip would otherwise *silently never fire*. **Proven red** (exit 1, `FAILED (1 of 47)`) and reverted byte-exact; it also settled the risk in the affirmative — the enum does return the bare member name, so the tip was never dead. **`starter-jobs-retirement` is unblocked**, its precondition coverage mapping written into both features' docs, and it inherits a finding: the map already marks every shop and dealer **ungated by any discovery flag**, so the five jobs' unique contribution was a marker on one *named instance* plus $100 and 10 XP. Gates: compile 0, Fast **47**, All **78**, `Language/` diff the `.st` master only. Owed: a Workbench export of **18 new ids + 1 rewritten body** (until then every tip draws its raw `#OVT-` key), the play-test, and **one wiki edit that could not be applied** — `wikijs_get_page` fails on every page and `wikijs_update_page` replaces the whole body, so writing blind would have destroyed the `**Tutorial Jobs**` paragraph the next feature owns; the paragraph to paste is saved in the feature's `context.md`. Reported, not fixed: **two Field Manual strings are now known wrong** while the public wiki is right on both, which inverts the usual staleness direction.

- v1.12 (2026-08-08): **field-manual CLOSED, the epic's first fully signed-off feature.** All twelve entries now carry bespoke black-ink tiles matching the base game's field-manual art style, each wired from a GUID read out of its own `.edds.meta`; `default_ui.edds` drops back to being the fallback for future entries only. Final gates: compile-check 0, Fast **47**, All **78**, twelve of twelve image paths resolving, element-GUIDs unique. Definition of Done finishes **24/24**, with the provenance kept honest in the record: 16 rows machine-measured, **7 attested by the user's play-test** (rendering, tile art, tone, wiki agreement, none of which any command in this repo can judge), and **I4 corrected rather than waived** since its expected test count was stale from the moment it was written. `tutorial-content` is now unblocked on both of its dependencies. The **16 open questions** the feature surfaced stay open and belong to a gameplay owner, headlined still by the shop-price integer division and recruit XP being hardcoded to `"US"`/`"USSR"`.

- v1.11 (2026-08-08): **new-player-experience/field-manual built — 56/56 tasks, 9 phases in one autorun, awaiting play-test.** Twelve manual entries under four sub-categories replace the single page that was four topics in a trench coat, and **102 new string ids are already exported**. The twelve title keys double as the deep-link ids and are frozen and documented, which unblocks `tutorial-content` and `first-spawn` immediately rather than at the end of the feature. Content work carried one rule — **never document behaviour not verified in the source** — and that rule paid: a drafted sentence claiming "nothing raises a wanted level unless somebody can see you" was falsified before it shipped (`SCR_CharacterDamageManagerComponent` escalates on any damage with no witness check and no faction check), house storage was refused because only warehouses persist contents, and no difficulty-scaled figure is quoted anywhere. The single `.c` change is test-only: three Init branches (**+164/-0**, nothing weakened) that turn the silent-pruning trap into a red build, each proven red against a realistic breakage rather than a synthetic one. The **~35-page wiki sweep** is where it compounded — nine wrong difficulty numbers (Extreme's respawn cost documented as 100, actually 25), a fifth preset (**`Insane`**) that ships and was documented nowhere, `realEstateCostMultiplier` sold as a working discount while being applied nowhere, the resistance tax documented in the wrong order, a camp priced $100 under, dead modder instructions to copy two dependencies Overthrow no longer declares, and a `getting-started` page claiming three tutorial jobs when five ship. **16 findings that need a gameplay owner were logged, not guessed** — headlined by an integer-division bug making shop prices a binary +10%/+0% step rather than the "prices move with the town" curve the shipped tutorial tip promises, and recruit XP hardcoded to `"US"`/`"USSR"` so recruits never level on a compatibility-mod faction. Definition of Done: **16 ✅ MET, 7 👤 human play-test, 1 ⚠️** (the stale expected All-count, 78 vs 77, caused entirely by a concurrent session's untracked Campaign case).

- v1.10 (2026-08-07): **new-player-experience/tutorial-system built — 63/63 tasks, 9 phases in one autorun, awaiting play-test.** Overthrow can finally teach. Tutorial entries are pure data: a `.conf`, some string ids and one prefab line adds one, with **zero EnforceScript** — demonstrated by adding the second proof entry that way, not merely asserted. Triggers hang off ten *existing* server-side manager invokers rather than instrumenting call sites, and delivery is one `RplRcver.Owner` RPC to the acting player's controller — the failure that killed the starter jobs (BUG-037) is now structurally impossible rather than merely avoided. Seen-state lives per **machine**, not per campaign, so a veteran never sees a tip twice on any server. Two design risks were closed by measurement rather than argument: the settings-store shape (**R1**) was proven by quoting the block back out of the on-disk profile, and the same-GUID field-manual delta (**R2**) by loading the merged root through the menu's own loader and counting 6 categories / 8 tile backgrounds / 141 entries — then *demonstrating* the merge rule by deliberately colliding an element GUID and watching vanilla's Introduction get replaced. Test spine 66 → **75**, and 3 of the 9 new cases were proven red by **genuine defects** rather than synthetic breakage — one of them `array<T>.Remove()` being swap-with-last, a reordering bug that would have been invisible in play until two tips queued at different priorities. One acceptance criterion was **retired, not met**: F5's escalation keybinding, because risk R3 fired twice — there is no free gamepad input during gameplay, and a second, inline-aware survey falsified the first one's conclusion that `KC_T` and `shoulder_left` were spare (both are VON at Priority 110). Rather than ship a collision, the escalation route is HUD prompt → main menu → Tips and no gameplay binding was added at all. Five pre-existing defects found outside the feature's scope and filed as **BUG-090…094**, two of which change how much other gates can be trusted: a listen-server host never receives its own owner-targeted RPCs (so host-side sell toasts and transfer progress are probably dead), and `check-input-conflicts.py` cannot see 197 inline-declared actions, which makes every past "no new conflicts" result weaker than it read.
- v1.9 (2026-08-06): **core/player-groups built (issue #147, second half) — 30/31 tasks, 6 phases in one autorun, awaiting MP play-test.** Players can finally squad up. Every player's group is now **private by default** and named after them; joining someone else's goes through vanilla's request/approve (and invite/accept) flow behind an **Overthrow confirmation dialog** that names the other leader and your exact recruit count; and the governing rule — **recruits are owned by a player, not a group** — is enforced server-side: they follow their owner into and out of any group, the group's leader commands them while they are there, but dismiss/rename/inventory/loadout/XP never transfer and they come back when you leave, switch or reconnect. New `OVT_PlayerGroupManagerComponent` (stateless server-side reactor on `SCR_AIGroup`'s membership invokers, **no replicated Overthrow state, nothing persisted**), a world-free `OVT_GroupRecruitTransfer` helper, and three `modded class` overrides on the vanilla group stack. **No new client→server RPC** (D1) — nothing added to the frozen `OVT_PlayerCommsComponent`, no getter on `OVT_Global`, both verified by empty diffs. **Phase 1's diagnosis cleared the Group Menu**: all of #147's "can't manage the Group menu" was downstream of BUG-088's single un-replicated faction map (no faction → no tiles → no selection → Join never appears); the greyed-out "Create new group" is vanilla-by-design. But the build then found **four defects nobody predicted**, two of them severe: **`SetName` was the wrong API** so group names never displayed *even in single-player*; **vanilla ships no Leave action at all** and its stand-in ("Create new group") was permanently disabled by a stray empty faction group, so **no player could ever leave a group**; fixing the leader-approve path (dead on dedicated servers — the BUG-088 RPC-direction family) silently activated a reconnect path that violated the "your group is yours" decision; and a **disconnect never reached the recruit pull-out**, leaving an ex-leader in command of a departed player's AI. All four fixed. Also closed by the same work: recruiting while inside a shared group did nothing (leader guard → membership guard), an unbounded 500 ms respawn-retry loop (capped at 10), and a vanilla hole where a rejected join to a full group **stranded the player with no group at all**. Coverage: +1 Init +5 Logic cases, each proven able to fail with a deliberate injected fault (Fast 38, All 60 → **66**). Localization: 11 `#OVT-` keys in the `.st` master only — the runtime exports were regenerated by the user mid-build, leaving 2 keys for one more pass. **Manual gate DISCHARGED same day:** the user ran the dedicated-server MP tests and found **not a single problem**, explicitly including the hardest uncovered case — a group leader with joined players *and* recruits disconnecting, which promoted a new leader who then commanded the group's recruits (leader promotion × recruit ownership × the disconnect pull-out all firing in one teardown). Feature **complete**. Left over: one Workbench export for 2 notification keys that have no code fallback, and the optional AI-density measurement (`MAX_SLAVE_AI_PER_GROUP` ships at 0 = disabled).
- v1.8 (2026-08-04): **economy/shop-ux built (issue #145) — 37/37 tasks, 7 phases, awaiting play-test.** Shop menu rebuilt around **category tabs** (mode-first arsenal mapping — magazines finally leave the Weapons tab) and a **Buy/Sell mode split** where Sell browses the *player's own unequipped inventory* aggregated by resource id, with grey-out + localized reasons for items a shop won't buy. Selling is now **server-authoritative** on the new `OVT_ShopTransactionComponent` (`OVT_OverthrowController`): one shared bulk-sell routine validates shop/range/type/price server-side and credits only what it deletes; the legacy client-trusted `OVT_PlayerCommsComponent.RpcAsk_Sell` (+ AK74/RPG7 remap) is **deleted** — BUG-020's shop half closed, real-estate half still open. **BUG-024 pagination died by construction** (tested model, `Math.Ceil` float division). Returning A3 feature: **vehicle trunk "Sell All"** user action on `Vehicle_Base.et`, shown only in range of a shop with a sell path. Feedback layer: live stock refresh invoker, `PurchaseFailedInventoryFull` finally sent + in-menu toast (HUD strip is hidden while menus are open — new notification invoker), HUD **money-delta ticker**. QOL: `FormatMoney` thousands separators everywhere touched, alphabetical port/warehouse lists, warehouse **Take All** + stale-refresh fix, real-estate zero-buildings NPE guards + 8 feedback messages, Sell rebound KC_S→KC_F (reserved-key collision). Coverage: +5 Logic + 1 Campaign cases (all proven red once; Fast 27, All 52). Play-test found **BUG-083** (engine's `PURPOSE_DEPOSIT` includes worn gear — loadout storage is `0x9`); fixed same-session with per-item slot classification, plus a review-found container-cascade fix (selling a loaded backpack destroyed contents uncredited). Manual gate outstanding: menu/gamepad pass, trunk action, MP/JIP steps.
- v1.7 (2026-08-03): **towns epic created — 5/5 features backfilled with retrospective docs.** New epic `docs/features/towns/` (`core`, `stability`, `support`, `gun-dealers`, `map-info`) documenting the civilian heart of the campaign: the 1579-line `OVT_TownManagerComponent` + world-authored controllers (discovery, population growth, village flips, index-addressed RPC/JIP sync, location-matched persistence), the shared modifier framework + 11 stability modifiers (derived `clamp(100+Σ)` value), the support random walk (±1 supporter/70 s with equilibrium damping — 13 modifiers, conversion, posters, momentum), per-town gun dealers (catalog-query stock, one random heavy weapon per category as the travel mechanic), and the vanilla-map extension (config delta at vanilla's GUID: town panel, icons with RplId retry, overlays, fast/bus travel). Five parallel very-thorough discoveries catalogued ~100 concrete issues — headliners: **permanent modifiers are wiped whenever any other modifier expires** (tick-rebuild defect + clients keep stale chips forever), **hour-gated handlers only process `m_Towns[0]`** (shared-instance state — patrol harassment and growing discontent dead for every other town), **supporter conversion is fully client-trusted** (`RpcAsk_AddSupporters(anyLocation, 9999)` flips every village), **unvalidated town RPC surface** (server crash / RecentBattle spam), handler `OnStart` never running (game-start seeding dead), **dealer weapon stock has never restocked** (save/load reroll is the only refill; BUG-005's X-only check found in 4 more sites incl. the spawner), support>population breaking every percentage gate, town-patrol modifier names that exist in no config, the map icon-validation pass running **every frame** (ms/s units), icon parallel-array desyncs, leaky map close paths (stuck panel, surviving bus mode), and drawn-vs-enforced restriction radii mismatches. Top 17 filed as **BUG-055…071**; cross-feature debt seeded in the epic's Tech Debt section. Docs reconciled mid-flight against the concurrent economy-bugfix session (`e82b892`: BUG-020 closed — sell now server-authoritative, transaction events fire on sells). Coverage today: 6 Logic town cases + Init/Campaign/Persistence seams; the modifier tick, handlers, queries and the entire map layer are uncovered.
- v1.6 (2026-08-02): **resistance epic created — 4/4 features backfilled with retrospective docs.** New epic `docs/features/resistance/` (`core`, `building`, `recruits`, `loadouts`) documenting the player's side of the war: the resistance faction manager (camps, FOBs with prefab-swap mobile deploy/undeploy, garrisons, officers), the placement/construction system (8 placeables / 7 buildables, ghost previews, item limits), persistent AI recruits (civilian/tent recruiting, offline body save-and-release respawn, XP), and equipment loadouts (recursive item-tree capture, conservation-based box apply, officer templates). Four parallel very-thorough discoveries catalogued dozens of concrete issues — headliners: **loadout box-apply spawns the equipped weapon + attachments from thin air** (infinite duplication in normal play), **officer promotion is a silent no-op on dedicated servers** (client broadcast that never reaches the server; no RpcAsk exists), **FOB deploy/undeploy spawns before validating** (vehicle+cargo duplication fall-throughs + ghost FOB records from exact position matching), **unvalidated client-trust RPCs across every feature** (place/build/garrisons/recruits/loadouts: unchecked indices are server-crash vectors, buildables never charged server-side, any character recruitable free, others' loadouts deletable), removal mode broken twice on dedicated (EntityID across the network + unreplicated ownership), recruit rename client-local, fast-travel leaving charged-for recruits behind, stowed weapons never captured in loadouts, and a **one-click VM error** from integer-division pagination in the place/build menus. Top 12 filed as **BUG-042…053**; cross-feature debt seeded in the epic's Tech Debt section; Logic-tier test candidates identified per feature.
- v1.5 (2026-08-02): **jobs epic created — 1/1 feature backfilled with retrospective docs.** New epic `docs/features/jobs/` (`core`) documenting the quest layer: the `ScriptAndConfig` condition/stage composition framework (8 conditions, 11 stages, false-means-advance protocol), the `OVT_JobManagerComponent` lifecycle (10 s poll, public vs player-allocated distribution, occupancy-derived/counter-stored book-keeping), JIP + 3 broadcast RPCs, the vanilla-persistence no-replay restore (cites `core/persistence` rather than restating it), the jobs menu UI, a per-job catalog of the 8 shipped configs and a "how to add a job" authoring guide (a `mission-statement.md` audience). Documented as ONE feature — a single ~1,400-line manager has no internal seam; a core/UI split would halve every MP finding. Discovery catalogued ~20 issues — headliners: **the tutorial chain is once-per-SERVER** (global `m_iMaxTimes` renders `m_iMaxTimesPlayer` inert; second player never onboards, likely regression from `f7bc36f`), **update-RPC identity omits owner** (two players' same-index jobs collide client-side while remove *does* key on owner), **declines never broadcast** (board drift), completion hints broadcast to everyone + double on listen hosts, and unguarded town/house/shop derefs in conditions/stages. Top 5 filed as **BUG-037…041** (BUG-005's X-axis dealer check already pinned by a deliberate test); cross-feature debt seeded in the epic's Tech Debt section. Coverage today: 4 Logic cases + 1 Init assert; manager/stages/impure conditions uncovered.
- v1.4 (2026-08-02): **occupying epic created — 4/4 features backfilled with retrospective docs.** New epic `docs/features/occupying/` (`core`, `base-upgrades`, `qrf`, `deployments`) documenting the AI antagonist: the faction manager's resources/threat economy and capture flow, the nine base-upgrade classes with value-banked proxying, the QRF battle controller, and the modular deployment framework (the stalled designated successor to base-upgrades). Four parallel very-thorough discoveries catalogued ~90 concrete issues — headliners: **unvalidated capture RPCs** (`RpcAsk_StartBaseCapture` validates nothing; `RpcAsk_InstantCaptureBase` is a shipped unauthenticated instant base flip), a **broken resource-spend chain** (dead per-base allocation so the top base drains the whole reserve + divide-by-zero at zero OF bases; QRFs read but never debit resources; upgrade allocation clamp dead; proxied-bank inflation), the **deployment-list leak** that silently halts all deploying at the 100 cap on long campaigns, seconds-vs-ms unit bugs, checkpoint compositions lost on load with slots left blocked, and **no victory/defeat condition anywhere**. Top 7 findings filed as **BUG-025…031**; cross-feature debt seeded in the epic's Tech Debt section; per-feature Future Enhancements prioritized. Epic-wide test coverage today is ~3 init assertions; Logic-tier candidates identified per feature.
- v1.3 (2026-08-02): **core/persistence built — the acceptance gate flipped.** Overnight autorun: plan v2 authored against the verified retail 1.7.0.54 API (`vanilla-api-reference.md`), then Phases 1-6 end-to-end. `SCR_PersistenceSystem` wired via the ChimeraSystemsConfig GUID-override + `Configs/Systems/Persistence/Overthrow.conf` (inherits vanilla Common.conf); `OVT_PersistenceManagerComponent` rewritten on `SaveGameManager` (honest async save UI, cached `HasSaveGame`, purge guard for dedicated servers); 12 manager serializers + entity persistence (placeables/buildables/deployments/jobs) + player edge cases (loadouts fixed end-to-end — they had never worked, recruit body respawn, vehicle ownership); vanilla AI self-spawn scoped out (would have doubled every garrison on continue); player spawn logic re-parented off EPF (restoring initial spawn, measurably dead before). `OVT_TEST_PersistenceRoundTripSuite` exit 1 → 0 (in-session save→dirty→re-apply), de-quarantined into All (41 cases, ~19s, `run-tests.sh` auto-resets CI save state). EPF/EDF removed entirely (zero references repo-wide; compile set 6001→5890 files; addon list = Overthrow only). Save tooling migrated to the empirically discovered vanilla layout (`profile/.save/<app_user>/game/...`). ⚠️ Breaking: all EPF saves dead, no converter. Remaining: 20-item manual play-test.
- v1.2 (2026-08-02): **dev-ops/test-coverage complete (61/62; 1 optional deferred)** — Overthrow's first real coverage: 30 behaviour-level assertions across four setup-cost tiers (Logic 14 world-free ~8s, Init 4, Campaign 4, Persistence 8 same-session), every case with a recorded can-fail proof, no `maxAttempts` anywhere. Two stable CI targets shipped as `SCR_AutotestGroup` configs: Fast `{6A6E29FF47ECB840}` (18 cases ~16s) and All `{6A6E2A002F53A581}` (30 cases ~19s). The quarantined `OVT_TEST_PersistenceRoundTripSuite` (9 cases, red by design) is now `core/persistence`'s machine-checkable acceptance gate (exit 1 → 0 = migration done). Save-state tooling made automation-safe (`.scripts/` argument forms, `--profile`, destructive-path guard). 11 gameplay/content bugs found and filed (BUG-001…011, incl. FillAmmoboxes 62 VM exceptions/campaign start and the unconditional save-success UI); the plan's suspected integer-division defects proven not to exist. Docs corrected repo-wide (technical-design §10, mission-statement, workbench-workflow skill v1.3.0, CLAUDE.md). **dev-ops/autotest-foundation complete (22/22)** — Overthrow's first automated test loop: `tools/run-tests.sh` (launch → `junit.xml` → honest 0/1/2/124 verdict, ~15s/run) on the shipped `SCR_Autotest` framework. `Scripts/Game/Tests/` tree: modded `SCR_AutotestHelper`, `OVT_TEST_SuiteBase`, one smoke suite + a kept always-red `OVT_TEST_MetaSuite` proving the failure path. All three `-autotest` forms (suite/case/`{GUID}`) verified in the retail client; test code proven inert without `-autotest`. Coverage is deliberately one smoke test — real coverage is feature #3. Docs corrected (CLAUDE.md, technical-design, mission-statement, workbench-workflow skill v1.2.0, agent definitions).
- v1.0 (2026-08-01): **dev-ops/workbench-automation complete (56/56)** — First automated compile check for Overthrow: `tools/compile-check.sh` (Workbench `-wbsilent -validate`, honest 0/1/2/124 exit codes, gcc-style `file:line:` errors, false-pass guard) and `tools/launch-game.sh` (game-client launcher; proved the `-autotest` → `junit.xml` loop end-to-end). Empirical CLI reference in `findings.md`. Also fixed the tree to compile on Reforger 1.7.0.54 (vanilla-persistence WIP had never compiled: illegal generic method, fictional persistence API, template files in the compiled tree). Docs corrected across CLAUDE.md, technical-design, mission-statement, workbench-workflow skill and agent definitions. Created this master overview.
